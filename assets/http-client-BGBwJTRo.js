const e="## Send a request\n\nThe outbound HTTP client requires PHP cURL. `http()` without a URL returns its fluent service:\n\n```php\n$response = http()->timeout(10)->get('https://api.example.com/items', [\n    'page' => 1,\n]);\n\nif ($response->isSuccessful()) {\n    $items = $response->json();\n}\n```\n\nThe helper's positional arguments begin with the **method**, then the URL. Do not use an older `http($url)` example; use the fluent form or `http('GET', $url)`.\n\n## Methods and request options\n\nThe client exposes `get`, `post`, `put`, `patch`, and `delete`. Configure `method()`, `headers()` / `header()`, `accept()`, `contentType()`, `useragent()`, and `timeout()` before sending.\n\n```php\n$response = http()\n    ->header('Authorization', 'Bearer ' . $token)\n    ->accept('application/json')\n    ->contentType('application/json')\n    ->post('https://api.example.com/items', ['name' => 'Example']);\n```\n\n`postFields()` configures the body; `option()` / `options()` set cURL options. `cookie()` and `cookieJar()` manage outbound cookies; `proxy()` configures a proxy. Keep transport timeouts finite for request-facing work.\n\n## Response objects\n\nThe result is an `HttpResponse` object, not the array shown in older documentation.\n\n| Method | Result |\n| --- | --- |\n| `status()` | HTTP status code |\n| `body()`, `text()` | Response body |\n| `json($key, $default)` | Decoded JSON or a selected value |\n| `headers()`, `header()` | Response headers |\n| `lastUrl()`, `length()` | Transfer metadata |\n| `ok()`, `isOk()` | Any 2xx status (not just 200) |\n| `isSuccessful()` | Successful status family |\n| `isClientError()`, `isServerError()`, `failed()` | Failure checks |\n| `isRedirect()` | Redirect status check |\n\nCheck the status and expected response shape before using returned data. For a single request, exhausted transport retries produce `status() === 0` with an empty body. `failed()` is true for every non-2xx result, including status zero and redirects; check `status() === 0` when distinguishing a transport failure from a real HTTP response. Setup errors, such as invalid download paths or missing cURL, can still throw `HttpException`.\n\n## Request pools\n\n```php\nuse Spark\\Http\\Client\\Http;\n\n$responses = Http::pool(fn($pool) => [\n    $pool->as('catalog')->get('https://api.example.com/catalog'),\n    $pool->as('status')->get('https://api.example.com/status'),\n]);\n```\n\nThe pool runs requests through cURL multi and returns objects keyed by name, such as `$responses['catalog']`. Keys must be unique. Configure each returned request with `withToken()`, `withTimeout()`, or other `with*` methods. Unlike the single-request path, a pool transport failure throws `HttpException` and can prevent the result map from being returned; per-request retry settings are not applied by the multi executor. Valid HTTP 4xx/5xx responses still need individual status checks.\n\n## Downloads\n\n`download($location, $force)` sets a download destination before sending the request. Use a controlled local path and understand overwrite behavior before enabling `force`. Validate the origin and expected content when a download will later be processed or served.\n\n## External-service boundaries\n\nKeep provider credentials in configuration. Avoid arbitrary user-supplied URLs, define retry rules only for operations safe to retry, and avoid logging Authorization headers or sensitive response bodies. Use dependency injection or small test doubles in service tests rather than depending on a live API for every test run.\n\n## Retries and transport failures\n\nSingle requests retry cURL failures twice by default (three total attempts), with a 200 ms delay. HTTP error status codes do not trigger that retry loop. Set retry count and delay explicitly, especially for writes:\n\n```php\n$response = http()->withToken($token)\n    ->withTimeout(10)\n    ->withRetry(0)\n    ->get('https://api.example.com/profile');\n\nif ($response->status() === 0) {\n    // No usable HTTP response: report a temporary upstream failure.\n} elseif (!$response->isSuccessful()) {\n    // The remote service responded with an unsuccessful HTTP status.\n} else {\n    $profile = $response->json();\n}\n```\n\n`withRetry($times, $delayMs)` counts retries after the initial attempt. A timeout during a POST may happen after the remote service committed the write; use provider idempotency keys before automatically retrying it. Keep TLS verification enabled; `withoutVerifying()` is an explicit diagnostic escape hatch, not production setup.\n\n## JSON, forms, and multipart uploads\n\nArray bodies default to URL-encoded form data unless you select a content type. `withContentType('application/json')` JSON-encodes an array body; a string body is sent as supplied. `withBasicAuth($username, $password)` and `withToken($token)` configure authentication.\n\n```php\n$response = http()->withToken($token)\n    ->attach('document', storage_dir('reports/summary.pdf'), 'summary.pdf')\n    ->post('https://api.example.com/documents', ['title' => 'Summary']);\n```\n\n`attach()` accepts a file path, stream, or raw string contents. It switches to multipart and lets cURL generate the boundary; do not manually force a JSON content type. Temporary attachment files are cleaned by the request object. Use a fresh client for an unrelated request to avoid carrying headers or attachments forward.\n\n## Download outcomes\n\n```php\n$destination = storage_dir('reports/export.csv');\n$response = http()->withTimeout(30)->download($destination)\n    ->get('https://api.example.com/export');\n```\n\nCreate the parent directory first. By default an existing destination throws `RuntimeException`; `force: true` permits replacement. Downloads use a temporary file, but any nonzero HTTP response—including a 404 body—can be saved to the destination. Check the response before treating the file as a valid report. Download responses do not keep the streamed content in `body()`.\n";export{e as default};
