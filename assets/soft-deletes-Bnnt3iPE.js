const e="Soft deletion retains a row and marks it with a timestamp. Active rows have a null deletion timestamp; archived rows have a value. This supports trash screens and recovery while keeping foreign keys and historical data intact.\n\n## Add the column\n\nIn a new table migration, add both ordinary timestamps and the deletion column:\n\n```php\nuse Spark\\Database\\Schema\\Blueprint;\nuse Spark\\Database\\Schema\\Schema;\n\nSchema::create('posts', function (Blueprint $table) {\n    $table->id();\n    $table->string('title')->required();\n    $table->timestamps();\n    $table->softDeletes();\n});\n```\n\n`softDeletes()` returns `void` and creates a nullable `deleted_at` timestamp. It accepts no column-name argument. For an existing table, add the column in a new [migration](./migrations.md#add-soft-deletes-to-an-existing-table). Apply that migration before enabling the model behavior.\n\n## Enable the model\n\nSet `USE_SOFT_DELETES` on the model:\n\n```php\n<?php\n\nnamespace App\\Models;\n\nuse Spark\\Database\\Model;\n\nclass Post extends Model\n{\n    protected const USE_SOFT_DELETES = true;\n    protected string $table = 'posts';\n    protected array $fillable = ['title'];\n    protected array $casts = ['deleted_at' => 'datetime'];\n}\n```\n\n## Read active and archived rows\n\nThe following examples use the model above:\n\n```php\n$active = Post::orderDesc('id')->all();\n$all = Post::withTrashed()->orderDesc('id')->all();\n$trash = Post::onlyTrashed()->orderDesc('id')->paginate(20);\n$archived = Post::withTrashed()->findOrFail($id);\n```\n\n| Query | Included rows |\n| --- | --- |\n| Normal model query | Deletion column `IS NULL` |\n| `withTrashed()` | Active and archived rows |\n| `onlyTrashed()` | Deletion column `IS NOT NULL` |\n| `withoutTrashed()` / `withTrashed(false)` | Deletion column `IS NULL` |\n\nThe last trash-scope method wins: `withTrashed()->onlyTrashed()` selects archived rows, while `onlyTrashed()->withTrashed()` selects all rows. `withoutTrashed()` and `withTrashed(false)` explicitly return to active-only selection. Existing WHERE conditions remain in place. Count, existence, and pagination follow the selected scope.\n\n## Delete and inspect a record\n\n```php\n$post = Post::findOrFail($id);\n$removed = $post->remove(); // bool: marks deleted_at\n\n$archived = Post::onlyTrashed()->findOrFail($id);\n$isArchived = $archived->trashed(); // true\n```\n\n`remove()` returns a boolean. A constrained builder `delete()` or `destroy($idOrIds)` returns an affected-row count. Ordinary deletion selects active rows. `onlyTrashed()->delete()` re-stamps archived rows, and `withTrashed()->delete()` stamps both active and archived rows; neither performs a physical deletion. A repeated ordinary deletion returns zero/false after the row is archived.\n\nThe delete path sets the stored deletion timestamp but does not populate it on an already-loaded object. `trashed()` checks the object's loaded attributes, so re-fetch with `onlyTrashed()` / `withTrashed()` before inspecting it. `refresh()` uses the normal query and cannot reload an archived row. `wasDeleted()` records a delete operation; it is different from checking the stored archive state.\n\n## Restore records\n\n```php\n$post = Post::onlyTrashed()->findOrFail($id);\n$restored = $post->restore();\n\n$restoredAny = Post::whereIn('id', $selectedIds)->restore();\n$restoredAll = Post::onlyTrashed()->restore();\n```\n\nBoth restore entry points return **bool**, not the number of affected rows. The instance method requires a loaded nonempty deletion timestamp and primary key. The builder restores archived rows selected by its WHERE conditions and explicit scope. `onlyTrashed()->restore()` and `withTrashed()->restore()` restore all archived rows unless further filtered; `withoutTrashed()->restore()` changes nothing because it selects active rows. Without a WHERE or explicit scope, `Post::query()->restore()` returns false.\n\nCall the bulk method on a builder. `Post::restore()` is not a static shortcut because `Model::restore()` is an instance method. Use a fresh model after restoration when cached existence/tracking state matters. Restoration changes only the deletion column; it does not fire a dedicated restoring/restored event or automatically restore child records.\n\n## Permanently delete records\n\n```php\n$deleted = Post::onlyTrashed()->forceDelete(); // Purge the entire trash\n$deleted = Post::onlyTrashed()->where('user_id', $userId)->forceDelete();\n$deleted = Post::withTrashed()->whereIn('id', $selectedIds)->forceDelete();\n```\n\n`forceDelete()` delegates to `delete(..., force: true)` and returns an affected-row count. It preserves explicit trash scopes and WHERE conditions:\n\n| Query | Physically deleted rows |\n| --- | --- |\n| `Post::onlyTrashed()->forceDelete()` | All archived rows; active rows remain |\n| `Post::withTrashed()->forceDelete()` | All active and archived rows |\n| `Post::withoutTrashed()->forceDelete()` | Active rows only |\n| `Post::where('id', $id)->forceDelete()` | The matching ID, whether active or archived |\n| `Post::query()->forceDelete()` | None: unfiltered-write guard returns zero |\n\nWithout an explicit trash scope, force deletion bypasses the implicit active-only filter. On a loaded model, `$post->forceDelete()` adds its primary-key condition and returns the affected-row count. This also works for an archived instance. Add ownership/tenant conditions when the action is limited to one account; `onlyTrashed()` alone deliberately selects the whole trash.\n\n`truncate()` physically empties a table and does not implement soft deletion. Plain `query('posts')`, `db()->table('posts')`, and raw PDO SQL also have no model soft-delete behavior.\n\n## Bulk updates and operation boundaries\n\n| Operation on a bound soft-delete model | Behavior |\n| --- | --- |\n| `update()` | Updates the selected active / with-trashed / only-trashed rows |\n| `increment()`, `decrement()` | Applies read scope; does not require an explicit WHERE |\n| `delete()` | Sets the deletion timestamp on rows selected by the current scope |\n| `restore()` | Clears the timestamp on selected archived rows only; returns bool |\n| `forceDelete()` | Physically deletes selected rows; bypasses the implicit active-only scope |\n| `truncate()` | Physically empties the entire table, regardless of scope |\n\nFor `update()`, `delete()`, `forceDelete()`, and builder `restore()`, a WHERE condition or an explicit `onlyTrashed()`, `withTrashed()`, or `withoutTrashed()` selection satisfies the write guard. The implicit default scope does not. On a plain table builder or a model without soft deletes, these switches do not authorize an unfiltered write.\n\nUse a fresh builder for each write because writes clear its condition/binding state. Bulk operations do not automatically cascade archive/restore actions, refresh loaded models, or dispatch per-row lifecycle callbacks.\n\n## Custom deletion column\n\nOn a model with `USE_SOFT_DELETES = true`, override the column constant and cast:\n\n```php\nprotected const SOFT_DELETE_COLUMN = 'archived_at';\nprotected array $casts = ['archived_at' => 'datetime'];\n```\n\nCreate the matching column explicitly with `$table->timestamp('archived_at')->nullable()`. Do not add `deleted_at` as well unless your application has a separate use for it. Adapt explicit purge conditions and relationship filters to the custom name.\n\n## Relationships, joins, and uniqueness\n\nRelated model reads inherit that model's own query behavior. Enable `USE_SOFT_DELETES` on each model that needs it; a parent setting does not enable it on children. A relation or eager-load callback can use `withTrashed()` to include archived related records.\n\nRelationship writes preserve the parent foreign-key condition:\n\n```php\n$user->posts()->onlyTrashed()->forceDelete();\n$user->posts()->onlyTrashed()->restore();\n```\n\nThese affect only that user's related posts. `whereHas()` and relationship aggregates currently build subqueries from raw WHERE conditions, bypassing the prepared soft-delete scope. Add a qualified condition such as `whereNull('posts.deleted_at')` in their callbacks when counting or filtering active children. See [Relationships](./relationships.md#soft-deleted-related-records).\n\nThe generated deletion predicate is unqualified. A join where multiple tables have a deletion column can be ambiguous. Use an explicitly filtered table query or `withTrashed()` plus qualified `whereNull('posts.deleted_at')` conditions for read queries. Joining an intermediate/pivot table does not automatically filter that table's archived rows.\n\nArchiving does not release a normal unique constraint. A new insert with an existing archived email/slug can still conflict; `firstOrCreate()` can also miss the archived row under the active scope. Decide whether the action should restore the existing record or use another unique value. Database `ON DELETE` actions apply to physical deletion, not to updating a deletion timestamp.\n";export{e as default};
