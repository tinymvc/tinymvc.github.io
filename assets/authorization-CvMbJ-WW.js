const e="## Define an ability\n\nGates name application permissions. Register them during provider boot:\n\n```php\ngate()->define('update-post', function ($post) {\n    $user = auth()->user();\n    return $user !== null && (int) $post->user_id === (int) $user->id;\n});\n```\n\nThe current Gate calls the ability with the arguments you pass, through the container. It does **not** automatically prepend the authenticated user. Read `auth()->user()` in the callback, as above, or explicitly pass the user along with the record. For an object passed positionally to an ability, keep its callback parameter untyped: the current container treats a class-typed positional parameter as a service to resolve rather than using the supplied object. Check the object type inside the callback if callers may pass different types. This differs from examples in the older guide.\n\n## Check and enforce\n\n```php\n$post = Post::findOrFail($id);\nauthorize('update-post', $post);\n\n$post->fill($validated);\n$post->save();\n```\n\n`authorize()` throws `AuthorizationException` on denial, mapped to HTTP 403. `can()` returns a boolean; `cannot()` returns the inverse. Unknown abilities are denied by the normal gate evaluation.\n\n`gate()->allows()` / `denies()` are the service equivalents. `has()` checks registration, and `abilities()` returns registered definitions.\n\n## Multiple abilities\n\n`canAny(['edit-posts', 'manage-posts'], $post)` or `gate()->any(...)` allows when any supplied ability passes. `gate()->none(...)` checks that all are denied. Supply arguments matching each ability's callback.\n\n## Before and after callbacks\n\n`before()` callbacks run before normal evaluation; a non-null result overrides it. Returning `null` continues to the ability. `after()` receives the ability name, result, and supplied arguments, and can also override with a non-null value.\n\nUse overrides sparingly and test them. An unconditional `true` in a before callback grants every checked ability, including ones that would otherwise be undefined.\n\n## Blade and route boundaries\n\n```html\n@can('update-post', $post)\n    <a href=\"{{ route_url('posts.edit', ['id' => $post->id]) }}\">Edit post</a>\n@endcan\n```\n\n`@cannot()` renders the inverse state; `@authorize()` enforces a check. Hiding a button is not server-side enforcement. Always authorize the action where the request changes or reads protected data.\n\n## Test access rules\n\nTest guests, owners, other users, and privileged roles against the same resource. Include missing and deleted records. For multi-tenant applications, apply tenant constraints in both lookup queries and permission checks. See [Testing](./testing.md).\n";export{e as default};
