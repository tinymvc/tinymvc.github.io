const e="## Authentication service\n\n`Spark\\Http\\Auth` resolves the current identity through configured channels. `auth()` returns the service; `user()` returns the current user or a selected attribute. The default model is `App\\Models\\User`.\n\nNamed **guards** give different user models or login areas their own Auth instances. **Channels** determine where each instance looks for credentials: session/remember cookie, bearer JWT, or HTTP Basic. A guard name does not automatically select a channel or configure a separate session key.\n\nThe skeleton's user model hides `password` and `remember_token` and casts `password` as `hashed`. Your database and fillable fields must match the application model.\n\n## Check credentials\n\n```php\n$credentials = $request->validate([\n    'email' => 'required|email',\n    'password' => 'required|string',\n]);\n\nif (!auth()->attempt($credentials->toArray())) {\n    return back()->withErrors(['email' => 'The credentials do not match.']);\n}\n\nsession()->regenerate(true);\nreturn redirect(auth()->getRedirectRoute());\n```\n\n`attempt()` looks up the user using credential fields other than `password`, verifies the password, and logs the user in on success. Pass an intentional set of credentials. Rate-limit the login endpoint, keep browser CSRF protection, and use HTTPS.\n\nFor a known, verified user, `auth()->login($user, remember: true)` supports remembered login. Call it only after verifying the identity or an authorized account-switch flow.\n\n## Inspect and end a login\n\n| Method | Meaning |\n| --- | --- |\n| `getUser()`, `user()` | Current model, or `null` for a guest |\n| `user('email', $default)` | Current user's attribute |\n| `id()`, `getId()` | User ID; 0 when unauthenticated |\n| `check()`, `isLogged()` | Authenticated user is available |\n| `isGuest()` | No authenticated user |\n| `hasId()` | An identity value is present |\n| `refresh()` | Refresh user state |\n| `clearCache($id)` | Invalidate a cached user |\n\n```php\nauth()->logout();\nsession()->invalidate();\nreturn redirect('/login');\n```\n\nUse `check()` for an authenticated-user decision rather than only checking that an ID exists.\n\nFor a named guard, use `auth('admin')->logout()`. This clears that guard's configured session key and remember cookie. `session()->invalidate()` clears the whole session, including other guards using it; reserve it for a complete browser-session logout.\n\n## Configure the service\n\nThe current constructor is `new Auth(?string $model = null, array $config = [])`. It no longer takes a Session object as its first argument.\n\nConfigure the existing service with `auth()->configure([...])`, or bind a customized Auth instance in your provider's `register()` method before first use.\n\n| Option | Current default | Purpose |\n| --- | --- | --- |\n| `session_key` | `user_id` | Session identity key |\n| `cache_enabled` | `false` | Cache loaded users |\n| `cache_name` | `auth_cache` | Cache namespace |\n| `cache_expire` | `10 minutes` | User cache duration |\n| `login_route` | `login` | Named guest destination |\n| `redirect_route` | `dashboard` | Named authenticated destination |\n| `cookie_enabled` | `true` | Allow remembered login |\n| `cookie_name` | `auth` | Remember-cookie name |\n| `cookie_expire` | `6 months` | Remember-cookie duration |\n| `jwt_expire` | `6 months` | Default token duration |\n| `channels` | `session`, `jwt`, `basic` | Identity channels to inspect |\n| `validate_jwt_hash` | `false` | Additional JWT hash validation |\n| `use_remember_token` | `false` | Use the model's remember token |\n| `driver` | `null` | Custom authentication driver |\n\nRegister the configured named routes. Choose token/cookie lifetimes and enabled channels explicitly for your application; defaults are not a universal policy.\n\nEnabled channels are checked in the fixed order **JWT → Basic → session**, regardless of their order in the array. The first valid identity wins. Remember-cookie recovery belongs to the session channel. Configure browser-only guards with `['session']` and bearer-only guards with `['jwt']` when that separation is intended.\n\n## Register named guards\n\nRegister guards in a service provider's `register()` method, before routes or middleware use them. For example, after creating your application's `Admin` model and its table:\n\n```php\nuse App\\Models\\Admin;\nuse Spark\\Http\\Auth;\n\nAuth::register(\n    model: Admin::class,\n    config: [\n        'channels' => ['session'],\n        'session_key' => 'admin_id',\n        'cookie_name' => 'admin_auth',\n        'cache_name' => 'admin_auth_cache',\n        'login_route' => 'admin.login',\n        'redirect_route' => 'admin.dashboard',\n    ],\n    guard: 'admin',\n);\n```\n\n`register(string $model, array $config, string $guard): void` registers a lazy singleton under `auth.<guard>`. It configures authentication; it does not create a user account. Use different session keys, cookie names, and cache names for independent guards. Define the named routes used in the configuration.\n\n`default` is reserved: `Auth::guard('default')` resolves `Spark\\Http\\Auth` from the container. Registering `auth.default` does not replace it. To customize the default service, bind the class before its first resolution:\n\n```php\n$this->app->singleton(\n    \\Spark\\Http\\Auth::class,\n    fn () => new \\Spark\\Http\\Auth(\n        model: \\App\\Models\\User::class,\n        config: ['channels' => ['session']],\n    ),\n);\n```\n\n## Use a guard consistently\n\n```php\nuse Spark\\Http\\Auth;\n\n$adminAuth = Auth::guard('admin'); // same registered instance as auth('admin')\n$signedIn = $adminAuth->attempt(['email' => $email, 'password' => $password]);\nif ($signedIn) {\n    session()->regenerate(true);\n}\n\n$admin = user(guard: 'admin');\n$email = user('email', 'Guest', guard: 'admin');\n$signedIn = is_logged('admin');\n$guest = is_guest('admin');\n\n$admin = $request->user(guard: 'admin');\n$email = $request->user('email', 'Guest', guard: 'admin');\n$signedIn = $request->isAuthenticated('admin');\n$guest = $request->isNotAuthenticated('admin');\n$adminAuth = $request->auth('admin');\n```\n\n`Spark\\Facades\\Auth::guard('admin')` is also available. Selecting a guard does not change the default: later `auth()` and `user()` calls still use the default service. Register a named guard before selecting it; unknown names fail container resolution. Keep the same guard through login, middleware, controllers, gates, and logout.\n\nIn Blade, use `@auth('admin') … @endauth` and `@guest('admin') … @endguest`. Omitting the argument uses the default guard. See [Blade templates](./blade.md).\n\nJWTs identify the user model through the `prv` claim, not the guard name. Two JWT-enabled guards using the same model and app key are not isolated token audiences merely because their names differ. Use application-specific token validation when separate audiences are required.\n\n## Protect routes\n\nUse application middleware that checks the intended guard and returns a redirect or JSON 401 for guests. The skeleton registers an `auth` alias in `bootstrap/middlewares.php`; the working standalone example below can replace that alias's class.\n\nThe recent core adds an abstract `AuthMiddleware` intended to accept guard names, allow a request when any guard matches, and call an overridable `failed()` method (401 by default). In the current checkout, `Foundation/Http/Middlewares/AuthMiddleware.php` lacks its namespace, so the skeleton's namespaced parent cannot autoload. Its handler also strips `!` before testing for negation, so `!admin` does not mean “guest of admin.” Until those implementation issues are corrected, use the standalone middleware below and explicit `is_guest('admin')` checks for guest-only routes.\n\nAuthentication establishes identity. Use [gates](./authorization.md) to decide whether that identity may edit a specific record. Blade's `@auth` and `@guest` only control rendered UI.\n\n## Token and basic channels\n\n`getJwtToken($user, $payload)` creates a user token; `createJwtToken($payload)` creates a token for the currently authenticated user and fails when there is no user. Clients use an Authorization bearer header when accessing token-protected routes. Configure channels and expiration deliberately; do not put sensitive data in a token payload because signing is not encryption.\n\n`Spark\\Utils\\JWT` exposes `encode`, `decode`, header extraction, signing, and URL-safe base64 helpers. Keep verification enabled when decoding untrusted tokens. A custom token design must handle expiry, key management, and revocation requirements.\n\nThe basic-auth channel reads HTTP basic credentials and looks up the model's `username` field. It does not reuse an email-based `attempt()` form automatically. Enable it only where intended and use HTTPS.\n\n## Custom drivers and cache invalidation\n\nA driver implements `Spark\\Contracts\\Http\\AuthDriverContract`: `login()`, `logout()`, `getUser()`, and `checkId()`. The Auth service delegates to a configured driver. Match both the driver's declared return contract and the Auth wrapper's nullable-user behavior when implementing a guest result.\n\nWhen user caching is enabled, clear or refresh cached identity data after changing a user's role, status, or other authorization-relevant attributes. Test login, invalid credentials, guest access, logout, and stale/deleted users.\n\n## A route protection middleware\n\n```php\nnamespace App\\Http\\Middlewares;\n\nuse Closure;\nuse Spark\\Contracts\\Http\\MiddlewareInterface;\nuse Spark\\Http\\Request;\n\nclass Authenticate implements MiddlewareInterface\n{\n    public function handle(Request $request, Closure $next, ...$guards): mixed\n    {\n        $guards = $guards ?: ['default'];\n\n        foreach ($guards as $guard) {\n            if (auth($guard)->check()) {\n                return $next($request);\n            }\n        }\n\n        return $request->expectsJson()\n            ? json(['message' => 'Unauthenticated.'], 401)\n            : redirect(auth($guards[0])->getLoginRoute());\n    }\n}\n```\n\nRegister `'auth' => \\App\\Http\\Middlewares\\Authenticate::class` in `bootstrap/middlewares.php`. Define each guard's named login route outside its protected group.\n\n```php\nuse Spark\\Facades\\Route;\n\nRoute::get('/account', fn () => view('account'))->middleware('auth');\nRoute::get('/admin', fn () => view('admin.dashboard'))->middleware('auth:admin');\nRoute::get('/shared', fn () => view('shared'))->middleware('auth:default,admin');\n```\n\nThis example accepts plain guard names and allows **any** listed guard. It does not switch the default identity for downstream code; read the intended guard explicitly. For a guest-only login route, check `auth('admin')->isGuest()` and redirect authenticated users to that guard's `getRedirectRoute()`.\n\n## Issue a user token\n\nAfter independently verifying the credentials and loading the correct user:\n\n```php\n$token = auth()->getJwtToken($user, ['exp' => time() + 3600]);\nreturn json(['access_token' => $token, 'token_type' => 'Bearer']);\n```\n\nThe user-token helper includes the user's identity and supports payload overrides. Keep `sub` and other identity claims application-controlled. The built-in authentication identity uses a numeric `id`; a model with a UUID or differently named identity key needs a deliberately implemented authentication driver rather than only changing `$primaryKey`.\n\nLogout clears the current session/remembered login. It does not maintain a blacklist of every previously issued bearer token. Plan token lifetime and revocation for the application, and separately implement registration, password reset, verification, and account status rules. [Orbit](./orbit.md) supplies starter authentication screens and flows.\n";export{e as default};
