const e="These notes reconcile the previous public documentation with the current framework source. They are an API migration checklist, not an automatic upgrade script. Review your Composer constraints and application tests before updating a working application.\n\n## Project and configuration\n\n| Older example | Current approach |\n| --- | --- |\n| `tinymvc/tinymvc` starter | `tinymvc/skeleton` |\n| Flat `env.php` settings | `.env` plus `config/*.php` arrays |\n| `config('debug')` | `config('app.debug')` |\n| Database `user` | `username` in the connection config |\n| Shared cache/queue setup | Separate `cache` and `queue` configuration |\n| Old Vite build paths/port | Skeleton uses `public/build`, manifest `.vite/manifest.json`, port 5173 |\n\n## Commands\n\nUse `php spark migrate` rather than `migrate:run`, and `php spark queue:work` rather than `queue:run`. Register custom commands through a real file loaded by `withRouting(commands: ...)` or `withCommands(load: ...)`.\n\nQueue logging arguments and Mail-specific logging methods are no longer public APIs. Use application logging around those services.\n\n## Models and queries\n\n- Model `$table` and `$primaryKey` are protected instance strings, not public static properties.\n- Create an empty model then `fill()`, or use `Model::create()`; the constructor takes no data array.\n- `load()` is an instance relationship-loading method. Use `loadRelations()` to load onto an array of models.\n- Delete a model instance with `remove()`; query `delete()` returns a row count.\n- `latest()` returns a builder ordered by `created_at`; finish with a retrieval call.\n- `select()` accepts an array, a string, or multiple column arguments; prefer an array for dynamic lists.\n- `all()` returns an array; `get()` returns a Collection.\n- `bulkUpdate()` is not declared by the current builder. Use a transaction with reviewed updates.\n- Insert declarations return integer IDs; update/delete return affected-row counts. Do not depend on old returned-row array examples.\n- `upsert($data, $conflict = null, $update = null)` now takes separate conflict/update arrays. Replace the old second-argument configuration array; an empty update array now selects all supplied non-conflict columns. See [Upserts](./query-builder.md#upserts-and-lookup-helpers).\n- Soft deletes require a nullable deletion column and model opt-in. Model binding and WHERE composition are handled in core; remove the old application compatibility overrides after updating. See [Soft deletes](./soft-deletes.md).\n- Builder `update()` / `delete()` require a WHERE or explicit soft-delete scope. `onlyTrashed()->forceDelete()` now preserves active rows; `withTrashed()->delete()` also re-stamps archived rows. `withoutTrashed()` and `withTrashed(false)` explicitly select active rows; the last scope wins. Increment/decrement and truncate have different boundaries.\n- Prefer classic accessors or custom casts; the current Attribute-object dispatch needs a core correction.\n\n## Authentication and gates\n\nAuth's constructor is `new Auth($model, $config)`, without a leading Session object. Config uses `login_route` and `redirect_route`. A guest `getUser()` result is nullable; do not use a false-only check.\n\nThe current Gate passes the arguments supplied to the ability. It does not prepend the authenticated user automatically. Read `auth()->user()` inside the callback or pass the user explicitly. Keep supplied object parameters untyped in Gate callbacks; class-typed positional parameters are resolved from the container. See [Authorization](./authorization.md).\n\n## Helpers and utilities\n\n| Older assumption | Current behavior |\n| --- | --- |\n| `session('key', $value)` writes | It reads with a default; set with an array or `set()` |\n| `http($url)` | First helper argument is the method; prefer `http()->get($url)` |\n| HTTP response is an array | Use the HttpResponse object |\n| `lock('key')->withLock(...)` | `lock('key')` returns a boolean; get the service without a key |\n| Image operations are chainable | `resize`, `compress`, and `rotate` return booleans |\n| Mail `addAttachment()` | Use `attachFile()` |\n| Uploader maximum is bytes | `maxSize` is KB |\n| Job closures are accepted | The current Job wrapper accepts strings/callable arrays |\n\n## Integrations\n\n`Route::inertia()` requires the Inertia service provider. Match the client and adapter major versions; the current adapter branch targets Inertia v3 and uses `optional()` in place of old `lazy()` props. Orbit's locked dependencies may target a different compatible release set.\n\n## Update safely\n\nReview configuration and route aliases, fix calls against the [API reference](./api-reference.md), test validation/authentication/CSRF, and run your application's tests. Verify database-specific behavior on the production driver. Preserve application keys and persistent data during the update.\n";export{e as default};
