const e="## Define a model\n\n```php\n<?php\n\nnamespace App\\Models;\n\nuse Spark\\Database\\Model;\n\nclass Post extends Model\n{\n    protected string $table = 'posts';\n    protected array $fillable = ['title', 'body', 'published'];\n    protected array $casts = ['published' => 'boolean'];\n}\n```\n\nGenerate a starting file with `php spark make:model Post`. Table names default to the plural snake-case model name, and the primary key defaults to `id`. Override the protected string `$table` and `$primaryKey` properties when needed; they are not static properties.\n\n## Timestamps\n\nModels manage `created_at` and `updated_at` by default. Include compatible columns in the migration. Change timestamp behavior through the model constants:\n\n```php\nprotected const USE_TIMESTAMPS = false;\n```\n\n`CREATED_AT` and `UPDATED_AT` name the columns. Timestamp values are decoded as dates when enabled.\n\n## Create records\n\n```php\n$post = Post::create([\n    'title' => 'Hello, Spark',\n    'body' => 'A small beginning.',\n    'published' => false,\n]);\n```\n\n`create()` accepts an array or `Arrayable` such as validated input. To build an unsaved instance, instantiate without arguments and call `fill()`:\n\n```php\n$post = new Post();\n$post->fill(['title' => 'Draft', 'body' => 'Work in progress']);\n$post->save();\n```\n\n`firstOrCreate($attributes, $values)` retrieves or inserts. `firstOrNew()` returns an unsaved model when nothing matches. `createOrUpdate($data, $uniqueBy, $values)` supports explicit conflict handling. Back lookup keys with database constraints when duplicate concurrent inserts would be invalid.\n\n## Mass assignment\n\n`fill()` stores supplied attributes on the object; `$fillable` / `$guarded` control the data returned by `getFillableData()` for persistence. A nonempty `$fillable` list takes precedence. With no fillable list, `$guarded` excludes exact field names; an empty guarded list permits fields. Do not assume `$guarded = ['*']` is a wildcard in this implementation. Prefer a deliberate fillable list for user-controlled writes.\n\nValidate input and authorize the action before filling a model. Do not let a public profile request set fields such as `is_admin`, `role_id`, or account ownership simply because they exist in the database.\n\n## Query records\n\n```php\n$posts = Post::where('published', true)->latest()->take(20)->all();\n$post = Post::find($id);         // Model or false\n$post = Post::findOrFail($id);   // Not-found exception on a miss\n```\n\nStatic model queries use the [query builder](./query-builder.md). `all()` returns an array; `get()` returns a collection. `latest()` configures descending `created_at` ordering and needs a retrieval call.\n\n## Update and delete\n\n```php\n$post = Post::findOrFail($id);\n$post->fill(['title' => 'A clearer title']);\n$post->save();\n```\n\n`save()` returns a boolean. `remove()` deletes this model; use the [Soft deletes setup](./soft-deletes.md#enable-the-model) before relying on archival instead of physical deletion. `Post::destroy($id)` or an array of IDs deletes by primary key; query-builder `delete()` applies its filters and returns an affected row count.\n\n```php\nPost::findOrFail($id)->remove();\n```\n\nDirect query-builder writes do not provide all instance-level model behavior. Use model instances when you need casts, attribute transformations, and model lifecycle behavior.\n\n## Attributes and serialization\n\nRead/write attributes with properties, array access, or `get()` / `set()`. `isset()`, `unset()`, `primaryValue()`, `hasPrimaryValue()`, and `exists()` help inspect state.\n\n`toArray()` and `toJson()` serialize a model. Use `$hidden` to omit private attributes, `makeHidden()` / `makeVisible()` for explicit changes, and `$appends` for computed accessor values. `only()` and `except()` limit attributes on a model; `copy()` creates a separate instance.\n\n## Track changes\n\n`getOriginal($field)`, `getChanges()`, `isDirty()`, `isClean()`, and `hasChanges()` distinguish loaded and edited values. `getNestedOriginal()`, `getNestedChanges()`, and `isNestedDirty()` support nested data. `restoreOriginal()` discards unsaved changes; `refresh()` reloads from the database.\n\nTracking methods include `wasNewlyCreated()`, `wasCreated()`, `wasUpdated()`, `wasChanged()`, and `wasDeleted()`. See [Casts & model attributes](./casts.md) for transformation and tracking examples.\n\n## Relationships and extension points\n\nDefine relationship methods in the model, then eager-load them before rendering lists. See [Relationships](./relationships.md) for one-to-one, one-to-many, pivot, through, and polymorphic loading.\n\nModels support framework macro/conditional traits and forward query operations through their dynamic methods. Consult actual declarations in the [API reference](./api-reference.md) rather than assuming an Eloquent method exists.\n\n## Soft deletion\n\nAdd `$table->softDeletes()` in the migration and opt into the model behavior with `USE_SOFT_DELETES`. The core model handles query binding and archive filtering automatically; see [model setup](./soft-deletes.md#enable-the-model).\n\nWith that setup, `remove()` and constrained `delete()` / `destroy()` mark the deletion timestamp; ordinary reads exclude those rows. `withTrashed()` includes all rows, `onlyTrashed()` selects archived rows, and `withoutTrashed()` selects active rows. These scopes also apply to deletion. `onlyTrashed()->forceDelete()` purges archived rows, while `onlyTrashed()->restore()` clears their timestamps. Re-fetch an archived instance before calling `trashed()` because deletion does not update the previously loaded attributes.\n\nFollow [Soft deletes](./soft-deletes.md) for complete examples, return types, custom columns, explicit purge conditions, and relationship limitations.\n\n## Local query scopes\n\nPut reusable conditions in public `scope<Name>()` methods on your model:\n\n```php\npublic function scopePublished(\\Spark\\Database\\QueryBuilder $query): \\Spark\\Database\\QueryBuilder\n{\n    return $query->where('published', true);\n}\n```\n\nCall `Post::published()->orderDesc('id')->all()`. Additional scope arguments follow the builder parameter. Scope methods should return the builder to preserve chaining. Unknown builder methods can fall back to Collection methods and execute the query; a familiar method name is not necessarily a database-side operation.\n\n## Model lifecycle callbacks\n\nOverride `events()` on the model to return `Spark\\Database\\Events`:\n\n```php\nprotected function events(): \\Spark\\Database\\Events\n{\n    return \\Spark\\Database\\Events::make(\n        created: function () { tracer_log('Post created: ' . $this->primaryValue()); },\n        updated: function () { tracer_log('Post updated: ' . $this->primaryValue()); },\n        deleted: function () { tracer_log('Post deleted: ' . $this->primaryValue()); },\n        changed: function () { /* Invalidate application state here. */ },\n    );\n}\n```\n\nCallbacks receive no arguments; use `$this` in a non-static closure. `changed` runs before the specific created/updated/deleted callback when the corresponding tracking method is invoked. `Events::none()` disables callbacks; `hasEvents()` / `hasEvent()` inspect a callback set.\n\nUse instance `save()` / `remove()` for an operation tied to a loaded object. A bulk builder operation does not load each affected model or fire a callback per row. Callbacks occur during persistence, not after transaction commit. Delay external side effects until the transaction succeeds. There are no built-in restoring/restored callbacks in this event set.\n\n## Persistence and object state\n\n`create()` returns a model, while `save()` returns whether an insert/update succeeded according to the write path. `save(forceCreate: true)` can attempt an insert when a supplied primary key is not found; it is not a database-native upsert. `createOrUpdate()` and `firstOrCreate()` perform lookups and writes separately, so back their lookup keys with unique constraints.\n\n`copy()` clones an object including its primary key and loaded state. It is not a “duplicate this row” operation; use a new instance with selected validated attributes for that purpose. `only()` / `except()` return new model objects containing selected attributes rather than arrays. Convert them with `toArray()` for a response, and avoid saving a projection unintentionally.\n\nLoaded relations are included in serialization. Runtime `makeVisible()` is an allowlist; it does not remove fields from `$hidden`, and hidden fields still win. Appended accessors are added after that filtering stage, so only append values intended for the response. Keep ownership IDs and credentials out of public representations.\n";export{e as default};
