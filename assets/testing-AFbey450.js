const e="## Run the built-in test runner\n\nTinyMVC includes a plain PHP test runner. The skeleton provides the `test` entry point and Composer script; PHPUnit and Pest are not required.\n\n```bash\nphp test\ncomposer test\nphp test --testsuite Unit\nphp test --testsuite Feature\nphp test --filter home\nphp test --list-tests\ncomposer test -- --filter home\n```\n\nFailures, warnings, and an empty test selection produce a nonzero exit. Use that exit status in CI.\n\n## Unit tests\n\nPlace `*Test.php` files under `tests/Unit` and extend `Spark\\Testing\\TestCase`:\n\n```php\n<?php\n\nnamespace Tests\\Unit;\n\nuse Spark\\Support\\Str;\nuse Spark\\Testing\\TestCase;\n\nfinal class SlugTest extends TestCase\n{\n    public function test_names_become_snake_case(): void\n    {\n        $this->assertSame('hello_world', Str::snake('HelloWorld'));\n    }\n}\n```\n\nTest methods are public, non-static, start with `test`, and accept no arguments. Use `setUp()` / `tearDown()` for lifecycle setup and cleanup. When overriding application-test lifecycle methods, call the parent implementation.\n\n## HTTP feature tests\n\nPlace tests under `tests/Feature` and extend your application's `Tests\\TestCase`:\n\n```php\n<?php\n\nnamespace Tests\\Feature;\n\nuse Tests\\TestCase;\n\nfinal class HealthTest extends TestCase\n{\n    public function test_health_endpoint_returns_json(): void\n    {\n        $this->getJson('/health')\n            ->assertOk()\n            ->assertJsonPath('status', 'ok');\n    }\n\n    public function test_missing_page_returns_404(): void\n    {\n        $this->getJson('/this-page-does-not-exist')->assertNotFound();\n    }\n}\n```\n\nThe first test assumes the `/health` route from [Quick start](./quick-start.md). Responses wrap the framework's Response object; no browser or live HTTP server is needed for these feature helpers.\n\n## Request helpers\n\nUse `get`, `post`, `put`, `patch`, `delete`, `options`, and `head`, or the JSON variants `getJson`, `postJson`, `putJson`, `patchJson`, and `deleteJson`.\n\nFor a custom case:\n\n```php\n$response = $this->request('POST', '/api/posts', [\n    'title' => 'A test post',\n    'body' => 'Created in an isolated test.',\n], ['Accept' => 'application/json'], json: true);\n```\n\n`withHeaders()`, `flushHeaders()`, `withToken()`, `withSession()`, `withCookie()`, and `withCookies()` configure request state. `actingAs($user)` sets an authenticated model; `assertAuthenticated()` and `assertGuest()` inspect it.\n\n## Response assertions\n\n| Area | Assertions |\n| --- | --- |\n| Status | `assertStatus`, `assertOk`, `assertCreated`, `assertAccepted`, `assertSuccessful`, `assertNoContent` |\n| Errors | `assertUnauthorized`, `assertForbidden`, `assertNotFound`, `assertUnprocessable`, `assertTooManyRequests`, `assertServerError` |\n| Content | `assertContent`, `assertSee`, `assertDontSee` |\n| Headers | `assertHeader`, `assertHeaderMissing`, `assertRedirect` |\n| JSON | `assertJson`, `assertExactJson`, `assertJsonPath`, `assertJsonFragment`, `assertJsonStructure`, `assertJsonCount` |\n| Validation | `assertJsonValidationErrors` |\n\n`assertJson()` compares **complete JSON with strict types**. Use `assertJsonFragment()` for a subset and `assertJsonPath()` for one nested value. An integer and its string representation are not interchangeable in strict assertions.\n\n## General assertions and exceptions\n\nThe Assert class supplies strict booleans/nulls, equality/identity, count, key, type, string, numeric-delta, and exception assertions. Prefer `assertSame()` when types matter. `assertEquals()` intentionally uses looser value equality.\n\nUse `expectException()`, `expectExceptionMessage()`, and `expectExceptionCode()` before the operation expected to fail, or `assertThrows()` for a callback. `markTestSkipped($reason)` records an explicit skip instead of silently returning.\n\n## Database assertions\n\n`assertDatabaseHas()`, `assertDatabaseMissing()`, and `assertDatabaseCount()` query the configured test database. Create the schema and fixtures required by a test; an in-memory connection starts empty unless your test setup creates tables.\n\nKeep tests independent of the developer's database and make fixture ownership explicit. Use the current database driver in a separate integration environment for SQL features that differ from SQLite.\n\n## Isolation and middleware\n\nSet `APP_ENV=testing` before application creation. The supplied runner handles this; `Application::create()` skips normal environment/config caching and merges `tests/config.php` before providers register.\n\n`ApplicationTestCase` creates a fresh app and a temporary storage directory. The skeleton uses in-memory SQLite and isolated cache/queue paths. Middleware—including CSRF—remains active. Include a valid token/session when testing a protected browser write, or deliberately test that missing tokens are rejected.\n\nUnexpected exceptions reach the runner. Early responses, redirects, aborts, and validation failures are captured. Deferred callbacks run after successful requests without flushing runner buffers.\n\n## A useful verification sequence\n\nLint changed PHP files, run the focused tests, then run the application's suite. Cover a successful request and its meaningful invalid/unauthorized cases. Use small stub objects for external services and reserve live-provider tests for a controlled integration environment.\n";export{e as default};
