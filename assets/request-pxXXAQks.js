const e="## Access the current request\n\nType-hint `Spark\\Http\\Request` in an action, or call `request()`:\n\n```php\nuse Spark\\Http\\Request;\n\nRoute::get('/search', function (Request $request) {\n    return ['query' => $request->query('q', '')];\n});\n```\n\n`request('email')` reads one input value. `request(['name', 'email'])` returns filtered input. Prefer explicit methods when the source of a value matters.\n\n## Read input\n\n| Method | Use |\n| --- | --- |\n| `query('page', 1)` | Query-string value with a default |\n| `post('email')` | Submitted body value |\n| `input('email')`, `get('email')` | Combined request input |\n| `all()` | Input array |\n| `only(['name', 'email'])` | Allowlisted fields |\n| `except(['password'])` | Input without listed fields |\n| `has('name')`, `hasAll([...])`, `hasAny([...])` | Presence checks |\n| `filled('name')` | A nonempty value |\n| `route('id')` | Route parameter |\n\nSend `Content-Type: application/json` for JSON requests. `Accept: application/json` communicates the desired response format. `expectsJson()` and `isAjax()` are related but distinct checks.\n\n## Input objects\n\n`$request->input()` without a field returns `Spark\\Http\\Input`. The wrapper supports property/array access, iteration, conversion to an array, and typed getters such as `boolean()`.\n\n```php\n$input = $request->input();\n$published = $input->boolean('published');\n$data = $request->validate(['title' => 'required|string|max:255']);\n$title = $data->get('title');\n```\n\nAn input wrapper is not proof of validation. Only the result of `validate()` or `validated()` is the validated payload. See [Validation](./validation.md).\n\n## Headers and metadata\n\n```php\n$token = $request->header('Authorization');\n$method = $request->getMethod();\n$path = $request->getPath();\n$uri = $request->getUri();\n$ip = $request->ip();\n```\n\nOther metadata methods include `getUrl()`, `getRootUrl()`, `server()`, `useragent()`, `referer()`, `isBot()`, `accept()`, and `getHeaders()`. Use `isGet()`, `isPost()`, `isPut()`, `isDelete()`, or `isMethod(['PUT', 'PATCH'])` for method checks. `is($path)` matches a request path; `isFirelineRequest()` and `isPrecognitive()` identify integration-specific request headers.\n\nClient-provided headers are input. Do not treat a referer, user agent, or arbitrary forwarded IP header as proof of identity.\n\n## Uploaded files\n\n```php\nif ($request->hasFile('avatar')) {\n    $file = $request->file('avatar');\n    // Validate type and size before storing the file.\n}\n```\n\n`getFiles()` / `getFileUploads()` expose upload collections. `moveFile('avatar', $destination)` moves an upload and returns a boolean. The [uploader utility](./uploads.md) adds naming, limits, and optional image transformations.\n\n## Sanitizing and output\n\n`safe('body', ['p', 'strong'])` provides a sanitizing helper. It is not a substitute for validating business rules or escaping values in the context where they are rendered. Use escaped Blade output by default and avoid inserting user text into raw SQL or JavaScript.\n\n## Session and authentication shortcuts\n\nThe request exposes `session()`, `auth()`, `user()`, `isAuthenticated()`, and `isNotAuthenticated()`. `old()` accesses flashed input; `errors()` exposes validation errors.\n\nAuthentication shortcuts accept an optional registered guard:\n\n```php\n$auth = $request->auth('admin');\n$admin = $request->user(guard: 'admin');\n$email = $request->user('email', 'Guest', guard: 'admin');\n$signedIn = $request->isAuthenticated('admin');\n$guest = $request->isNotAuthenticated('admin');\n```\n\n`user()` returns the guard's model or `null`; attribute reads return the supplied default when there is no user. Omitting the guard uses the default service. Checking a named guard does not change subsequent default `user()` calls. See [Authentication](./authentication.md) for guard registration and route protection.\n\n## Adapting requests\n\nTests and middleware can use `setQueryParam()`, `setPostParam()`, `setRouteParam()`, `setServerParam()`, and `setFile()`. Their corresponding `merge*` methods accept arrays. These change the request object; they do not persist data to a session or database.\n\nSee the [API reference](./api-reference.md) for exact input and request signatures.\n\n## Input precedence and missing values\n\n`all()` merges query parameters, body parameters, then files; later sources replace duplicate keys. `only()` also includes requested-but-missing keys with null values. Use `query()` for a URL filter and `post()` for a body field when mixing sources would be ambiguous. Route parameters are separate and available through `route()`.\n\n`has()` checks presence and `filled()` checks a usable value; neither establishes a type or permission. JSON bodies are decoded into body input when the request has a JSON content type. Reading the body is separate from asking for a JSON response through `Accept`.\n\n## Typed input and mutation\n\n```php\n$data = $request->validate([\n    'published' => 'required|boolean',\n    'title' => 'required|string|max:255',\n]);\n$published = $data->boolean('published');\n$title = $data->string('title');\n```\n\nInput supports `number()`, `float()`, `boolean()`, `string()`, `array()`, and `date()` in addition to `get()` / `set()`. These conversions are convenience reads, not replacements for validation. Use the input object's `toArray()` when passing data to a strictly array-typed service.\n\nAn uploaded file is a PHP upload metadata array, including `name`, `tmp_name`, `error`, and `size`; it is not a Laravel UploadedFile object. Handle multi-file payloads using the uploader’s supported structure. See [File uploads](./uploads.md).\n";export{e as default};
