const e="## Password hashing\n\nUse a one-way password hash for credentials:\n\n```php\n$hash = passcode('a-long-user-chosen-password');\n$valid = passcode($submittedPassword, $hash);\n```\n\nThe `Spark\\Hash` service and Hash facade expose `hashPassword()`, `validatePassword()`, `password()`, `verify()`, `needsRehash()`, and password algorithm/options settings. Model `hashed` casts provide the same kind of storage transformation for password attributes.\n\nPassword hashes are not decryptable. General-purpose `hash()`, `hmac()`, or `make()` methods serve different purposes and should not replace a password-hashing API.\n\n## Application key\n\n```bash\nphp spark key:generate\n```\n\nThe application key is read from `app.key` / `APP_KEY`. Generate it for a new installation, keep it private, and preserve it across deployments. A replacement key may make previously encrypted values unreadable and invalidate signed credentials. Plan key rotation as a data migration when stored values depend on it.\n\n## Encrypt and decrypt\n\n```php\nuse Spark\\Facades\\Hash;\n\n$ciphertext = Hash::encryptString($privateValue);\n$plaintext = Hash::decryptString($ciphertext);\n```\n\n`encrypt()` / `decrypt()` operate on strings; `encryptArray()` / `decryptArray()` support structured arrays. `encryptString()` / `decryptString()` are explicit string helpers. Catch decryption failures at an application boundary and return a controlled error rather than exposing key or ciphertext details.\n\nUse encryption only for data that must be recoverable. Keep encrypted columns long enough for the encoded result, and consider how a future key change will affect them.\n\n## Hashes, signatures, and random values\n\n`hash()` computes a digest, `hmac()` computes a keyed digest, and `equals()` / `isEqual()` compare values using the utility's comparison behavior. `random()`, `randomBytes()`, and `uuid()` generate identifiers or randomness for supported use cases. Choose a token length and encoding appropriate to its purpose.\n\n`bcrypt()`, `argon2i()`, and `argon2id()` expose password algorithm choices when available in your PHP runtime. `passwordInfo()` and `isHashed()` help inspect existing values; they do not prove that a user's submitted password is correct.\n\n## Related topics\n\nUse [Authentication](./authentication.md) for login and JWT flows, [Casts](./casts.md) for encrypted/hashed attributes, and the [API reference](./api-reference.md) for the exact Hash method defaults and parameter order.\n\n## Encodings and failure handling\n\n`Hash::random(32)` generates 32 random bytes and returns **64 hexadecimal characters**; `randomBytes(32)` returns the raw 32 bytes. Choose a storage column large enough for the encoding you keep. `Hash::uuid()` and `Str::uuid()` are different utility implementations; the latter needs its optional package.\n\nString encryption uses the framework’s authenticated AES-256-CBC envelope, with a random IV and HMAC, encoded for storage. It requires OpenSSL and an encryption key of at least 32 characters; use the generated application key. Decrypt with the same helper and key; it is not an interoperable JWT format. Direct decrypt helpers throw `DecryptionFailedException` for invalid or modified ciphertext, while the model’s encrypted cast catches failure and returns null. Handle the boundary you actually use.\n\nAfter successfully verifying a password, `needsRehash()` can indicate that its saved algorithm/options are outdated. Rehash the submitted plaintext at that point; never hash an existing encoded password as a password-upgrade strategy.\n";export{e as default};
