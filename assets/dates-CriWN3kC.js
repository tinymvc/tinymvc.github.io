const e="## Create dates\n\n`Spark\\Carbon` is TinyCore's date utility. It wraps PHP date/time behavior and provides immutable-style transformations.\n\n```php\n$now = now();\n$deadline = carbon('2026-12-01 09:00:00');\n$utc = \\Spark\\Carbon::now('UTC');\n```\n\nConstructors accept strings, timestamps, DateTimeInterface instances, and Carbon instances. Factories include `parse()`, `instance()`, `today()`, `tomorrow()`, `yesterday()`, `createFromFormat()`, `createFromTimestamp()`, and `fromArray()`.\n\n## Transform without changing the original\n\n```php\n$start = carbon('2026-01-10 12:00:00');\n$end = $start->addDays(7)->endOfDay();\n```\n\nAddition/subtraction, boundary, timezone, and `modify()` methods return a new instance. Assign the returned value; `$start->addDays(7)` alone does not update `$start`.\n\n| Operation | Methods |\n| --- | --- |\n| Add | `addYears`, `addMonths`, `addDays`, `addHours`, `addMinutes`, `addSeconds` |\n| Subtract | Corresponding `sub*` methods |\n| Boundaries | `startOfDay/Week/Month/Year`, `endOfDay/Week/Month/Year` |\n| General change | `modify($modifier)` |\n| Copy | `copy()`, `clone()` |\n\nUse the plural method names in this implementation, such as `addDays(1)`, rather than assuming `addDay()` exists.\n\n## Format and serialize\n\n`format()` accepts PHP date format strings. Convenience methods include `toDateString()`, `toTimeString()`, `toDateTimeString()`, `toISOString()`, `toISOUtcString()`, and `toRfc3339String()`.\n\nReadable variants include `toFormattedDateString()`, `toDayDateTimeString()`, `toDayDateString()`, `toTimeAmPmString()`, and `toFormattedDateTimeString()`. `toArray()` exposes components; `getTimestamp()` and `getPreciseTimestamp()` expose epoch values.\n\n## Compare dates\n\n```php\nif ($deadline->isFuture()) {\n    $remaining = $now->diffInDays($deadline);\n}\n```\n\nUse `isBefore()`, `isAfter()`, `equals()`, or `lt/gt/lte/gte`. Calendar checks include `isToday`, `isTomorrow`, `isYesterday`, `isWeekend`, `isWeekday`, `isSameDay`, `isSameMonth`, and `isSameYear`.\n\n`diffInSeconds/Minutes/Hours/Days` provide numeric differences. `diffForHumans()` and `diffForHumansShort()` produce relative text. Check direction and rounding expectations in a test when a difference drives a business deadline.\n\n## Time zones\n\n```php\n$local = $utc->setTimezone('Asia/Dhaka');\n$stored = $local->utc();\n```\n\n`getTimezone()` inspects the zone; `setDefaultTimezone()` changes the utility default. Application configuration uses `app.timezone`. Store instants consistently, usually in UTC, and convert for display.\n\nCalendar additions around daylight-saving changes are not always equivalent to adding a fixed number of seconds. Validate input dates before relying on them for scheduling.\n\n## Components and ranges\n\nGetters expose year, month, day, hour, minute, second, weekday, and localized-style names provided by the utility. `maxDate()` / `minDate()` select a boundary among instances; `age()` computes age using its documented reference date. The [API reference](./api-reference.md) lists all formats and signatures.\n\n## Difference direction and precision\n\n`diffInSeconds()`, `diffInMinutes()`, `diffInHours()`, and `diffInDays()` return absolute integer differences. Minutes, hours, and days truncate the elapsed-second difference, so a 23-hour interval is zero whole days. Use `isBefore()` / `isAfter()` separately when direction matters. Calendar-day reporting should compare dates in the intended timezone rather than assuming every day is 86,400 seconds.\n\n```php\n$start = carbon('2026-01-10 12:00:00', 'UTC');\n$end = $start->addHours(25);\n$wholeDays = $start->diffInDays($end); // 1\n$isLater = $end->isAfter($start);      // true\n```\n\n`age()` compares the supplied birth date (or this instance) with the current time, not an arbitrary reference date represented by the receiver. Parsing a date is separate from validating an exact user-input format; see [Validation](./validation.md#rule-parser-limits).\n";export{e as default};
