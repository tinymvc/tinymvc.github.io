const e="## Choose a disk\n\nA disk is a named storage destination. Use the same file API with private local files, public uploads, or S3-compatible object storage.\n\n```php\nuse Spark\\Facades\\Disk;\n\nDisk::put('reports/status.txt', 'Ready');             // default disk\nDisk::disk('public')->put('exports/status.txt', 'Ready');\n\n$disk = Disk::disk('s3');                           // named S3 disk\n$disk->put('reports/status.txt', 'Ready');\n$contents = $disk->get('reports/status.txt');\n\n$sameDestination = disk('s3');                      // equivalent helper\n```\n\n`Spark\\Facades\\Disk` provides static access. `Spark\\Storage\\Disk` is the concrete class: construct it, call `Spark\\Storage\\Disk::disk('public')`, or inject it to use the default disk. Selecting a named disk returns a new instance; it does not change other instances or the default.\n\n| Entry point | Behavior |\n| --- | --- |\n| `Spark\\Facades\\Disk::put($key, $contents)` | Resolves the concrete Disk service from the container |\n| `Spark\\Facades\\Disk::disk('s3')` | Creates the selected disk directly, without resolving the default first |\n| `Spark\\Storage\\Disk::disk('s3')` | Same named-disk factory on the concrete class |\n| `disk('s3')` | Helper for the concrete named-disk factory |\n| `disk()`, `Disk::disk()`, `new Spark\\Storage\\Disk()` | Creates a disk from `config('disk.default', 'local')` |\n| `Disk::build($config)` | Creates an on-demand disk from explicit settings |\n\nBoth Disk classes expose the `disk()` and `build()` factories. Other operations, such as `put()` and `get()`, are instance methods on `Spark\\Storage\\Disk`; import the facade to use `Disk::put(...)` directly.\n\nPaths are relative to the selected disk. Store keys such as `avatars/photo.png` in your database, alongside the disk name when necessary. Absolute paths, URLs, stream prefixes, control characters, and `.` / `..` path segments are rejected. Backslashes become forward slashes and repeated separators are collapsed. Local operations reject symbolic links below the configured root; keep storage directories under application control.\n\n## Configuration\n\nThe skeleton includes `config/disk.php`. Set `FILESYSTEM_DISK` to the disk name; the older `FILESYSTEM_DRIVER` variable is accepted as a fallback.\n\n| Disk | Driver | Destination | Access |\n| --- | --- | --- | --- |\n| `local` | `local` | `storage/app` | Private; no public URL |\n| `public` | `local` | `storage/uploads` | Public uploads URL |\n| `s3` | `s3` | Configured bucket | Bucket policy or explicitly configured ACL |\n\nRun `php spark storage:link` to expose the skeleton's `storage/uploads` through its public uploads link. A URL setting alone does not create the link. If you change the public root or URL, update your web-server mapping too. Private files should remain outside the web root and public links.\n\nLocal disks create directories as needed. New private files/directories use `0600` / `0700`; public files/directories use `0644` / `0755`. The process umask affects directory creation. Existing directories are not recursively chmodded. The PHP account needs the appropriate ownership and directory permissions. Local writes publish a temporary file using a rename within the same directory.\n\nAdd more disk names to `disk.disks`, or construct an on-demand disk:\n\n```php\nuse Spark\\Facades\\Disk;\n\n$exports = Disk::build([\n    'driver' => 'local',\n    'root' => storage_dir('exports'),\n    'visibility' => 'private',\n]);\n\n$exports->put('daily.csv', \"name,total\\nAda,42\\n\");\n```\n\nUse absolute filesystem paths for local roots. In configuration files, build defaults from `dirname(__DIR__)`, as the skeleton does: application configuration helpers are not yet available while the configuration files are being evaluated. After changing environment values, refresh your cached configuration as described in [Configuration](./configuration.md).\n\n## Common operations\n\n```php\n$disk = disk('public');\n\n$disk->put('notes/readme.txt', 'Hello');\n$contents = $disk->get('notes/readme.txt');\n\n$exists = $disk->exists('notes/readme.txt');\n$missing = $disk->missing('notes/missing.txt');\n\n$disk->copy('notes/readme.txt', 'notes/copy.txt');\n$disk->move('notes/copy.txt', 'archive/copy.txt');\n\n$bytes = $disk->size('notes/readme.txt');\n$mime = $disk->mimeType('notes/readme.txt');\n$modified = $disk->lastModified('notes/readme.txt'); // Unix timestamp\n\n$files = $disk->files('notes');                    // immediate files\n$all = $disk->allFiles('notes');                   // includes nested files\n\n$disk->delete('notes/readme.txt');\n$disk->delete(['archive/copy.txt', 'already-missing.txt']);\n```\n\n`put()`, `copy()`, `move()`, and `delete()` return `true` on success. Configuration, invalid path, storage, authentication, and transport failures throw exceptions. `exists()` returns `false` for a missing file; an S3 permission error is not treated as absence. `get()` and metadata calls throw for missing files. Deleting an absent file succeeds, and deletion accepts exact file keys rather than directory prefixes. Listings return sorted relative file keys, omit directory markers, and consume all S3 pages automatically. S3 keys that cannot be represented by the disk's relative-path rules are omitted.\n\nArray deletion and multi-file uploads are not transactions. A move copies first and deletes the source only after success; if deletion fails, both files can remain. Moving a file to the same path leaves it intact. Catch exceptions at an application boundary and handle partial operations where needed.\n\n`get()` reads the whole object into memory. Local copies stream; S3 `copy()` uses a server-side CopyObject request and preserves source metadata. Single S3 uploads and copies are limited to 5 GiB; a copy failure leaves the source available, including errors embedded in an HTTP 200 response. This compact API does not implement recursive directory deletion or multipart S3 transfers.\n\n## Store an existing local file\n\nUse `putFile()` to generate a random filename, or `putFileAs()` to choose one. Both copy a readable local file and return the stored key; the original file remains available.\n\n```php\n$disk = disk('s3');\n\n$path = $disk->putFile('reports', storage_dir('exports/report.pdf'));\n$namedPath = $disk->putFileAs(\n    'reports',\n    storage_dir('exports/report.pdf'),\n    'monthly.pdf',\n);\n```\n\nThe directory may be `''` to store at the disk root. The name passed to `putFileAs()` must be a filename, without a directory. These methods import trusted local files; they do not validate upload extensions, MIME types, or application size limits. For browser uploads, use the uploader below.\n\n## Validate and process HTTP uploads\n\nThe disk delegates upload validation and image processing to the existing [Uploader](./uploads.md). The returned paths are relative to the selected disk, including the requested destination directory.\n\n```php\n$request->validate(['avatar' => 'required|image|mimes:jpg,jpeg,png|max:2048']);\n\n$disk = disk('s3');\n$path = $disk->uploader(\n    uploadTo: 'avatars',\n    extensions: ['jpg', 'jpeg', 'png'],\n    maxSize: 2048,\n    resize: [320, 320],\n)->upload($request->file('avatar'));\n\n$url = $disk->url($path); // requires public access to the object to be usable\n```\n\n`uploader()` also accepts `multiple`, `resizes`, and `compress`. `upload('avatar')` resolves the request field. Multiple uploads or image variants return an array of keys. Size limits are in KB and are checked against the actual uploaded file. The source must be a PHP HTTP upload; use `putFile()` for generated files or CLI imports.\n\nDisk-backed uploaders use private staging beneath `storage/temp/disk-uploads`. Ordinary driver uploads transfer directly from PHP's temporary upload file. Image processing uses staging files and removes them after upload or failure. The PHP account needs writable temporary storage even for an S3 disk. Multi-file failures may leave earlier completed objects; keep returned keys and clean up at an application boundary when atomic behavior is required.\n\n### Uploader driver integration\n\n`Spark\\Storage\\S3UploaderDriver` implements the existing `UploaderUtilDriverInterface` with `upload($filepath, $destination)` and `delete($destination)`, and adds `getPublicUrl($path)`. It delegates to `Spark\\Storage\\S3Storage`; you do not need to write a separate `CloudStorage` service for each provider.\n\n`$disk->getDriver()` exposes that driver for integrations that already manage relative destinations. Prefer `$disk->uploader()` for HTTP uploads: it handles staging and path conversion. Existing `uploader(driver: $customDriver)` integrations continue to use their configured upload root. The optional `relativeTo` argument on `Uploader` / `Uploader::make()` changes the base stripped from driver destinations and returned paths; it does not change the storage root itself. Pass stored relative keys to deletion methods rather than trying to strip a public URL.\n\n## URLs and private downloads\n\n```php\n$publicUrl = disk('public')->url('avatars/photo with spaces.png');\n$localPath = disk('local')->path('reports/private.pdf');\n\n$downloadUrl = disk('s3')->temporaryUrl('reports/private.pdf', 300);\n// Or supply a DateTimeInterface for an absolute expiration:\n$downloadUrl = disk('s3')->temporaryUrl('reports/private.pdf', new DateTimeImmutable('+10 minutes'));\n```\n\n`url()` encodes object keys, including spaces and literal `+` or `%` characters. On S3 it uses `url` when configured (for example, a CDN URL), otherwise the bucket origin. Constructing a URL does not make a private object public. A local disk without a `url` throws when asked for one; serve its files through an authorized controller instead.\n\n`path()` is local-only. `temporaryUrl()` is S3-only and signs the origin rather than a CDN URL. The expiry is 1–604800 seconds, but temporary credentials or provider policies may expire sooner. A signed URL grants access to anyone holding it until it expires, so create one only after authorizing the request. No ACL changes are made by these methods.\n\nSigned downloads now include `response-content-disposition=attachment; filename=\"<basename>\"`. For `reports/private.pdf`, the suggested download filename is `private.pdf`. This response override is included in the signature; do not edit or remove it afterward. The current API does not accept a custom download filename or an inline-display option. Generating the URL performs no network request or existence check; access is evaluated when the client downloads it.\n\n## Amazon S3\n\n```dotenv\nFILESYSTEM_DISK=s3\nAWS_ACCESS_KEY_ID=your-access-key\nAWS_SECRET_ACCESS_KEY=your-secret-key\nAWS_DEFAULT_REGION=eu-west-1\nAWS_BUCKET=your-bucket\nAWS_USE_PATH_STYLE_ENDPOINT=false\n```\n\nLeave `AWS_ENDPOINT` unset for the regional Amazon S3 origin. `AWS_SESSION_TOKEN` supports temporary credentials, and `AWS_URL` optionally configures the public/CDN base URL. The client uses the supplied credentials; it does not discover or refresh instance-role credentials.\n\nACLs are omitted by default, which works with policy-controlled buckets. If your provider and bucket support ACLs and you deliberately want publicly readable uploads, set `'acl' => 'public-read'` in that disk's configuration. `'private'` is also available for ACL-enabled buckets. Do not set an ACL on an AWS bucket that disables ACLs through Bucket owner enforced ownership. See [AWS's ACL guidance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/managing-acls.html).\n\nThe bucket must already exist. Give the configured credentials permissions for the operations you use: object read/write/delete and bucket listing. Versioned buckets may retain older versions after deletion.\n\n## DigitalOcean Spaces and other S3 services\n\nThe same `s3` driver accepts a custom origin:\n\n```dotenv\nFILESYSTEM_DISK=s3\nAWS_ACCESS_KEY_ID=your-spaces-key\nAWS_SECRET_ACCESS_KEY=your-spaces-secret\nAWS_DEFAULT_REGION=sgp1\nAWS_BUCKET=your-space\nAWS_ENDPOINT=https://sgp1.digitaloceanspaces.com\nAWS_USE_PATH_STYLE_ENDPOINT=false\nAWS_URL=https://your-space.sgp1.cdn.digitaloceanspaces.com\n```\n\nFor another compatible service, use its signing region, endpoint, credentials, and bucket. `AWS_ENDPOINT` must be an origin without a bucket path, query string, or credentials. Path-style endpoints place the bucket in the URL path; set `AWS_USE_PATH_STYLE_ENDPOINT=true` when your provider requires this, for IP-based endpoints, or for dotted bucket names over HTTPS. HTTP endpoints are accepted for local services such as MinIO; use HTTPS for remote storage.\n\nThe client implements Signature V4, streaming single-object PUTs up to 5 GiB, GET, HEAD, DELETE, CopyObject, ListObjectsV2 pagination (or legacy V1), and presigned GET URLs. It supports custom ports and session tokens. TLS verification stays enabled, signed requests do not follow redirects, and bucket administration/multipart upload are outside this utility. S3 compatibility varies by provider; consult its operation support, including [DigitalOcean's compatibility list](https://docs.digitalocean.com/products/spaces/reference/s3-compatibility/).\n\nS3 network operations require PHP cURL, and listing/copy responses require SimpleXML. Fileinfo identifies upload MIME types; GD is required for image transformations. Verify the configured provider and bucket policy with the deployment checks below.\n\n## Storage namespace and dependency injection\n\nThe implementations live together under `Spark\\Storage`:\n\n| Class | Responsibility |\n| --- | --- |\n| `Spark\\Storage\\Disk` | Select and operate on a configured disk |\n| `Spark\\Storage\\Uploader` | Validate and transform HTTP uploads |\n| `Spark\\Storage\\S3Storage` | Low-level S3-compatible requests and signing |\n| `Spark\\Storage\\LocalStorage` | Local filesystem operations |\n| `Spark\\Storage\\S3UploaderDriver` | Bridge the uploader driver contract to S3 |\n| `Spark\\Storage\\Contracts\\DiskContract` | Application-facing operations on a selected disk |\n\n`Spark\\Facades\\Disk`, `disk()`, and `uploader()` remain the convenient entry points. `Spark\\Disk`, `Spark\\Utils\\Uploader`, and `Spark\\Utils\\S3Storage` are not present in the current package; there are no compatibility aliases. Replace direct imports with their `Spark\\Storage` counterparts, or use `Spark\\Facades\\Disk` for static operations. The existing `UploaderUtilContract`, `UploaderUtilDriverInterface`, and `UploaderUtilException` names are retained.\n\nThe container can construct the concrete `Spark\\Storage\\Disk` using its default constructor. Inject it into a controller or application service:\n\n```php\nuse Spark\\Storage\\Disk;\n\nclass ReportStore\n{\n    public function __construct(private Disk $disk) {}\n\n    public function save(string $contents): void\n    {\n        $this->disk->put('reports/latest.csv', $contents);\n    }\n}\n```\n\nYou can replace the binding in a service provider, including for tests:\n\n```php\n$this->app->bind(\n    \\Spark\\Storage\\Disk::class,\n    fn () => \\Spark\\Storage\\Disk::disk('s3'),\n);\n```\n\nDefault `Spark\\Facades\\Disk` operations use this concrete-class binding. Explicit `Disk::disk('public')`, `Disk::build(...)`, and the `disk()` helper construct disks directly; they do not use the binding, even when the name is omitted.\n\nFor interface-based injection or test doubles, bind `Spark\\Storage\\Contracts\\DiskContract` explicitly in a provider's `register()` method. The application does not register that interface automatically:\n\n```php\n$this->app->bind(\n    \\Spark\\Storage\\Contracts\\DiskContract::class,\n    fn () => $this->app->make(\\Spark\\Storage\\Disk::class),\n);\n```\n\nServices may then type-hint `DiskContract`. This example follows the concrete Disk binding, including a selected S3 disk if configured above. Replacing only the interface binding does not affect facade calls, which resolve the concrete class. The contract describes instance operations; factories and disk configuration belong to the concrete class.\n\n## Provider settings at a glance\n\nThe endpoint is the API origin, not the website or CDN URL. The bucket is supplied separately. Store credentials in environment configuration; never accept endpoint or bucket configuration from an untrusted request.\n\n| Provider | Endpoint | Signing region | Path style | ACL |\n| --- | --- | --- | --- | --- |\n| Amazon S3 | Leave unset for a regional origin | Actual bucket region | Usually `false`; `true` for dotted bucket names over HTTPS | Leave `null` for policy-controlled buckets |\n| DigitalOcean Spaces | `https://sgp1.digitaloceanspaces.com` (choose your region) | `sgp1` in this example | Usually `false` | `null`, or explicit `public-read` where intended |\n| Cloudflare R2 | `https://<account-id>.r2.cloudflarestorage.com` | `auto` | `true` | `null`; ACLs are not supported |\n| MinIO / another compatible service | Service's S3 API origin, including its port | Region configured by the service | Commonly `true` | Follow the service's access policy |\n\nCloudflare documents its `auto` region and supported operations in its [S3 compatibility reference](https://developers.cloudflare.com/r2/api/s3/api/). DigitalOcean's [compatibility reference](https://docs.digitalocean.com/products/spaces/reference/s3-compatibility/) covers supported calls and restrictions. Provider examples describe configuration, not a guarantee that every bucket policy or optional S3 feature is supported.\n\nFor example, configure a local MinIO development service with `AWS_ENDPOINT=http://127.0.0.1:9000`, `AWS_USE_PATH_STYLE_ENDPOINT=true`, and the service's configured region and credentials. Use a trusted HTTPS endpoint for remote deployments. The client keeps TLS certificate verification enabled.\n\nListObjectsV2 is the default. Set `'list_version' => 1` in a disk configuration only when its service requires legacy ListObjects. The low-level `S3Storage::listFiles($limit, $prefix, $marker)` returns one page with `files`, `is_truncated`, `next_marker`, and `count`. For V2, `next_marker` is an opaque continuation token: pass it back exactly as returned, without decoding or deriving it from a filename. Disk listings consume all pages and reject missing or repeated continuation cursors. A disk directory such as `reports` lists the `reports/` prefix; low-level `listFiles(prefix: 'reports')` is a raw S3 prefix filter instead.\n\n## Permissions, failures, and deployment verification\n\nFor AWS, scope permissions to the intended bucket and objects. Common operations use:\n\n| Operation | Typical AWS permission |\n| --- | --- |\n| Listing | `s3:ListBucket` on the bucket |\n| Reading, HEAD metadata, signed downloads | `s3:GetObject` on objects |\n| Uploading | `s3:PutObject` on objects |\n| Deleting | `s3:DeleteObject` on objects |\n| Copying/moving | Read the source, write the destination; moving also needs source deletion |\n\nExplicit ACLs or encryption policies can require additional permissions. A signed URL does not bypass the signing identity's permissions. Some providers return 403 for a missing object when the credentials cannot list the bucket, so `exists()` correctly throws for that response rather than claiming the file does not exist.\n\nUploads and copies do not modify bucket policies. CopyObject preserves metadata, but the new object's ACL follows the configured disk ACL or the provider's default; it does not inherit the source ACL. A move is copy followed by deletion, not an atomic rename. Concurrent modifications, bucket versioning, retention, or deletion failures can leave multiple objects or historical versions. HTTP requests are not retried automatically, since a timeout can occur after a write has reached the server.\n\nThe low-level client returns stored bytes as-is, including objects marked with `Content-Encoding: gzip`. Downloads are not automatically decompressed. Signed requests never follow redirects; choose the correct regional endpoint rather than relying on a redirect to fix it.\n\n| Symptom | What to check |\n| --- | --- |\n| Disk is not configured | Disk name and `config/disk.php`; clear/regenerate cached config |\n| Local write fails | Absolute root, PHP process ownership, directory permissions, and disk space |\n| `SignatureDoesNotMatch` | Key/secret/token, signing region, system clock, endpoint, proxies rewriting paths/headers |\n| 301/307 response | Correct bucket region, origin endpoint, and addressing style |\n| 403 / `AccessDenied` | Credentials, token expiry, bucket policy, object permissions, and explicit ACL requirements |\n| ACL-related rejection | Leave `acl` unset for ACL-disabled buckets and providers |\n| Public URL returns 403 | The URL is only a location; configure intentional public access or use an authorized temporary URL |\n| Incomplete listing error | Provider's listing version and continuation-token behavior |\n| Image upload fails | Genuine PHP upload, application/PHP size limits, GD support, and writable private staging |\n\nImage rollback attempts to remove every completed variant even when one remote deletion fails, and it preserves the original upload error. Cleanup is best-effort: unreachable storage or denied deletion may leave objects requiring reconciliation. Malformed multiple-upload structures are rejected before storing files. Validate MIME types and ownership in the application; extension checks alone are not content validation.\n\nBefore enabling a production disk, use a disposable, authorized prefix in your own bucket to upload, read, inspect, list, copy, generate and open a temporary URL, and delete a small file. Confirm the attachment filename and public/CDN access separately if used. Include encoded keys, empty files, expired credentials, listing pagination, and genuine multipart/image uploads in your application's integration tests. Bucket policy, credentials, network access, and provider-specific limits need verification against the actual deployment.\n";export{e as default};
