const e="## Translation files\n\nThe default locale is `config('app.lang')`, sourced from `APP_LOCALE`. Translation files live in `app.lang_dir`, normally `resources/languages`.\n\n```php\n<?php\n// resources/languages/en.php\nreturn [\n    'hello' => 'Hello!',\n    'welcome' => 'Welcome, %s!',\n    'greeting' => 'Hello, :name.',\n    'message_count' => ['%d message', '%d messages'],\n];\n```\n\n## Translate and escape\n\n```php\n$text = __('welcome', 'Ada');\n$greeting = __('greeting', ['name' => 'Ada']);\n$safeHtmlText = _e('welcome', '<Ada>');\n```\n\n`__()` returns translated text. `_e()` also escapes it for HTML; despite its name, it returns a string. In Blade, use `{{ __('welcome', $name) }}` for escaped output.\n\nMissing translations fall back to the source key/text. Keep keys consistent across language files so missing entries are easy to identify.\n\n## Placeholders and plural forms\n\nNumeric arguments select the singular/plural array entry and can fill a `%d` placeholder:\n\n```php\n__('message_count', 1);\n__('message_count', 5);\n```\n\nFor several placeholders, pass the count followed by the formatting arguments:\n\n```php\n// Translation: ['%s has %d message', '%s has %d messages']\n__('user_messages', 2, ['Ada', 2]);\n```\n\n`translate()` also accepts separate plural arguments and named replacements. `choice($key, $number, $replace)` and `transWithReplace()` expose dedicated variants. The translator is a compact implementation; test languages whose plural rules need more than a singular/other distinction.\n\n## Add and manage translations\n\n```php\n$translator = app(\\Spark\\Translator::class);\n$translator->addLanguageFile(lang_dir('admin/en.php'));\n$translator->mergeTranslatedTexts(['saved' => 'Changes saved.']);\n```\n\n`addLanguageFile()` queues a file for deferred loading; `prepend: true` adjusts its load order. `setTranslatedTexts()` replaces the loaded translation map. `has()`, `get()`, `all()`, `count()`, `getLoadedFiles()`, and `hasLoadedFiles()` provide inspection. `flush()` clears translator state.\n\n## Switching locale\n\nSet the intended locale before resolving the translator or deliberately replace/reconfigure that service for the request. Changing a config value after a singleton translator has loaded its files does not automatically unload old translations.\n\nValidate a requested locale against supported language codes. Keep placeholders and meaning consistent across translations, and avoid storing presentation logic in language files.\n\n## Load a request-specific language\n\n```php\n$locale = $request->query('lang', 'en');\nabort_unless(in_array($locale, ['en', 'bn'], true), 400, 'Unsupported language.');\n\napp()->instance(\\Spark\\Translator::class,\n    new \\Spark\\Translator(lang_dir($locale . '.php')));\n```\n\nApply locale selection early, before controllers or views translate text. This replaces the translator for this application instance; persist a selected locale in the session only when the product requires it. Avoid deriving arbitrary filesystem paths from unvalidated locale strings.\n\nPlural arrays use a singular/other selection for numeric counts. Named `one` / `other` maps do not provide a full locale-aware plural-rules engine; use the indexed pair shown above for the supported count behavior.\n";export{e as default};
