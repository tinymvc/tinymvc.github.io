const e="## Environment and config files\n\nEnvironment values belong in `.env`; application configuration belongs in PHP files under `config/`. Each file returns an array, and its filename becomes the first part of its configuration key.\n\n```php\n// config/services.php\nreturn [\n    'catalog_url' => env('CATALOG_URL', 'https://catalog.example.com'),\n];\n```\n\n```php\n$url = config('services.catalog_url');\n$debug = config('app.debug', false);\nconfig(['services.catalog_url' => 'https://sandbox.example.com']);\n```\n\nA runtime override changes this process's configuration, not the PHP file. Read environment values in configuration files and use `config()` throughout your application.\n\n## Application settings\n\n| Key | Purpose |\n| --- | --- |\n| `app.key` / `APP_KEY` | Application encryption/signing key |\n| `app.debug` / `APP_DEBUG` | Development error reporting |\n| `app.name` / `APP_NAME` | Application name |\n| `app.timezone` / `APP_TIMEZONE` | Default time zone, normally UTC |\n| `app.lang` / `APP_LOCALE` | Default translation locale |\n| `app.url` / `APP_URL` | Application base URL |\n| `app.storage_dir`, `temp_dir`, `upload_dir` | Writable filesystem locations |\n| `app.views_dir`, `lang_dir` | Templates and translation files |\n| `app.asset_url`, `media_url` | Public asset and uploaded-file URL prefixes |\n\nUse the exact names above. The old documentation's `env.php` structure does not describe the current skeleton.\n\n## Service configuration\n\nDatabase, cache, queue, and mail have separate configuration arrays:\n\n- [Database](./database.md): `database.driver` and `database.connections`.\n- [Cache](./cache.md) and [locks](./locks.md): `cache.driver` and `cache.connections`.\n- [Queues](./queues.md): `queue.driver` and `queue.connections`.\n- [Mail](./mail.md): SMTP and sender settings.\n\nCache and queue can use different drivers. SQLite cache configuration accepts a directory, while the SQLite queue configuration points to a database file.\n\n## Configuration cache\n\nEnvironment and configuration data are cached during normal bootstrap. After changing `.env` or config files, clear the configuration cache:\n\n```bash\nphp spark config:clear\n```\n\nThen restart long-running processes such as queue workers. `view:clear` clears compiled templates; it does not replace `config:clear`.\n\nWith `APP_ENV=testing`, environment/config caching is skipped and `tests/config.php` is merged before service providers register. Set this environment before creating the application. See [Testing](./testing.md).\n\n## Paths and portability\n\nPrefer `root_dir()`, `storage_dir()`, `temp_dir()`, `upload_dir()`, and `views_dir()` over machine-specific paths. Config files may use `dirname(__DIR__)` to anchor paths to the project root.\n\nKeep `.env` out of version control. Commit a `.env.example` with the keys needed to run the application and harmless defaults. Set `APP_DEBUG=false` and a correct HTTPS `APP_URL` in production.\n\n## Environment types and nested configuration\n\nUnquoted environment booleans and numeric values are parsed into PHP values. Quote values that should remain text, and use `config()` defaults deliberately when a setting may be absent. Avoid treating a string such as `'false'` as a boolean with a plain PHP cast.\n\nNested config directories are supported: `config/services/catalog.php` maps to `services.catalog`. Each config file must return an array. The loader checks source files and the environment file when deciding whether a configuration cache is fresh; `config:clear` is still useful after deployment or when diagnosing cached state. Ensure `bootstrap/cache` is writable as well as `storage/`.\n\nAvoid closures or resource handles in cached configuration. Register runtime factories in a service provider and keep config values serializable. Changing a config value does not rebuild a service that has already been resolved as a singleton.\n";export{e as default};
