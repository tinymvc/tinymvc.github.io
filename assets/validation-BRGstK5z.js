const e="## Validate a request\n\n```php\n$data = $request->validate([\n    'name' => 'required|string|max:100',\n    'email' => 'required|email',\n    'password' => 'required|string|min:12|confirmed',\n]);\n```\n\nSuccessful validation returns `Spark\\Http\\Input`. Use `$data->toArray()` when an API requires an array. On failure, the request prepares a validation response rather than continuing the action with invalid data. JSON clients should send `Accept: application/json`; browser forms receive redirect/error handling.\n\nValidate an allowlist of expected fields before calling a model's `create()` or `fill()`. Model mass-assignment settings provide another boundary; neither replaces authorization.\n\n## Rule reference\n\nSeparate rules with `|` and rule parameters with commas after `:`.\n\n| Category | Rules and examples |\n| --- | --- |\n| Presence | `required`, `required_if:kind,business`, `required_unless:kind,personal`, `present`, `filled`, `nullable`, `prohibited` |\n| Text | `string` (`text`, `char`), `alpha`, `alpha_num` (`alphanumeric`), `alpha_dash`, `lowercase`, `uppercase` |\n| Numbers | `numeric` (`number`), `int` (`integer`), `float`, `decimal`, `min_value:1`, `max_value:100` |\n| Size | `min:3` (`minimum`), `max:255` (`maximum`), `size:6` (`length`), `between:3,20` |\n| Digits | `digits:6`, `digits_between:3,8`, `min_digits:3`, `max_digits:8` |\n| Comparisons | `same:password` (`equal`, `same_as`), `confirmed`, `in:draft,published`, `not_in:blocked` |\n| Patterns | `regex:/^[a-z]+$/`, `starts_with:inv-`, `ends_with:.pdf`, `contains:example`, `not_contains:script` |\n| Contact / identifiers | `email` (`mail`), `url` (`link`), `ip`, `ipv4`, `ipv6`, `mac_address`, `uuid` |\n| Dates | `date`, `date_format` (see parser limits below), `before:2027-01-01`, `after:2025-01-01` |\n| Structured data | `array` (`list`), `json`, `distinct` |\n| Boolean / consent | `boolean` (`bool`), `accepted`, `declined` |\n| Database | `unique:users,email`, `exists:users,id`, `not_exists:users,email` |\n| Files | `file`, `image`, `mimes:jpg,png,pdf` |\n| Password policy | `password:12,uppercase,lowercase,numbers,symbols` |\n\nSize rules depend on the input type: strings, arrays, numeric fields, and uploaded files are measured differently. Uploaded file sizes are in **KB**. `confirmed` expects a matching `<field>_confirmation` input. Keep complex regular expressions in a form that the rule parser can accept; a `|` inside a pattern conflicts with a pipe-delimited rule string.\n\nFor edits, `unique:users,email,42` excludes numeric `id = 42`. Build the exclusion from the already-authorized model’s ID, never a submitted “ignore ID.” The third parameter always refers to `id`; there is no custom exclusion-column parameter. `exists` / `not_exists` accept table and column. These checks use table queries, include archived rows, and do not add tenant conditions. Enforce database constraints for concurrent writes and perform ownership checks separately.\n\n## Form requests\n\nGenerate `php spark make:request StorePostRequest`, then define rules in `app/Http/Requests/StorePostRequest.php`:\n\n```php\n<?php\n\nnamespace App\\Http\\Requests;\n\nuse Spark\\Foundation\\Http\\FormRequest;\n\nclass StorePostRequest extends FormRequest\n{\n    public function authorize(): bool\n    {\n        return auth()->check();\n    }\n\n    public function rules(): array\n    {\n        return [\n            'title' => 'required|string|max:255',\n            'body' => 'required|string',\n        ];\n    }\n\n    public function messages(): array\n    {\n        return ['title.required' => 'Please give your post a title.'];\n    }\n}\n```\n\nType-hint this class in a controller action and read `$request->validated()`. Form requests authorize and validate during construction, before the action runs. Use `authorize()` for a request-level permission check; use gates when permissions depend on a particular record.\n\n## Displaying errors\n\n```html\n<form action=\"/posts\" method=\"POST\">\n    @csrf\n    <label for=\"title\">Title</label>\n    <input id=\"title\" name=\"title\" value=\"{{ old('title') }}\">\n    @error('title')\n        <p role=\"alert\">{{ $message }}</p>\n    @enderror\n    <button type=\"submit\">Save post</button>\n</form>\n```\n\n`errors()` returns the input error wrapper; `old()` reads flashed input. You can explicitly redirect with `back()->withErrors([...])->withInput()`. Never flash passwords, tokens, or other sensitive fields indiscriminately.\n\n## Standalone validation\n\n```php\nuse Spark\\Http\\Validator;\n\n$validator = Validator::make();\n$data = $validator->validate(\n    ['email' => 'required|email'],\n    ['email' => 'ada@example.com'],\n);\n\nif ($data === false) {\n    $errors = $validator->getErrors();\n}\n```\n\nThe standalone validator returns `false` on failure and an `Input` object on success. `passes()`, `fails()`, `hasErrors()`, `errors($field)`, `getFirstError()`, and `validated()` expose results. Unlike request validation, this use does not itself decide your HTTP response.\n\n## Custom messages and attributes\n\n`Request::validate()` accepts optional `attributes:` and `messages:` arrays. **`attributes` is the input data to validate**, not a map of friendly field labels. Likewise, a form request’s `attributes()` returns the input payload (default: `all()`). Form requests can override `messages()` and `passedValidation()`; the latter runs only after successful, non-precognitive validation. At the validator level, `setErrorMessages()`, `mergeErrorMessages()`, and `setErrorMessage()` customize rule messages. Messages support the framework's translation helper; see [Localization](./localization.md).\n\n## Presence, types, and nested data\n\nUse `required` whenever omission or an empty value must fail. Most other rules skip empty values. `filled` permits an absent field but rejects it when present and empty; `present` requires a key even if its value is empty; `prohibited` requires the key to be absent. Combine consent checks with `required|accepted`.\n\nValidation checks values without casting them. `integer`, `numeric`, `float`, and `decimal` currently use `is_numeric()`: `integer` alone does not reject fractional numbers. Use an explicit integer check when that distinction matters. Read validated booleans through `$data->boolean('published')`, since the string `'false'` should not be converted with a plain PHP boolean cast.\n\nRules address top-level keys. `items.*.name` does not expand nested input, and closures/custom rule objects are not executed as rules. Validate the outer array, validate each row explicitly, then assemble only the validated fields:\n\n```php\n$payload = $request->validate(['items' => 'required|array|max:50']);\n$items = [];\n\nforeach ($payload->get('items') as $row) {\n    abort_unless(is_array($row), 422, 'Each item must be an object.');\n    $item = $request->validate(\n        ['name' => 'required|string|max:100'],\n        attributes: $row,\n    );\n    $items[] = $item->toArray();\n}\n```\n\nThis example returns ordinary field errors. Use a standalone validator and build indexed errors in your application when the client needs errors such as `items.2.name`.\n\n## Rule parser limits\n\nOnly use rules listed above: unknown rule names currently pass rather than reporting a configuration error. `sometimes`, `bail`, `dimensions`, and Laravel-style Rule objects are not implemented. Rule arrays separate strings but do not add those features.\n\nThe parser lowercases the entire rule string, including parameters, and splits parameters on commas. This affects case-sensitive `in` values, prefixes, regex patterns, and PHP date format letters. In particular, `date_format:Y-m-d` becomes `y-m-d`. For an exact four-digit date, validate a required string and check it explicitly:\n\n```php\n$data = $request->validate(['starts_on' => 'required|string']);\n$value = $data->get('starts_on');\n$date = DateTimeImmutable::createFromFormat('!Y-m-d', $value);\nabort_unless($date !== false && $date->format('Y-m-d') === $value,\n    422, 'Use a valid date in YYYY-MM-DD format.');\n```\n\n## Error responses and live validation\n\nJSON validation failures return **422** with `message` and an `errors` object whose field values are message arrays. Browser failures redirect back with errors and the supplied input flashed to the session. The default browser path does not automatically remove passwords from old input; for sensitive forms, use standalone validation and an explicit `withInput($safeFields)` error redirect.\n\nFireLine failures return its `{status: 'error', message: ...}` response shape. Requests identified by the `Precognition` header can limit fields through `Precognition-Validate-Only`; a successful validation sends **204** and stops before the action continues. Keep authorization and normal final-submit validation in place.\n";export{e as default};
