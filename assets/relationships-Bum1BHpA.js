const e="## Define related models\n\nA relationship method returns a `Spark\\Database\\Relation` object. Accessing the relationship as a property loads its result; calling it as a method returns the relation for query chaining.\n\n```php\n<?php\n\nnamespace App\\Models;\n\nuse Spark\\Database\\Model;\nuse Spark\\Database\\Relation\\HasMany;\n\nclass User extends Model\n{\n    protected string $table = 'users';\n\n    public function posts(): HasMany\n    {\n        return $this->hasMany(Post::class, foreignKey: 'user_id');\n    }\n}\n```\n\nIn `Post`, define the inverse:\n\n```php\npublic function user(): \\Spark\\Database\\Relation\\BelongsTo\n{\n    return $this->belongsTo(User::class, foreignKey: 'user_id');\n}\n```\n\n## Relationship types\n\n| Helper inside a model | Meaning | Important keys |\n| --- | --- | --- |\n| `hasOne(Related::class)` | One related record | Related foreign key, local key |\n| `hasMany(Related::class)` | Multiple related records | Related foreign key, local key |\n| `belongsTo(Related::class)` | Inverse relation | Current model foreign key, owner key |\n| `belongsToMany(Related::class)` | Many-to-many through a pivot | Table, both pivot keys, parent/related keys |\n| `hasManyThrough(Related::class, Through::class)` | Related rows through an intermediate model | First/second foreign keys, local/second local keys |\n\nKeys are inferred from model conventions. Specify them explicitly for existing schemas. These helper methods are protected extension points used inside your model; the relationship methods you define should be public.\n\nThe constructors also accept `lazy:` and an optional `callback:` for constraints. Set `lazy: false` when a relationship must be explicitly eager-loaded; an unexpected lazy load then raises an exception.\n\n## Lazy and eager loading\n\n```php\n$user = User::findOrFail($id);\n$posts = $user->posts; // Result, loaded on access\n$recent = $user->posts()->latest()->take(5)->all();\n```\n\nFor a list, load relations together to avoid querying once per record:\n\n```php\n$users = User::with('posts')->take(20)->all();\n$posts = Post::with(['user'])->latest()->take(20)->all();\n```\n\nThe current API loads relations on an existing instance using `load()`:\n\n```php\n$user->load('posts');\n$loaded = $user->relationLoaded('posts');\n$user->unsetRelation('posts');\n```\n\n`load()` uses the lazy access path and returns an already cached relation without refreshing it. It still rejects a relation configured with `lazy: false`; use `with()` or `Model::loadRelations($relations, $models)` for explicit eager loading. The latter loads onto an array of models by reference. `reloadRelations()` refreshes loaded relations; `clearRelations()` removes cached relation data. `setRelation()` lets an integration supply known results.\n\n## Constrain eager loading\n\n```php\n$users = User::with([\n    'posts' => fn($query) => $query->where('published', true),\n])->all();\n```\n\nNested paths also accept callbacks, for example `with(['posts.comments' => fn($q) => $q->where('approved', true)])`. The callback constrains the deepest relation. You can use `with('posts.comments')` directly without separately listing `posts`.\n\n`withFiltered()` is also available for filtered relations. Keep primary and foreign key columns in narrowed selections so rows can be associated correctly. Inspect the relation configuration when building custom constraints.\n\n## Query by related data\n\n```php\n$users = User::whereHas('posts', function ($query) {\n    $query->where('published', true);\n})->all();\n```\n\nA callback defined on the relationship itself applies to lazy loading, eager loading, existence filters, and aggregates. Additional query callbacks extend those conditions. Repeated subqueries use independent bindings, and self-relations receive an alias for correct parent correlation.\n\n`has()` and `doesntHave()` filter existence; `whereHas()` and `whereDoesntHave()` accept a callback. OR variants support grouped alternatives. `has($relation, $operator, $count)` filters a relation count.\n\nConvenience methods include `whereRelation`, `whereRelationIn`, `whereRelationNotIn`, `whereRelationNull`, `whereRelationNotNull`, `whereRelationLike`, `whereRelationBetween`, `whereRelationFindInSet`, and `whereRelationJson`, with corresponding OR forms.\n\n## Many-to-many and pivot data\n\n```php\npublic function roles(): \\Spark\\Database\\Relation\\BelongsToMany\n{\n    return $this->belongsToMany(\n        Role::class,\n        table: 'role_user',\n        foreignPivotKey: 'user_id',\n        relatedPivotKey: 'role_id',\n    );\n}\n```\n\nOperate on the relationship of a persisted parent:\n\n```php\n$user->roles()->attach([2, 3]);\n$user->roles()->detach(2);\n$user->roles()->sync([3, 4]);\n```\n\n`sync()` replaces the related ID set; ensure removal is intended. Pivot helpers support additional fields and conditions, attaching attributes, updating existing pivots, and toggling associations. Check `BelongsToMany` and `InteractsWithPivotTable` in the [API reference](./api-reference.md) for exact argument shapes and return types. Reload a previously cached relationship after changing its pivot rows.\n\n## Through relationships\n\n`hasManyThrough()` receives the final model, intermediate model, and optional explicit keys. Use it when a parent reaches the final records through an intermediate table, such as a country reaching posts through users. Choose the two local keys and foreign keys to match the actual schema; it does not create the foreign-key constraints for you.\n\n## Polymorphic loading\n\nThe query layer offers `morphWith($relation, $morphMap, $typeColumn, $idColumn)` and the ORM loader offers `loadMorphRelation()`. Use an explicit map of permitted type values to application model classes. Do not derive a PHP class name directly from request input.\n\nThis is TinyCore's mapping-based polymorphic loader; do not assume Laravel's `morphTo()` or `morphMany()` model-definition helpers exist.\n\n## Relationship aggregates\n\n`withCount()`, `withSum()`, `withAvg()`, `withMin()`, and `withMax()` add relationship aggregate information through the query layer. Callbacks constrain the related query. `withCount()` exposes extra comparison/count arguments, but its current implementation does not use them to filter the parent rows. Use `has()` / `whereHas()` for filtering.\n\nUse [query inspection](./query-builder.md#raw-sql-and-debugging) and feature tests for advanced relationship queries.\n\n\n## Nested loading and selected columns\n\nLoad each base relation before its nested path:\n\n```php\n$users = User::with(['posts', 'posts.comments'])->all();\n$users = User::with(['posts:id,user_id,title'])->all();\n```\n\nIncluding the base entry avoids the current nested-path parser's missing `columns` entry. Alternatively, use a callback on `posts` and call `$query->with('comments')` there. Preserve the parent key and related foreign keys in projections; a relationship cannot match rows when its identifying values are missing.\n\n`with()` changes what is loaded, not which parents match. Combine it with `whereHas()` when both the parent filter and eager-loaded records must meet a condition. Pass the condition to each operation explicitly; do not assume a loading callback automatically constrains an aggregate or existence subquery.\n\n## Create and associate related records\n\n```php\n$user = User::findOrFail($userId);\n$post = $user->posts()->create(['title' => 'From the community']);\n$user->posts()->createMany([\n    ['title' => 'First follow-up'],\n    ['title' => 'Second follow-up'],\n]);\n\n$post->user()->associate($otherUser);\n$post->save();\n```\n\n`HasMany` also offers `save()`, `saveMany()`, `firstOrCreate()`, and `createOrUpdate()`. Persist the parent first, and allow the assigned foreign-key field through the related model's fillable rules. `HasOne` shares these write helpers; enforce one-to-one uniqueness with a database constraint when required.\n\n`associate($model)` sets the inverse foreign key on the child. `dissociate()` clears it; the foreign-key column must allow null. Save the child to persist either change, then clear/reload cached relations when displaying them.\n\n## Pivot attributes and synchronization\n\nThe default pivot name sorts the two **table names** alphabetically (for example, `roles_users`). The earlier `role_user` example sets an explicit application convention.\n\n```php\n$user->roles()->attach([2 => ['assigned_by' => $adminId]]);\n$user->roles()->updateExistingPivot(2, ['assigned_by' => $newAdminId]);\n$changes = $user->roles()->sync([2, 3]);\n$user->roles()->syncWithoutDetaching([4]);\n$user->roles()->toggle([3, 4]);\n```\n\n`sync()` reports `attached` and `detached` IDs. It does not update attributes on existing associations; call `updateExistingPivot()` for those. `detach()` without IDs removes all pivot rows for that parent, not the related model rows. `syncWithoutDetaching()` keeps existing links, while `toggle()` detaches present IDs and attaches missing ones.\n\nConfigure `withPivot(['assigned_by'])` and `wherePivot('active', true)` in the relationship before executing it. Selected pivot data is exposed on the related model's `pivot` relation. Pivot helpers are direct database operations and do not invoke a related model's remove/restore lifecycle. Wrap a multi-step association change in a transaction when it must succeed as one operation; there is no implicit transaction across a sync workflow.\n\n## Aggregate names and aliases\n\n```php\n$users = User::withCount('posts')\n    ->withCount('posts as published_posts', fn($query) => $query->where('published', true))\n    ->withSum('posts', 'views')\n    ->all();\n```\n\nRead `posts_count`, `published_posts`, and `posts_sum` on the returned models. Default names are the relation plus the aggregate name; they do not include the aggregated column. Provide aliases when adding multiple aggregates that would collide. A nested existence/aggregate path constrains the outer related records by descendants; do not assume it flattens and counts every grandchild.\n\n## Polymorphic map shape\n\n```php\n$activities = Activity::morphWith('subject', [\n    'post' => ['class' => Post::class, 'relations' => ['user']],\n    'comment' => ['class' => Comment::class, 'relations' => ['user']],\n], 'subject_type', 'subject_id')->all();\n```\n\nStore only the explicit type keys such as `post` and `comment`. The loader groups records by type and uses the mapped model and nested relations. Include both type and ID columns in the activity projection.\n\n## Soft-deleted related records\n\nAfter applying the [soft-delete setup](./soft-deletes.md), each query uses the **related model's** own soft-delete setting and deletion column. If `Post` enables soft deletes, these operations exclude archived posts by default:\n\n| Operation | Default behavior |\n| --- | --- |\n| `$user->posts` or `User::with('posts')` | Loads active related posts |\n| `$user->posts()->count()` | Counts active posts belonging to this user |\n| `User::has('posts')` / `whereHas('posts', $callback)` | Matches users with active posts satisfying the constraints |\n| `User::doesntHave('posts')` | Matches users with no active posts, including users whose posts are all archived |\n| `User::withCount('posts')` | Adds an active-post count without filtering users |\n| `withSum()`, `withAvg()`, `withMin()`, `withMax()` | Aggregates active related rows |\n\nThe same rule applies to OR variants and `whereRelation*()` helpers. Relationship subqueries now use the prepared WHERE clause, so a manual `whereNull('posts.deleted_at')` is no longer needed for ordinary model relations. Custom `SOFT_DELETE_COLUMN` names are respected too.\n\n## Include archived related records\n\nSet the scope on the related query explicitly:\n\n```php\n$users = User::with([\n    'posts' => fn($query) => $query->withTrashed(),\n])->all();\n\n$users = User::whereHas('posts', fn($query) => $query->onlyTrashed())\n    ->withCount('posts')\n    ->withCount('posts as archived_posts', fn($query) => $query->onlyTrashed())\n    ->withCount('posts as all_posts', fn($query) => $query->withTrashed())\n    ->all();\n\n$allCount = $user->posts()->withTrashed()->count();\n$archivedCount = $user->posts()->onlyTrashed()->count();\n```\n\n`withoutTrashed()` or `withTrashed(false)` returns the related query to active-only selection. The last scope switch wins. `withCount()` returns zero for no matching rows; SQL `SUM`, `AVG`, `MIN`, and `MAX` normally return null for an empty set.\n\nParent and related scopes are independent. `User::withTrashed()->with('posts')` includes archived users while their loaded posts remain active-only. A scope inside the `posts` callback does not include archived users. Likewise, a `whereHas()` callback does not change the separate `with()` or `withCount()` query; apply the intended scope to each operation.\n\nFor an inverse relation, `Post::with('user')` returns a null `user` when its owner is archived and `User` uses soft deletes. Use `with(['user' => fn($query) => $query->withTrashed()])` when historical ownership should remain visible.\n\n## Nested archive scopes\n\nEach level of a nested relationship uses its own model scope. `has('posts.comments')` requires an active post with an active comment. A callback passed to `whereHas('posts.comments', ...)` changes the deepest query (`comments`), not the intermediate `posts` query. To include archived posts and comments, constrain both levels:\n\n```php\n$users = User::whereHas('posts', function ($posts) {\n    $posts->withTrashed()->whereHas('comments', fn($comments) => $comments->withTrashed());\n})->with([\n    'posts' => fn($posts) => $posts->withTrashed()->with([\n        'comments' => fn($comments) => $comments->withTrashed(),\n    ]),\n])->all();\n```\n\n## Joined relations and archive boundaries\n\nThe query builder qualifies automatic deletion predicates with the query's table or read alias, including custom deletion columns. A related table and a joined table can both have `deleted_at` without making the model's automatic scope ambiguous. You no longer need `withTrashed()` plus a manual condition just to disambiguate that scope. Qualify your own selected columns and conditions when a join repeats their names.\n\n### Through models\n\nIn `Country → User → Post`, `User` is the intermediate (through) model. When `User` enables soft deletes, `hasManyThrough()` excludes posts reached through archived users by default. It also applies `Post`'s own soft-delete scope. This works for lazy/eager loading, direct counts, `has()` / `whereHas()`, and relationship aggregates.\n\n```php\n// In Country:\npublic function posts(): \\Spark\\Database\\Relation\\HasManyThrough\n{\n    return $this->hasManyThrough(\n        Post::class, User::class,\n        firstKey: 'country_id', secondKey: 'user_id',\n    );\n}\n```\n\nUse `withTrashedParents()` to include archived intermediate models. It does not include archived final records; `withTrashed()` controls those separately:\n\n```php\n$activePosts = $country->posts()->all();\n$viaAllUsers = $country->posts()->withTrashedParents()->all();\n$allPosts = $country->posts()->withTrashedParents()->withTrashed()->all();\n\n$countries = Country::with([\n    'posts' => fn($query) => $query->withTrashedParents(),\n])->withCount('posts', fn($query) => $query->withTrashedParents())->all();\n\n$countries = Country::whereHas('posts', fn($query) => $query->withTrashedParents())->all();\n```\n\n`withTrashedParents(false)` restores intermediate-model filtering. You can also chain it on the relationship definition to set that relation's default; loading, existence queries, and aggregates preserve that setting. The outer `Country` query keeps its own scope. Intermediate models without soft deletes add no deletion predicate, and custom deletion-column names are respected.\n\n### Plain pivot tables\n\nA many-to-many pivot such as `role_user` stores links, not a model with a declared soft-delete setting. TinyCore does not inspect its schema or assume it has `deleted_at`. The `Role` model's own archive scope still applies automatically.\n\nIf your application deliberately archives links, add the deletion column and opt into active-link filtering once in the relationship:\n\n```php\npublic function roles(): \\Spark\\Database\\Relation\\BelongsToMany\n{\n    return $this->belongsToMany(\n        Role::class, table: 'role_user',\n        foreignPivotKey: 'user_id', relatedPivotKey: 'role_id',\n    )->wherePivotNull('deleted_at');\n}\n```\n\nThat condition is shared by `$user->roles`, `with('roles')`, `has('roles')`, `withCount('roles')`, and direct relation queries. `wherePivotNotNull('deleted_at')` selects archived links; define a separate relation without the active-link condition when building a link trash screen. `withTrashed()` controls related roles, so it does not remove a configured pivot condition. `withTrashedParents()` applies to through models, not plain pivot rows.\n\nThis is a **read filter**, not a pivot archive lifecycle. `detach()` / `sync()` still physically remove associations; archive or restore link rows explicitly if that is your application policy. Ordinary pivot tables without an archive column need no change.\n\nFor general `join()` calls outside these relationships, only the bound model is scoped automatically. The builder cannot infer the intended model or archive policy of an arbitrary joined table. Relationship-definition callbacks apply to lazy/eager reads; existence/aggregate queries apply their own supplied callback. The declared pivot conditions and `withTrashedParents()` relation setting are carried into all these paths.\n\nArchiving a parent does not archive its children or delete pivot rows. Restoring a parent does not restore related records. Design cascading workflows explicitly and reload cached relationships after archive/restore operations.\n";export{e as default};
