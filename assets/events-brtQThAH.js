const e="## Register listeners\n\nEvents decouple a completed action from its in-process follow-up behavior. Register listeners in a provider's `boot()` method:\n\n```php\nevent()->addListener('order.created', function ($order) {\n    tracer_log('Order created: ' . $order->id);\n});\n```\n\nListeners may be closures, callable arrays, or resolvable class callbacks. `app()->on()` is an application shortcut. `addListener()` accepts a priority; use explicit priorities when execution order matters and keep listeners independent where possible.\n\n## Dispatch payloads\n\n```php\nevent('order.created', $order);\nevent()->dispatch('order.created', $order);\n```\n\nThe global helper's signature is `event($eventName = null, $payload = [], $halt = false)`. An array payload is treated as an argument list. Wrap an array if the listener expects that entire array as one argument:\n\n```php\nevent('report.ready', [['id' => 12, 'format' => 'csv']]);\n```\n\nThe dispatcher method `dispatch($eventName, ...$args)` directly accepts multiple arguments. Do not pass extra positional payload arguments to the helper and accidentally populate its `$halt` parameter.\n\n## Responses and conditional dispatch\n\n`dispatchWithResponse()` returns listener responses. `until()` stops at the first non-null response; `event('name', $payload, halt: true)` uses that behavior. `dispatchIf()` and `dispatchUnless()` accept an explicit condition. `halt()` can stop current dispatch processing.\n\nUse response-oriented dispatch for a well-defined extension point. For a simple notification, prefer ordinary dispatch.\n\n## One-time listeners and subscriptions\n\n`once()` registers a listener that removes itself after running. `subscribe($events, $priority)` registers an event/listener map. The helper also accepts a listener map when its first argument is an array.\n\n`removeListener()` removes one callback; `clearListeners($eventName)` removes a group's callbacks. `flush()` resets all listeners.\n\n## Inspect and troubleshoot\n\nUse `hasListeners()`, `getListeners()`, `countListeners()`, `getEventNames()`, and `hasEvent()` to inspect registration. Check the event name, provider boot order, listener signature, and payload argument shape when a listener does not run as expected.\n\nEvents execute in the current process. An event is not durable background delivery. Dispatch a [queue job](./queues.md) from a listener when the work needs retries or can be slow, and dispatch only after the relevant database transaction succeeds.\n\n## Listener order and failure behavior\n\nHigher numeric priority runs first. Ordinary `dispatch()` stops if a listener returns `false` or calls `halt()`. `until()` instead returns the first non-null response, including `false`; it is useful for a lookup with several providers.\n\n```php\nevent()->addListener('report.lookup', function (int $id) {\n    return cache('reports')->retrieve((string) $id);\n}, priority: 10);\n\n$cached = event()->until('report.lookup', $reportId);\n```\n\nListener errors are logged; debug mode rethrows them, while non-debug dispatch can continue. Do not use a listener exception as the sole mechanism for rejecting a business operation. Validate or authorize explicitly before dispatching, and test failure handling in the intended environment.\n";export{e as default};
