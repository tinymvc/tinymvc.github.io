const e="## Define a route\n\nImport `Spark\\Facades\\Route` in your route file. The `router()` helper exposes the same underlying service.\n\n```php\nuse Spark\\Facades\\Route;\n\nRoute::get('/status', fn() => ['status' => 'ok']);\nRoute::post('/messages', [MessageController::class, 'store']);\n```\n\nImport your application controller before using its short class name. Callbacks can be closures, invokable classes, or class/method arrays resolved through the container.\n\n## HTTP methods\n\nUse `get`, `post`, `put`, `patch`, `delete`, and `options` for a single method. `match()` accepts an array of methods; `any()` accepts all methods.\n\n```php\nRoute::match(['GET', 'POST'], '/preview', [PreviewController::class, 'show']);\nRoute::options('/messages', fn() => response('', 204))->middleware('cors');\n```\n\nUse a narrow method list for state-changing endpoints. Browser forms can send `@method('PUT')` with an actual POST; the request's method handling recognizes the override.\n\n## Parameters\n\n```php\nRoute::get('/posts/{id}', fn(int $id) => ['id' => $id]);\nRoute::get('/archive/{year?}', fn(?string $year = null) => [\n    'year' => $year,\n]);\nRoute::get('/files/*', fn() => response('Matched a file path'));\n```\n\nName callback parameters to match route placeholders. Supply a default value for optional parameters. To read the value explicitly, use `$request->route('id')`; `hasRouteParam()` checks presence.\n\nWildcard paths match a tail of the URL. Register specific paths before broad wildcard/fallback routes, and validate identifiers before using them as database or filesystem input.\n\n## Named routes and URLs\n\n```php\nRoute::get('/posts/{id}', [PostController::class, 'show'])\n    ->name('posts.show');\n\n$url = route_url('posts.show', ['id' => 42]);\nreturn to_route('posts.show', ['id' => 42]);\n```\n\n`as()` aliases `name()`. `route_url()` returns a string; `route()` returns a `Spark\\Url` object. `router()->has('posts.show')` checks whether a name exists. Use unique route names and pass the required placeholders.\n\n## Groups\n\n```php\nRoute::group([\n    'prefix' => 'admin',\n    'middleware' => ['auth'],\n], function () {\n    Route::get('/dashboard', [DashboardController::class, 'index'])\n        ->name('dashboard');\n});\n```\n\nThe `auth` alias must be registered in your application. A controller group can use a shared callback class:\n\n```php\nRoute::group([\n    'prefix' => 'posts',\n    'callback' => PostController::class,\n], function () {\n    Route::get('/', 'index')->name('posts.index');\n    Route::post('/', 'store')->name('posts.store');\n});\n```\n\nGroup attributes include prefix/path, method, callback, name, middleware, and exclusions. The fluent router also supports `prefix()`, `middleware()`, `withoutMiddleware()`, and `group()`; keep related routes together rather than repeating setup.\n\n## Middleware and exclusions\n\n```php\nRoute::get('/account', [AccountController::class, 'show'])\n    ->middleware(['auth', 'throttle:60,1,account']);\n\nRoute::post('/hooks/provider', [WebhookController::class, 'store'])\n    ->withoutMiddleware('csrf');\n```\n\nRemoving CSRF is appropriate only when another mechanism, such as a verified webhook signature or stateless token authentication, protects the endpoint. See [CSRF & CORS](./csrf-cors.md).\n\n## Resource routes\n\n```php\nRoute::resource('/posts', PostController::class, name: 'posts');\n```\n\n| Method | Path | Controller action |\n| --- | --- | --- |\n| GET | `/posts` | `index` |\n| GET | `/posts/create` | `create` |\n| POST | `/posts` | `store` |\n| GET | `/posts/{id}` | `show` |\n| GET | `/posts/{id}/edit` | `edit` |\n| PUT / PATCH | `/posts/{id}` | `update` |\n| DELETE | `/posts/{id}` | `destroy` |\n\nUse `->only(['index', 'show'])` or `->except(['create', 'edit'])` to limit the generated routes. Resource builders also accept names, middleware, and middleware exclusions.\n\n## Views, redirects, and fallbacks\n\n```php\nRoute::view('/about', 'pages.about');\nRoute::redirect('/old-about', '/about', 301);\nRoute::fallback(fn() => response('Page not found', 404));\n```\n\n`Route::fireline()` renders a [FireLine-aware view](./fireline.md). `Route::inertia()` becomes available only after installing the [Inertia provider](./inertia.md); it is an integration macro, not a core router method.\n\n## Route files and inspection\n\n`withRouting(api: ...)` automatically adds `/api`; `withRouting(webhook: ...)` adds `/webhook`. Web routes have no automatic prefix.\n\n```bash\nphp spark route:list\n```\n\nUse the route listing to verify methods, paths, names, and registration before debugging a controller. For the low-level `add()`, route builders, and router extension methods, see the [API reference](./api-reference.md).\n\n## Model parameters and route boundaries\n\n```php\nuse App\\Models\\Post;\n\nRoute::get('/posts/{post:slug}', function (Post $post) {\n    return view('posts.show', ['post' => $post]);\n});\n```\n\nThe placeholder name must match the type-hinted parameter. The field after `:` replaces the primary-key lookup; a missing model produces 404. Normal model lookup excludes archived rows when soft deletes are enabled. A trash endpoint should explicitly query `onlyTrashed()` using an ID and its ownership conditions.\n\nResource routes always use `{id}`. Their actions should accept `int $id` and look up the model, or use a model parameter named `$id`; a `show(Post $post)` action belongs with an explicit `{post}` route. Register a specific `/posts/create` or `/posts/search` route before `/posts/{post}`.\n\nThe matcher accepts an optional trailing slash and treats HEAD as a GET match. CORS preflight matching uses `Access-Control-Request-Method`; see [CSRF & CORS](./csrf-cors.md#preflight-behavior). A fallback callback runs outside the matched-route middleware pipeline, so keep it limited to a not-found response instead of protected application work.\n\nRoute and resource builders register on destruction. Finish fluent declarations as ordinary statements; retaining builders in long-lived variables delays registration. `route:list` helps catch that kind of bootstrap issue.\n";export{e as default};
