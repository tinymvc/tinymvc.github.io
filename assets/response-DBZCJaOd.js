const e="## Content and status\n\n```php\nreturn response('Saved', 200);\nreturn json(['data' => $post], 201);\nreturn response('', 204);\n```\n\nThe response object is `Spark\\Http\\Response`. A route can return an array directly for JSON, but an integer return value represents an HTTP status, not a numeric response body. Use `response('42')` when the body should be the text `42`.\n\n## Headers\n\n```php\nreturn json(['status' => 'ok'])\n    ->setHeader('X-Request-Source', 'spark')\n    ->withHeaders(['Cache-Control' => 'no-store']);\n```\n\n`setContent()`, `write()`, `setStatusCode()`, `withData()`, and `withJson()` configure an existing response. `getContent()`, `getStatusCode()`, and `getHeaders()` support inspection and tests.\n\n## Redirects\n\n```php\nreturn redirect('/login');\nreturn to_route('posts.show', ['id' => $post->id]);\nreturn back()->with('message', 'Your changes were saved.');\n```\n\nUse named-route redirects when paths may change. Pass only trusted destinations to redirects, especially when accepting a “return to” URL from a request.\n\nFor validation errors:\n\n```php\nreturn back()\n    ->withErrors(['email' => 'This address is already in use.'])\n    ->withInput(['email' => $request->input('email')]);\n```\n\n## Response caching\n\n`noCache()` sets headers to prevent caching. `cache($seconds)` configures a cacheable response for a duration. Cache only content appropriate for all clients who can receive that representation; account-specific data should not be exposed through shared caches.\n\n## Files and downloads\n\n```php\nresponse()->file(storage_dir('reports/summary.pdf'));\n```\n\n```php\nresponse()->download(\"name,total\\nAda,12\\n\", 'report.csv', [\n    'Content-Type' => 'text/csv',\n]);\n```\n\n`file()` accepts a filesystem path. `download()` accepts **content**, a filename, and headers. These methods send the response and return `void`; they are not fluent response builders. Authorize access and derive file paths from trusted application data.\n\n## JSON details\n\n`json()` accepts an array, a status code, encoding flags, and a depth. Use models' hidden/visible attribute settings or explicit output arrays to control serialization. A successful JSON encoding does not imply a safe public response: keep password hashes and internal credentials out of the payload.\n\n## Sending and clearing\n\nThe normal router lifecycle calls `send()` for you. Only call it directly in an integration that takes over response handling. `clear()` resets the response object, while `noContent()` prepares an empty response. Prefer one response per controller action.\n\nFor cookies and flashed state, see [Sessions & cookies](./session.md).\n\n## Build a predictable API response\n\n```php\nreturn json([\n    'data' => ['id' => $post->id, 'title' => $post->title],\n], 201)->setHeader('Location', route_url('posts.show', ['id' => $post->id]));\n```\n\nUse 201 for a newly created resource and 204 when there is deliberately no body. Choose a consistent error shape across your application; framework validation uses `message` and `errors` with status 422. A status code alone does not validate the response payload.\n\n`noContent()` prepares status 204 and clears its content. Keep empty responses empty explicitly. A HEAD request can still invoke a GET route’s callback, so GET handlers should not perform a state-changing operation.\n";export{e as default};
