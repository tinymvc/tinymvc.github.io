const e="## String helpers\n\nUse `Spark\\Support\\Str` for static operations or `str()` for fluent strings:\n\n```php\nuse Spark\\Support\\Str;\n\n$column = Str::snake('DisplayName');\n$title = Str::headline('user_profile');\n$clean = str('  hello spark  ')->trim()->title()->toString();\n```\n\nCommon operations include case conversion (`camel`, `snake`, `studly`, `kebab`, `headline`), inspection (`contains`, `startsWith`, `endsWith`, `is`), extraction (`before`, `after`, `between`, `substr`), and formatting (`limit`, `words`, `replace`, `padLeft`, `padRight`).\n\nUse `Stringable` chains for readable transformations and convert to a string at a boundary that requires one.\n\n## Slugs, inflection, Markdown, and UUIDs\n\nSome features rely on optional Composer packages:\n\n| Feature | Package |\n| --- | --- |\n| Markdown conversion | `league/commonmark` |\n| Non-English slug transliteration | `voku/portable-ascii` |\n| `Str::uuid()` | `ramsey/uuid` |\n| Plural/singular inflection | `doctrine/inflector` (core dependency) |\n\n`Spark\\Support\\Pluralizer` supplies inflection behavior. A generated slug or UUID is an identifier, not an authorization secret. Enforce uniqueness where required by your database/application.\n\nMarkdown output is HTML. Configure allowed HTML/link behavior for your application's trust model before rendering user-supplied Markdown unescaped.\n\n## Numbers\n\n`Spark\\Support\\Number` provides number formatting utilities, including readable numbers, percentages, currency, and compact/file-size formats supported by the class. Some localized formatting needs PHP's Intl extension.\n\nKeep calculations in a well-defined numeric representation and format only for display. Do not parse a localized display string back into a business value by removing punctuation.\n\n## HTML and JavaScript values\n\n`Spark\\Support\\HtmlString` marks already-trusted HTML. It does not sanitize the content. Prefer escaped Blade output for untrusted strings.\n\n`Spark\\Support\\Js` encodes values for JavaScript contexts; Blade's `@json()` uses it. Do not concatenate user data into inline script source manually.\n\n## Support helpers\n\n`e()` escapes HTML. `blank()` / `filled()` inspect emptiness. `value()` resolves a lazy value, `tap()` performs an action while retaining the original value, and `transform()` applies a callback conditionally. See [Helpers](./helpers.md) for their exact declarations.\n\n## Macros and conditional operations\n\nClasses using `Spark\\Support\\Traits\\Macroable` can register application extensions via `macro()`; `hasMacro()` and `flushMacros()` inspect/reset them. Register macros once in a provider and avoid collisions with existing methods.\n\nThe `Conditionable` trait supplies `when()` / `unless()` patterns on supporting classes. A macro does not become a core API automatically; document application-specific extensions alongside your app.\n\nUse the [API reference](./api-reference.md) to search every current Str, Stringable, Number, Pluralizer, HtmlString, Js, and trait declaration.\n\n## Formatting examples\n\n```php\nuse Spark\\Support\\Number;\nuse Spark\\Support\\Str;\n\n$summary = Str::limit('A longer explanation of the feature', 24);\n$slug = Str::slug('Hello Spark');\n$amount = Number::currency(1250, in: 'USD');\n$size = Number::fileSize(1536);\n```\n\nInstall the optional dependency for an operation before using it in a request. `Number` uses Intl for locale-aware formatting; output depends on the chosen locale and currency. Keep integer minor units or your chosen decimal representation for stored amounts, and convert deliberately for display.\n";export{e as default};
