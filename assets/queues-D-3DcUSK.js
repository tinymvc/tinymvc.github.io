const e="## Configure a queue\n\nQueue settings live in `config/queue.php`, separately from cache settings. The default driver is SQLite:\n\n```php\nreturn [\n    'driver' => env('QUEUE_DRIVER', 'sqlite'),\n    'connections' => [\n        'sqlite' => ['path' => dirname(__DIR__) . '/storage/queue/jobs.db'],\n        'redis' => [\n            'host' => env('REDIS_HOST', '127.0.0.1'),\n            'port' => env('REDIS_PORT', 6379),\n            'password' => env('REDIS_PASSWORD', null),\n            'database' => env('REDIS_DATABASE', 0),\n            'prefix' => env('REDIS_PREFIX', 'spark'),\n        ],\n    ],\n];\n```\n\nThe SQLite path is a **file**, unlike the cache directory. Redis also accepts timeout/read-timeout/persistent options through its connector. Workers and web processes must share the intended queue storage.\n\n## Create a class job\n\n```bash\nphp spark make:job SendWelcomeEmail\n```\n\n```php\n<?php\n\nnamespace App\\Jobs;\n\nuse Spark\\Queue\\Contracts\\JobInterface;\nuse Spark\\Queue\\Contracts\\JobContract;\nuse Spark\\Queue\\Dispatchable;\nuse Throwable;\n\nclass SendWelcomeEmail implements JobInterface\n{\n    use Dispatchable;\n\n    public int $tries = 3;\n    public array $backoff = [120, 300];\n\n    public function __construct(private int $userId) {}\n\n    public function handle(): void\n    {\n        // Load the user and send the message through your application service.\n    }\n\n    public function failed(JobContract $job, Throwable $exception): void\n    {\n        tracer_log('Welcome email failed after all attempts.');\n    }\n}\n```\n\nKeep constructor data serializable and small, usually record IDs. The worker invokes `handle()` through the container, allowing service dependencies in the handler. A real handler must perform the application work represented by this example.\n\n## Dispatch and delay\n\n```php\nSendWelcomeEmail::dispatch($userId)->onQueue('emails');\nSendWelcomeEmail::dispatch($userId)->onQueue('emails')->delay(60);\n```\n\nConstructor arguments are passed through `dispatch(...$arguments)`. Pending dispatches are sent when the fluent object leaves scope; call `send()` or `dispatch()` to enqueue explicitly.\n\n`onQueue()` chooses the queue name. `delay()` uses seconds; `schedule()` accepts a date/time string or `Spark\\Carbon`. The driver is selected by queue configuration; there is no core `onConnection()` fluent method.\n\n## Repeating work\n\n```php\nSyncReports::dispatchOnce()\n    ->onQueue('reports')\n    ->repeatEveryMinutes(5);\n```\n\nUse `dispatchOnce()` or `once()` for repeated bootstrap/scheduler registration so the same pending job is not inserted on every request. This duplicate-safe insertion is not a promise that a business side effect executes exactly once.\n\nSupported helpers include `repeatHourly()`, `repeatDaily()`, `repeatWeekly()`, and `repeatMonthly()`. `repeat()` accepts the Job repeat constants or aliases: `hourly`, `daily`, `weekly`, `biweekly`, `monthly`, `quarterly`, and `yearly`.\n\nRegister recurring jobs in bootstrap with:\n\n```php\n->withQueue(jobs: [\n    job(App\\Jobs\\SyncReports::class)->repeatEveryMinutes(5),\n])\n```\n\n`withQueue()` uses duplicate-safe push behavior and accepts an optional `then:` callback.\n\n## Job wrapper API\n\nThe `job()` helper wraps a **class/function string or callable array**, parameters, scheduled time, and repeat configuration:\n\n```php\njob(App\\Jobs\\SendWelcomeEmail::class, [$userId])->dispatch('emails');\n```\n\nClosures are not accepted by the current Job constructor. Prefer class jobs for durable application tasks instead of copying the older closure examples.\n\n## Run a worker\n\n```bash\nphp spark queue:work --queue=emails\nphp spark queue:work --once --queue=emails\n```\n\nCLI options are `--once`, `--timeout`, `--sleep`, `--delay`, `--tries`, and `--queue`. The current CLI defaults are timeout **3580 seconds**, idle sleep **4 seconds**, retry delay **8 seconds**, and **3** attempts. Direct `Queue::work()` defaults differ (3600, 3, 5, and 3 respectively); set explicit values when consistency matters.\n\nRun workers under a process supervisor in production and restart them after code/config changes. A repeating job still needs a running worker; registration alone does not execute it.\n\n## Retries and failure hooks\n\nA public `$tries` overrides the worker attempt count. A public `$backoff` integer or array controls retry delays in **seconds**. Array entries are selected by failed attempt number; later attempts reuse the final delay. Use lowercase `backoff`.\n\n`failed()` runs only after attempts are exhausted. It may accept `Throwable $exception` or `JobContract $job, Throwable $exception`. Keep failure reporting reliable and avoid leaking credentials from payloads into logs.\n\nJobs may run again after failure or recovery. Design email sends, billing operations, and imports around idempotency keys or recorded completion state.\n\n## Inspect and recover\n\n```bash\nphp spark queue:list\nphp spark queue:failed\nphp spark queue:retryFailed\n```\n\n`queue:clear` and `queue:clearFailed` remove queued/failed work; use them deliberately. The Queue service also exposes `getJobs()`, `getFailedJobs()`, `retryFailedJobs()`, `removeJobById()`, `removeQueue()`, and clearing methods. Job metadata exposes its ID, queue name, attempts, creation/failure time, and failure reason.\n\nThe public queue logging option was removed. Do not pass `log: true` or call `Queue::logging()`. Use application logging and supervisor output instead.\n\n## Commit data before dispatching\n\nA worker may consume a job as soon as it is inserted. Dispatch after the database transaction has successfully committed so the worker can read the referenced rows:\n\n```php\nuse Spark\\Facades\\DB;\nuse App\\Jobs\\SendWelcomeEmail;\nuse App\\Models\\User;\n\n$user = DB::transaction(fn() => User::create($validated));\nSendWelcomeEmail::dispatch($user->id)->onQueue('emails')->send();\n```\n\nThis queue has no automatic `afterCommit()` coordination. If the enqueue itself fails after commit, the row already exists; an outbox or explicit retry record is appropriate when delivery must be recoverable.\n\n## Worker lifetime and scheduling\n\n`--timeout` limits the worker loop's overall lifetime, checked between jobs. It is not a hard per-job execution timeout and does not interrupt a blocked handler. Configure network/database timeouts inside jobs and let a supervisor restart a worker that exits normally. `--once` exits after processing one job. If the queue is empty, the current loop continues until its overall timeout; it is not an immediate empty-queue probe. Use a short explicit timeout when invoking it from bounded tooling.\n\nRepeats are interval-based scheduling, not cron expressions. `repeatEveryMinutes(5)` schedules subsequent runs through the queue; missed executions are not a promise to replay every elapsed five-minute slot. Inspect `getScheduledTime()` and job metadata when diagnosing timing, and keep application timezone configuration consistent between CLI and web processes.\n";export{e as default};
