const e="## Requirements\n\nUse **PHP 8.2 or newer** and Composer. Install the PDO driver for your database. The default cache, lock, and queue drivers use SQLite, so enable `pdo_sqlite` even when your application database uses MySQL or PostgreSQL.\n\n| Feature | Additional requirement |\n| --- | --- |\n| Frontend asset build | Node.js and npm compatible with the application's Vite version |\n| HTTP client | PHP cURL extension |\n| Upload MIME detection | PHP Fileinfo extension |\n| Image transformations | PHP GD extension |\n| Encryption | PHP OpenSSL extension |\n| Localized number formatting | PHP Intl extension |\n| Unicode string operations | PHP Mbstring extension |\n| Redis services | PHP Redis extension and a Redis server |\n| Mail | `phpmailer/phpmailer` |\n| Markdown strings | `league/commonmark` |\n| International slug transliteration | `voku/portable-ascii` |\n| `Str::uuid()` | `ramsey/uuid` |\n\nCheck your CLI environment before installing:\n\n```bash\nphp -v\nphp -m\ncomposer --version\n```\n\n## Create a project\n\n```bash\ncomposer create-project tinymvc/skeleton my-app\ncd my-app\n```\n\nThe skeleton contains your application structure and installs `tinymvc/tinycore` under `vendor/`. Work in `app/`, `routes/`, `config/`, and `resources/`; framework updates belong in Composer dependencies.\n\nIf `.env` was not created during installation, copy the example:\n\n```bash\ncp .env.example .env\nphp spark key:generate\n```\n\nGenerate the application key once for a new application. Preserve it across deployments: changing it affects encrypted data and signed credentials.\n\n## Start the application\n\n```bash\nphp spark serve\n```\n\nOpen `http://localhost:8080`. To choose a different port, pass it as a positional argument:\n\n```bash\nphp spark serve 8090\n```\n\nKeep `APP_URL` in `.env` aligned with the URL you use. This development server is for local work; production web servers must point at `public/`.\n\n## Start frontend development\n\nIn another terminal:\n\n```bash\nnpm install\nnpm run dev\n```\n\nThe skeleton's Vite entry is `resources/app/app.js`. Its `@vite` template directive loads assets from the development server while developing. For deployment, run `npm run build` and include the generated `public/build` directory. See [Vite & Alpine](./vite.md).\n\n## Application directories\n\n| Path | Purpose |\n| --- | --- |\n| `app/Http/Controllers` | Request handlers |\n| `app/Http/Middlewares` | Application middleware |\n| `app/Http/Requests` | Reusable validation requests |\n| `app/Models` | Database models |\n| `app/Providers` | Service registration and boot logic |\n| `app/Jobs` | Background jobs, when added |\n| `bootstrap` | Application, middleware, provider, and helper setup |\n| `config` | PHP arrays that configure services |\n| `database/migrations` | Database schema changes |\n| `public` | The only publicly served directory |\n| `resources/views` | Blade templates |\n| `resources/app` | Frontend source assets |\n| `routes/web.php` | Browser routes |\n| `storage` | Writable cache, logs, temporary files, and uploads |\n| `tests` | Unit and application feature tests |\n\nThe basic skeleton loads web routes. Create optional API, webhook, or console route files when you need them, then register those files in [bootstrap](./lifecycle.md).\n\n## Verify the setup\n\n```bash\nphp spark route:list\nphp test\n```\n\nIf the application cannot write caches, check ownership and write access for `storage/` and the SQLite database location. A “could not find driver” error means the relevant PDO extension is missing from the PHP installation actually running the app.\n\nContinue with the [Quick start](./quick-start.md).\n";export{e as default};
