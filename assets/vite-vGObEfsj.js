const e="## Development workflow\n\nThe skeleton uses Vite with Tailwind CSS and Alpine. Source assets live in `resources/app`, the entry is `app.js`, and the development server uses port **5173**.\n\n```bash\nnpm install\nnpm run dev\n```\n\nRun the PHP server separately with `php spark serve`. Load your page through the PHP application's URL; Vite serves the assets and hot updates.\n\n```html\n@vite('app.js')\n```\n\n`@vite` without arguments uses the default entry. An array loads multiple entries, such as `@vite(['app.tsx', 'app.css'])`, provided those entries are included in the build configuration.\n\n## Build configuration\n\nThe skeleton's `vite.config.js` defines:\n\n| Setting | Skeleton value |\n| --- | --- |\n| Root | `resources/app` |\n| Production base | `/build/` |\n| Output directory | `public/build` |\n| Manifest | Enabled; `.vite/manifest.json` in the output |\n| Alias `@` | `resources/app` |\n| Development port | 5173, strict port |\n| Template reload | `resources/views/**/*.blade.php` |\n\nKeep the PHP Vite helper and JavaScript configuration aligned when changing these paths.\n\n## Alpine components\n\nThe skeleton initializes Alpine in `resources/app/app.js` and imports `app.css`. Add focused interactions to Blade:\n\n```html\n<div x-data=\"{ open: false }\">\n    <button type=\"button\" x-on:click=\"open = !open\" x-bind:aria-expanded=\"open\">\n        Show details\n    </button>\n    <p x-show=\"open\">A little JavaScript, right where it is useful.</p>\n</div>\n```\n\nRegister plugins before `Alpine.start()`. Avoid initializing Alpine twice when adding FireLine or another plugin.\n\n## PHP helper configuration\n\nThe underlying `Spark\\Utils\\Vite` accepts an entry string, entry array, or configuration array. Config keys include `scheme`, `host`, `port`, `running`, `base`, `root`, `entry`, `dist`, `dist_path`, `manifest`, and `manifest_path`.\n\nThe default distribution is `build` under `public`. `running: null` allows detection; an explicit boolean selects the mode. `getManifestPath()`, `hasManifest()`, `assetUrl()`, `importsUrls()`, and `cssUrls()` are useful when diagnosing custom integration paths. `reactRefreshTag()` supports React development setup.\n\n## Production assets\n\n```bash\nnpm run build\n```\n\nDeploy the complete `public/build` output, including `.vite/manifest.json`. The PHP helper reads the manifest to emit hashed asset paths and dependent CSS/imports. Build source files are not a substitute for the production manifest.\n\nA missing-manifest error usually means assets were not built, a hidden `.vite` directory was omitted from deployment, or the PHP and Vite output paths disagree. A browser requesting the development port in production indicates incorrect development detection/configuration.\n\n## React, Vue, and other clients\n\nThe core asset helper is not limited to Alpine. Configure the appropriate Vite plugin and entry for your client framework, then use the matching [Inertia client and adapter](./inertia.md) when building an Inertia application. [Orbit](./orbit.md) supplies a configured React administration starter.\n\n## Switching between a build and hot reload\n\nWith `running: null`, local request hosts (`localhost`, loopback addresses, and\n`.test`, `.local`, `.localhost`, `.localdomain`, or `.internal` suffixes) probe the\ndev server first, even when a build manifest exists. A missing request host is also\ntreated as local. If the probe fails, the helper uses the manifest.\n\nFor other request hosts, an existing manifest selects production assets without a\nnetwork probe; without one, the helper probes the dev server as a fallback. The\nprobe has a short timeout and retries disabled. `app.debug` and the configured Vite\nasset host do not determine whether the request host is local.\n\nSelect development mode explicitly in local-only configuration when needed:\n\n```html\n@vite('app.js', ['running' => true, 'host' => 'localhost', 'port' => 5173])\n```\n\nDo not deploy that forced development setting. `running: false` explicitly selects production assets. The host must be reachable from the browser, and HTTPS application pages need a compatible asset-server scheme to avoid mixed-content failures. React entries can include the refresh preamble through the helper; their Vite configuration still needs the React plugin.\n";export{e as default};
