const e="## Run Spark commands\n\n```bash\nphp spark help\nphp spark help serve\nphp spark route:list\n```\n\nRun from the application root. The entry point is `spark`, not `artisan`. Command handlers use the same application container and configuration as the rest of the app.\n\n## Built-in commands\n\n| Command | Purpose |\n| --- | --- |\n| `help [command]` | List commands or show one command's help |\n| `serve [port]` | Local PHP server, default port 8080 |\n| `route:list` | Inspect registered routes |\n| `key:generate` | Generate an application key |\n| `migrate` | Apply pending migrations |\n| `migrate:rollback --step=2` | Roll back recorded migrations |\n| `migrate:fresh` | Roll back all and run again |\n| `queue:work` | Process queued work |\n| `queue:list` | List scheduled jobs |\n| `queue:failed` | List failed jobs |\n| `queue:retryFailed` | Retry failed jobs |\n| `queue:clear` | Remove queued jobs |\n| `queue:clearFailed` | Remove failed jobs |\n| `view:clear` | Clear compiled templates |\n| `cache:clear` | Clear framework caches |\n| `config:clear` | Remove cached configuration |\n| `storage:link` | Link public uploads |\n| `tinker` | Interactive PHP application shell |\n\n`migrate --seed` includes seed files. Worker options and current CLI defaults are documented in [Queues](./queues.md#run-a-worker). Destructive commands should target a deliberately selected environment and database.\n\n## Generators\n\n```bash\nphp spark make:controller PostController\nphp spark make:model Post\nphp spark make:migration create_posts_table\nphp spark make:request StorePostRequest\nphp spark make:job PublishPost\n```\n\nOther generators are `make:provider`, `make:seeder`, `make:cast`, `make:middleware`, `make:view`, and `make:component`. Generated stubs are starting points; review namespaces, table names, rules, and registration before using them.\n\nIntegrations may add commands. For example, Orbit adds `make:bread`; it is not a command of an unmodified TinyCore installation.\n\n## Register a custom command\n\nCreate `routes/console.php` and load it using `withRouting(commands: ...)` or `withCommands(load: ...)` in bootstrap:\n\n```php\n<?php\n\nuse Spark\\Console\\Prompt;\n\ncommand('greet', function (array $args) {\n    $name = $args['_args'][0] ?? 'developer';\n    Prompt::message('Hello, ' . $name . '!', 'success');\n})\n    ->description('Print a greeting')\n    ->help('Usage: php spark greet [name]');\n```\n\n```bash\nphp spark greet Ada\n```\n\nPositional arguments are available in `$args['_args']`; parsed options appear by their option name. Validate argument values before using them in SQL, paths, or process commands.\n\n## Command classes and injection\n\nUse a class with `__invoke()` or a `[ClassName::class, 'handle']` callback for a substantial command. Constructor and method dependencies are resolved through the container. Keep business operations in a service so HTTP routes and commands can use the same behavior.\n\n## Prompts and output\n\n`Spark\\Console\\Prompt` supports `ask()`, `confirm()`, `message()`, and additional formatting/selection utilities listed in the [API reference](./api-reference.md). A noninteractive deployment command should not wait indefinitely for input; choose explicit flags or a separate workflow where appropriate.\n\n## Registry and process tools\n\nThe Commands service supports `addCommand`, `addCommands`, `removeCommand`, `disable`, `isDisabled`, `hasCommand`, `getCommand`, and `getAllCommands`. Descriptions and help improve discoverability.\n\nUse [Process](./concurrency.md#execute-a-process) for external commands, check exit status, and keep argument handling safe. Test commands with isolated configuration before scheduling them against production data.\n";export{e as default};
