const e="## Read and write\n\nUse `filemanager()` / `fm()` or `Spark\\Utils\\FileManager`:\n\n```php\n$path = storage_dir('reports/summary.txt');\nfm()->ensureDirectoryExists(dirname($path));\n$bytes = fm()->put($path, 'Report generated.');\n$content = fm()->get($path);\n```\n\n`get()` returns a string or false. `put()`, `append()`, and `prepend()` return a byte count or false. Check failure explicitly; an empty string or zero-byte write can be a valid result.\n\n## File operations\n\n| Method | Purpose |\n| --- | --- |\n| `exists`, `isFile`, `isDirectory` | Path checks |\n| `isReadable`, `isWritable` | Access checks |\n| `copy`, `move`, `delete` | File operations |\n| `size`, `lastModified`, `mimeType` | Metadata |\n| `extension`, `name`, `basename`, `dirname` | Path components |\n| `permissions`, `chmod` | Permission inspection/change |\n| `lines`, `replace` | Text-file helpers |\n| `hash`, `same` | Content comparison |\n| `uniqueFilename` | Choose a non-conflicting name |\n| `putAtomic` | Write through the utility's atomic-write path |\n\n`isImage()` checks image metadata; it does not authorize a file for public serving. `hash()` defaults to MD5 for file comparison, not password protection.\n\n## Directories\n\n`makeDirectory($path, $mode, $recursive)` creates a directory. `ensureDirectoryExists()` and `ensureDirectoryWritable()` prepare a location. Use a controlled mode and correct ownership rather than broad permissions.\n\n`files()` lists immediate files; `allFiles()` recurses; `directories()` lists directories. Hidden files can be included explicitly. `glob()` matches patterns, and `directorySize()` totals contents.\n\n```php\nforeach (fm()->allFiles(storage_dir('reports')) as $file) {\n    // Inspect known application files.\n}\n```\n\n## Copy, move, and clean directories\n\n`copyDirectory()` and `moveDirectory()` operate recursively. `deleteDirectory()` removes a directory; its `preserve` option retains the directory itself. `cleanDirectory()` removes contents. Confirm the resolved directory before invoking a recursive deletion.\n\nFilesystem helpers perform operations; they do not establish which user owns a file. Derive paths from trusted application records and a known base directory.\n\n## Links and display\n\n`link()`, `isLink()`, and `realPath()` support symbolic links and canonical paths. Account for symlinks when enforcing a storage boundary. `humanFileSize($bytes, $decimals)` formats a byte count for display.\n\nFor incoming browser files, use [File uploads](./uploads.md). For sending file responses, use [Responses](./response.md#files-and-downloads).\n";export{e as default};
