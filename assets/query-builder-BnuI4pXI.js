const e="## Start a query\n\n```php\n$users = query('users')\n    ->select(['id', 'name', 'email'])\n    ->where('active', true)\n    ->orderDesc('id')\n    ->take(20)\n    ->all();\n```\n\n`query()` creates a `Spark\\Database\\QueryBuilder`. Models forward supported query methods to a builder configured for that model. Builders are mutable; use `copy()` or `clone()` before deriving independent queries from a shared base.\n\n## Tables and columns\n\n`table()` selects the table; `from()` can also set an alias. `as()` sets an alias, and `prefix()` applies a table prefix. `select()` accepts an array, a string, or multiple column arguments. Arrays make a dynamic selection straightforward; `selectRaw()` handles SQL expressions.\n\n```php\n$rows = query('users', 'u')\n    ->select(['u.id', 'u.name'])\n    ->where('u.active', true)\n    ->all();\n```\n\n`column()` selects one column. `value()` fetches a scalar; `pluck('name', 'id')` returns an array optionally keyed by another column. `selectRaw($sql, $bindings)` is available for expressions. Keep raw expressions application-controlled.\n\n## Read results\n\n| Method | Result |\n| --- | --- |\n| `all()` | Array of rows or model objects |\n| `get()` | `Spark\\Support\\Collection` |\n| `first()` | First matching row, or `false` |\n| `firstOrFail()` | First row, or a not-found exception |\n| `last('id')` | First row after descending ordering on the supplied column |\n| `value('email')` | One column value |\n| `pluck('name', 'id')` | Array of values |\n| `count()` | Integer row count |\n| `exists()`, `doesntExist()` | Existence checks |\n| `paginate(20)` | Paginator |\n\n`latest('created_at')` and `oldest('created_at')` **configure ordering and return a builder**. Finish with `all()`, `first()`, or another retrieval method.\n\n```php\n$latest = query('posts')->latest()->take(10)->get();\n```\n\nFor custom hydration, use `fetch()`, `fetchAssoc()`, `fetchColumn()`, or `fetchClass()`. Model queries hydrate the model class. `addMapper($callback)` receives the **entire result array** and returns the transformed array; do not use a mapper when a database aggregate can avoid loading rows altogether.\n\n## Basic conditions\n\n```php\n$query = query('posts')\n    ->where('published', true)\n    ->where('views', '>=', 100)\n    ->whereIn('category_id', [2, 3])\n    ->whereNotNull('published_at');\n```\n\nBindings preserve `0`, `false`, an empty string, an empty JSON array, and `null` as distinct values. Empty condition groups add no predicate.\n\n`where('column', $value)` implies equality. `where()` also accepts an associative array or a closure. Use `orWhere()`, `notWhere()`, and `orNotWhere()` for other boolean forms.\n\nGroup OR conditions so they do not bypass another filter:\n\n```php\n$posts = query('posts')\n    ->where('published', true)\n    ->grouped(function ($query) {\n        $query->where('category_id', 2)->orWhere('category_id', 3);\n    })\n    ->all();\n```\n\n## Condition families\n\n| Need | Methods |\n| --- | --- |\n| Null | `whereNull`, `whereNotNull`, `orWhereNull`, `orWhereNotNull` |\n| Membership | `whereIn`, `whereNotIn`, `orWhereIn`, `orWhereNotIn` |\n| Range | `between('score', 10, 20)`, `whereBetween('score', [10, 20])` and negative/OR variants |\n| Pattern | `like('title', '%Spark%')`, `notLike`, `orLike`, `orNotLike` |\n| Text helpers | `whereContains`, `whereStartsWith`, `whereEndsWith` and available negative/OR variants |\n| Date parts | `whereDate`, `whereYear`, `whereMonth` and their OR variants |\n| Comma-separated set | `findInSet`, `notFindInSet`, `orFindInSet`, `orNotFindInSet` |\n| JSON field | `findInJson`, `whereJsonContains`, and their negative/OR variants |\n| Raw expression | `whereRaw`, `orWhereRaw`, `bind` |\n\n```php\n$rows = query('posts')\n    ->whereDate('published_at', '>=', '2026-01-01')\n    ->whereBetween('views', [100, 1000])\n    ->all();\n```\n\nThe JSON helper accepts **field, key, value**. It is not Laravel's two-argument JSON API. Confirm behavior against your database's JSON support before relying on portable results. A normalized related table is usually preferable to comma-separated database fields.\n\n## Joins\n\n```php\n$posts = query('posts')\n    ->select(['posts.id', 'posts.title', 'users.name'])\n    ->leftJoin('users', 'posts.user_id', '=', 'users.id')\n    ->all();\n```\n\nAvailable join builders include `join`, `innerJoin`, `leftJoin`, `rightJoin`, `fullOuterJoin`, `leftOuterJoin`, and `rightOuterJoin`. Use `on()`, `andOn()`, and `orOn()` for further join conditions, or `joinRaw()` for supported custom SQL. Availability of a join type depends on the database engine and version.\n\n## Sorting, groups, and aggregates\n\n```php\n$total = query('orders')->where('status', 'paid')->sum('total');\n$average = query('orders')->avg('total');\n```\n\n`max`, `min`, `sum`, and `avg` declare a float return. SQL can return null for an empty aggregate; the current implementation does not normalize every empty result, so do not assume it returns zero. Use a deliberate `COALESCE` expression when that is your desired result. `orderBy($field, 'DESC')`, `orderAsc()`, `orderDesc()`, and `orderByRaw()` configure ordering. `random()` applies random ordering.\n\n`groupBy()` accepts a string or array. `groupByRaw()` supports expressions; `having()` accepts a SQL string. Use trusted SQL and bind values through the builder when needed. `distinct($column)` selects distinct values, and `union($query, all: true)` combines compatible queries.\n\n## Limits and pagination\n\n`take(20)->skip(40)` and `limit(40, 20)` mean an offset of 40 and a count of 20. A single `limit(20)` sets only the count. `offset()` sets an explicit offset. Always use a stable order for paginated results.\n\n```php\n$posts = query('posts')->orderDesc('id')->paginate(20);\n```\n\nSee [Pagination](./pagination.md) for rendering links and serializing metadata.\n\n## Insert rows\n\n```php\n$id = query('posts')->insert([\n    'title' => 'A new post',\n    'body' => 'Written with Spark.',\n]);\n```\n\n`insert()` accepts one associative row or rows supported by the driver wrapper, and an optional configuration array. `insertOrIgnore()`, `insertOrReplace()`, and `upsert()` are also available; their conflict behavior depends on the SQL engine and constraints. These insert declarations return an integer; do not rely on older examples promising an array of returned rows.\n\nUse a database unique index for conflict keys. If portability matters, test conflict updates on every supported driver rather than assuming MySQL and PostgreSQL syntax are interchangeable.\n\n## Update and delete\n\n```php\nquery('posts')->where('id', $id)->update(['title' => 'Updated title']);\nquery('posts')->where('id', $id)->increment('views');\nquery('posts')->where('id', $id)->delete();\n```\n\n`update()` and `delete()` return affected row counts. `increment()` and `decrement()` return booleans. `updateOrInsert($attributes, $values)` and `firstOrCreate()` handle common lookup/write patterns; use unique constraints when concurrent requests can insert the same key.\n\n`update()` and `delete()` require a WHERE or an explicitly selected trash scope on a soft-delete model; otherwise they return zero. `increment()` / `decrement()` do not have this guard and can affect every row in their scope. `truncate()` physically empties the table. The guard is not a substitute for an ID, ownership, or tenant condition.\n\n## Raw SQL and debugging\n\n`raw($sql, $bindings)` executes SQL and returns rows. `toSql()` inspects the select SQL; `getBindings()` and `getParameters()` expose parameter state. `getDB()` returns the PDO wrapper.\n\nThe [API reference](./api-reference.md) lists exact signatures in `QueryBuilder`, `BuildsReadQueries`, `BuildsWriteQueries`, and `BuildsConditionalClauses`, including all negative and OR variants.\n\n\n## Upserts and lookup helpers\n\n`upsert(array|Arrayable $data, ?array $conflict = null, ?array $update = null): int` inserts rows and updates existing rows when a unique key conflicts. Pass the conflict columns and update columns as **separate arguments**:\n\n```php\nquery('products')->upsert(\n    [\n        ['sku' => 'SPARK-01', 'name' => 'Starter', 'price' => 25],\n        ['sku' => 'SPARK-02', 'name' => 'Team', 'price' => 50],\n    ],\n    ['sku'],\n    ['name', 'price'],\n);\n```\n\nCreate a unique constraint on `sku` first. You can also pass a single associative row and use named arguments:\n\n```php\nquery('products')->upsert(\n    ['sku' => 'SPARK-01', 'name' => 'Starter', 'price' => 30],\n    conflict: ['sku'],\n    update: ['price'],\n);\n```\n\nIf the SKU already exists, this updates only `price`, preserving its stored `name`. If it does not exist, all supplied fields are inserted.\n\n| Argument | Behavior |\n| --- | --- |\n| `data` | A single associative row, a list of rows, or an `Arrayable` value |\n| `conflict` | Columns identifying the unique key; omitted or null defaults to `['id']` |\n| `update` | Columns to update from the incoming row; omitted, null, or `[]` defaults to all supplied columns except the conflict columns |\n\nFor example, `query('products')->upsert($rows, ['sku'])` updates every supplied non-`sku` column on a conflict. Pass a nonempty update list to restrict those changes. An empty update array does **not** mean “ignore duplicates.” For a composite key, pass all columns, such as `conflict: ['tenant_id', 'sku']`, and create the matching composite unique constraint.\n\nUpdate mappings can also use `['destination_column' => 'incoming_column']`; the values refer to incoming column names, not literal values or SQL expressions. MySQL detects conflicts using its actual primary/unique indexes; PostgreSQL and SQLite use the configured conflict target.\n\nWhen upgrading older calls, replace `upsert($rows, ['conflict' => ['sku'], 'update' => ['price']])` with `upsert($rows, ['sku'], ['price'])`. The configuration-array form remains available on `insert($data, $config)`; it is no longer the second argument to `upsert()`. Model calls such as `Product::upsert($rows, ['sku'], ['price'])` forward to this same query-builder method.\n\n`insert()` and these variants return an integer ID, not an affected-row count or a list of inserted IDs. MySQL/SQLite use the connection's last insert ID; PostgreSQL uses `RETURNING` and reads the model's primary key (or `id` for plain builders). An ignored insert returns zero. PostgreSQL inserts without a numeric primary key also return zero even when the write succeeds. Bulk inserts should contain compatible columns in every row.\n\n`insertOrReplace()` uses MySQL `REPLACE` or SQLite `INSERT OR REPLACE`. Replacement can delete an existing row before inserting, affecting defaults and foreign-key actions. PostgreSQL raises `QueryBuilderException`; use `upsert()` with an explicit unique conflict target instead. If an upsert supplies only conflict columns, the conflict branch leaves the existing row unchanged.\n\n`firstOrCreate($attributes, $values)` performs a lookup then an insert. On a plain table builder, an existing result is normally an object while a new result is an array; a model-backed query hydrates models. Prefer `Model::firstOrCreate()` when callers need a consistent model result. `updateOrInsert()` also performs multiple steps; use unique constraints and handle conflicts rather than treating either helper as an atomic upsert.\n\n## Model binding and soft deletes\n\n`fetchModel(Post::class)` controls row hydration. `useModel($post)` attaches model behavior to the builder; these are separate settings. Automatic soft-delete predicates and writes require an attached model with soft deletes enabled.\n\nModel queries attach their model automatically. Follow [Soft deletes](./soft-deletes.md) for `withTrashed()`, `onlyTrashed()`, `withoutTrashed()`, restoration, and permanent deletion. The last scope selected wins; `withTrashed(false)` selects active rows. These scopes apply to reads, updates, and deletion.\n\n`count()` and relationship counts use the same prepared scope as ordinary model reads. Relationship subqueries created by `has()` / `whereHas()` and `withCount()` / other aggregates now do too. Set archive overrides inside their related-query callbacks; an outer `withTrashed()` affects only the parent model. See [related archive scopes](./relationships.md#soft-deleted-related-records) for examples and join boundaries.\n\n`Post::onlyTrashed()->forceDelete()` permanently removes archived rows; `Post::withTrashed()->forceDelete()` removes all rows. Explicit scopes satisfy the model's bulk-write guard even without another WHERE. Force deletion with no explicit scope bypasses the implicit active-only filter, so `Post::where('id', $id)->forceDelete()` can remove an archived ID. A completely unfiltered `Post::query()->forceDelete()` still returns zero.\n\nAutomatic model deletion predicates use the read alias from `as('p')` / `table('posts', 'p')` / `from('posts', 'p')`, or the actual table name when unaliased. Custom columns and table prefixes are quoted using the connection's driver. Qualify your own ambiguous join columns separately. Write statements do not emit read aliases; use unaliased column names in write conditions.\n\nThrough relations also scope a soft-deletable intermediate model. `withTrashedParents()` includes archived intermediates, independently of `withTrashed()` on the final model; see [Relationships](./relationships.md#through-models).\n\nTable builders such as `query('posts')` and raw SQL have no automatic archive filtering. Trash switches on plain table builders or non-soft-delete models do not authorize an unfiltered write.\n\n## Query state and result transforms\n\nUse a fresh builder for each independent operation, or copy a base before executing it:\n\n```php\n$base = query('posts')->where('published', true);\n$count = $base->copy()->count();\n$recent = $base->copy()->orderDesc('id')->take(5)->all();\n\n$titles = query('posts')->select(['id', 'title'])\n    ->addMapper(fn(array $rows) => array_map(fn($row) => $row->title, $rows))\n    ->all();\n```\n\nRetrieval methods such as `all()`, `first()`, `get()`, and `paginate()` reset query state; mapper callbacks are consumed during retrieval. `count()`, `exists()`, `value()`, and `pluck()` retain the query. Qualified names and aliases work with `value()` / `pluck()`; model retrieval preserves attribute casts. Writes clear conditions and bindings but do not necessarily reset every other setting. Do not reuse a builder assuming its previous filters still exist.\n\nUnknown methods may forward to `get()` and then a Collection method. That loads results into memory; it is not a cursor or database batch iterator. Use ordered, bounded queries for large datasets. A model's `scope<Name>()` method is resolved before this Collection fallback.\n\n## SQL and driver details\n\n- Repeated `orderBy()` calls replace the stored order. For multiple sort columns, use an application-controlled `orderByRaw('created_at DESC, id DESC')`.\n- `count()` counts result rows, including groups, distinct selections, and unions, ignoring the current limit/offset. Pagination uses that same total on all drivers. Give selected expressions unique aliases when counting or paginating joined projections.\n- JSON helpers perform text matching at a path: MySQL/SQLite use JSON extraction, and PostgreSQL uses `jsonb` text extraction. They are not structural JSON containment. Dot-separated object paths such as `profile.name` are portable; use driver-specific SQL for advanced array/path syntax. SQL LIKE wildcards in the value retain their normal meaning.\n- `whereDate()`, `whereYear()`, and `whereMonth()` extract the requested part on all three drivers. Pass a numeric year or month, such as `2026` or `9`.\n- Comma-separated set helpers match complete tokens, so searching for `2` does not match `20`. MySQL uses `FIND_IN_SET`, PostgreSQL uses array membership, and SQLite uses delimiter-aware matching. Prefer normalized tables for indexed relationships.\n- `union()` keeps each child query's bindings separate. Ordering and pagination on the outer builder apply to the combined result; a child builder retains its own order/limit. Use a consistent parameter style throughout the combined query. Prefer distinct named placeholders for complex SELECT/WHERE/ORDER fragments and relationship aggregates; ambiguous positional aggregate composition raises `QueryBuilderException`.\n\nBind **values**, and select identifiers, sort directions, operators, and raw expressions from application-controlled allowlists. A parameter cannot substitute for a table/column identifier.\n\n```php\n$rows = query('orders')->raw(\n    'SELECT status, COALESCE(SUM(total), 0) AS total FROM orders WHERE user_id = :user GROUP BY status',\n    ['user' => $userId],\n);\n```\n\n`raw()` returns associative rows. A raw statement may use named or positional parameters; do not mix the two styles. When adding a raw clause to builder-generated conditions, prefer named parameters with unique names, since the builder generates named bindings itself. `toSql()` inspects a SELECT; it does not preview every write operation.\n";export{e as default};
