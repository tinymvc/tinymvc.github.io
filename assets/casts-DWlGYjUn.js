const e="## Declare casts\n\nCasts transform values between database storage and PHP attributes:\n\n```php\nprotected array $casts = [\n    'published' => 'boolean',\n    'views' => 'integer',\n    'price' => 'decimal:2',\n    'meta' => 'array',\n    'published_at' => 'datetime',\n];\n```\n\n| Cast | Use |\n| --- | --- |\n| `int`, `integer` | Integer value |\n| `float`, `double`, `real` | Floating-point value |\n| `decimal:2` | Formatted decimal string with a chosen precision |\n| `string` | Text |\n| `bool`, `boolean` | Boolean |\n| `array`, `json` | Structured array data |\n| `object` | Structured object data |\n| `collection` | Framework collection |\n| `date`, `datetime`, `timestamp` | `Spark\\Carbon` values, including the `timestamp` cast |\n| `encrypted` | Encrypt for storage, decrypt on access |\n| `hashed` | One-way password-style hashing |\n| Cast class name | Custom get/set transformation |\n\nChoose database column types and lengths that match stored representations. Floating-point arithmetic is not a substitute for a deliberate money representation. Encrypted fields require the same application key to be readable later.\n\n## Accessors and mutators\n\nUse the classic `get<Name>Attribute()` and `set<Name>Attribute()` methods:\n\n```php\nuse Spark\\Database\\Model;\n\nclass User extends Model\n{\n    public function getNameAttribute($value): string\n    {\n        return trim((string) $value);\n    }\n\n    public function setNameAttribute($value): string\n    {\n        return trim((string) $value);\n    }\n}\n```\n\nThe getter controls the PHP-facing value. The setter returns the normalized storage value when the model prepares data for saving; assigning a property alone does not run this mutator. Use `$appends` to include a computed accessor in serialized output, and `$hidden` to omit private fields.\n\n`Spark\\Database\\Casts\\Attribute::make(get: ..., set: ...)` exists and requires both closures. However, the current model dispatch reads `<name>Attribute` as a property instead of invoking the method. Use the classic methods above or a custom cast until that dispatch path is corrected.\n\n## Custom casts\n\nGenerate a class with `php spark make:cast TrimmedText`. Implement `Spark\\Database\\Contracts\\CastsAttributes`, whose methods are `get($value)` and `set($value)`:\n\n```php\n<?php\n\nnamespace App\\Casts;\n\nuse Spark\\Database\\Contracts\\CastsAttributes;\n\nclass TrimmedText implements CastsAttributes\n{\n    public function get($value)\n    {\n        return trim((string) $value);\n    }\n\n    public function set($value)\n    {\n        return trim((string) $value);\n    }\n}\n```\n\nAssign `TrimmedText::class` in the model's `$casts` array. Do not use a different framework's cast interface signature.\n\n## Dirty tracking\n\n```php\n$post = Post::findOrFail($id);\n$before = $post->getOriginal('title');\n$post->title = 'Revised title';\n\nif ($post->isDirty('title')) {\n    $changes = $post->getChanges();\n    $post->save();\n}\n```\n\n`getChanges()` contains the **original values** of changed attributes, not their new values. Read the current attributes to build an old/new audit pair. `isClean()` is the inverse dirty check. `hasChanges()` checks pending changes, and nested tracking methods help inspect structured attributes. `restoreOriginal()` discards local changes; `refresh()` loads current database state. Keep copies of original values before saving when they are needed in an audit record.\n\n## Write paths matter\n\nInstance `fill()`/`save()` uses model behavior. Raw SQL and direct query-builder updates do not run the same model attribute lifecycle. Apply the intended transformations explicitly when using those paths, and test both storage and retrieval for custom casts.\n\n\n## Storage formats and null values\n\nNull passes through the cast layer. Array, object, and collection casts serialize structured data as JSON; an incoming string is treated as already encoded, so validate external JSON before assigning it. Date casts store a date/time string in `Y-m-d H:i:s` format. The `timestamp` cast produces a date object rather than a Unix integer.\n\nCustom cast classes are instantiated without constructor arguments and receive only the value in `get()` / `set()`. Keep them independent of an assumed model/key/options signature. A storage mutator takes precedence over the storage cast. Loaded attributes are cast during hydration; an accessor then controls the value returned on access, so it may receive an already-cast value.\n\n`hashed` leaves an already recognized hash intact. It is one-way: reading the field does not recover the original value. `encrypted` requires a stable application key; failed decryption currently yields null. Avoid logging sensitive cast input or output.\n\nFor a deletion timestamp, declare `'deleted_at' => 'datetime'` (or the custom column) to obtain a date object. Follow [Soft deletes](./soft-deletes.md) for the separate model configuration; a cast alone does not enable archival.\n\n## Nested attributes and change baselines\n\nFor array data, read, modify, and assign the value back so the model sees the change:\n\n```php\n$settings = $user->settings ?? [];\n$settings['notifications']['email'] = true;\n$user->settings = $settings;\n$user->save();\n```\n\nThe model also exposes dotted-key `get()` / `set()` and nested original/change inspection. Dirty tracking compares a loaded baseline with the current values; an unsaved instance does not have the same persisted baseline. `clearOriginal()` removes that baseline and `clearTracks()` resets operation tracking. Use these deliberately rather than as substitutes for saving or reloading a model.\n";export{e as default};
