const e="## Configure a connection\n\nTinyMVC uses PDO. The current skeleton defaults to SQLite and reads `config/database.php`:\n\n```php\nreturn [\n    'driver' => env('DB_CONNECTION', 'sqlite'),\n    'connections' => [\n        'sqlite' => [\n            'file' => dirname(__DIR__) . '/database/sqlite.db',\n        ],\n        'default' => [\n            'host' => env('DB_HOST', '127.0.0.1'),\n            'port' => env('DB_PORT', '3306'),\n            'name' => env('DB_DATABASE', 'spark'),\n            'username' => env('DB_USERNAME', 'root'),\n            'password' => env('DB_PASSWORD', ''),\n        ],\n    ],\n];\n```\n\nUse `sqlite`, `mysql`, or `pgsql` for the driver. Set the matching PDO extension, database credentials, and port; PostgreSQL commonly uses port 5432. The database user key is `username`. Clear the configuration cache after changes.\n\nSQLite requires a writable database file and parent directory. For application tests, use `file => ':memory:'` in isolated test configuration.\n\n## Resolve the database service\n\n```php\n$db = database(); // db() is an alias\n$pdo = $db->getPdo();\n$users = query('users')->where('active', true)->all();\n```\n\n`Spark\\Database\\DB` wraps PDO. `query('users')` returns a query builder. The connection is resolved when needed, and `getDriver()`, `isMySQL()`, `isSQLite()`, and `isPostgreSQL()` let an integration inspect its driver. `getConfig($key, $default = null)` reads a value out of the active connection's configuration array.\n\nThe `Spark\\Facades\\DB` facade exposes the same wrapper statically, plus two entry points straight into the query builder without resolving the connection yourself first:\n\n```php\nuse Spark\\Facades\\DB;\n\n$reports = DB::table('reports', 'r')->where('r.active', true)->all();\n$rows = DB::select('id, name')->from('users')->all();\n```\n\n`DB::table($table, $alias = null)` and `DB::select($fields = '*', ...$args)` both return a `QueryBuilder` instance. The facade's first `select()` argument is typed as a string; for an array, start with `DB::table('users')->select(['id', 'name'])`.\n\n## Additional connections\n\nDefine named connection arrays under `database.connections`, including an explicit driver when needed. Resolve a separate connection with `Spark\\Database\\DB::connection('reporting')` or pass a complete array:\n\n```php\nuse Spark\\Database\\DB;\n\n$reporting = DB::connection([\n    'driver' => 'sqlite',\n    'file' => storage_dir('reporting.db'),\n]);\n$rows = $reporting->table('reports')->all();\n```\n\n`connect_db($config)` returns a **query builder** for a new connection. Use `getDB()` to access its wrapper. `reset($config)` and `resetPdo()` are available for integrations that deliberately replace a connection's configuration or force a fresh PDO instance; do not reset an active transaction.\n\n## Prepared SQL\n\nUse query builder conditions for routine SQL. At the PDO level, bind values:\n\n```php\n$statement = db()->prepare('SELECT id, name FROM users WHERE email = :email');\n$statement->execute(['email' => $email]);\n$user = $statement->fetch(PDO::FETCH_ASSOC);\n```\n\n`statement($sql, $params)` executes a prepared statement and returns a boolean. `exec()` is for SQL without bound values; `query()` exposes PDO query execution. `lastInsertId()` and other PDO methods are delegated by the wrapper.\n\nPlaceholders bind values, not table/column names. Choose identifiers from application-defined allowlists. When a value genuinely can't go through a placeholder (rare — prefer bound parameters whenever possible), `quote($string, $type = PDO::PARAM_STR)` escapes it for the active driver.\n\n## Transactions\n\n```php\n$db = db();\n$db->beginTransaction();\n\ntry {\n    $db->table('accounts')->where('id', $fromId)->decrement('balance', 10);\n    $db->table('accounts')->where('id', $toId)->increment('balance', 10);\n    $db->commit();\n} catch (Throwable $error) {\n    if ($db->inTransaction()) {\n        $db->rollBack();\n    }\n    throw $error;\n}\n```\n\nUse the same connection for every statement in the transaction. Business rules, locking, and sufficient-balance checks must be designed for your domain; this example demonstrates transaction control only. The wrapper exposes PDO's `beginTransaction()`, `commit()`, `rollBack()`, and `inTransaction()`.\n\nFor the common begin/commit/rollback-and-rethrow pattern, `DB::transaction(callable $callback)` wraps it for you:\n\n```php\nuse Spark\\Facades\\DB;\n\n$result = DB::transaction(function () use ($fromId, $toId) {\n    DB::table('accounts')->where('id', $fromId)->decrement('balance', 10);\n    DB::table('accounts')->where('id', $toId)->increment('balance', 10);\n\n    return true;\n});\n```\n\nThe callback's return value is passed back to the caller; any thrown exception triggers a rollback and is rethrown.\n\n## Portability and diagnostics\n\nSchema grammar and query wrappers adapt common operations for the supported drivers. Database-specific SQL, JSON operations, joins, and indexes still need testing on the database used in production. Inspect `toSql()` and `getBindings()` on a builder when debugging; avoid logging secrets in bound values.\n\nContinue with the [Query builder](./query-builder.md) and [Migrations](./migrations.md).\n\n## Connection configuration in detail\n\nThe wrapper resolves nested configuration by merging the selected connection over the top-level options. `default_connection` selects a named entry; otherwise it uses the driver-named entry when present, or `connections.default`. A named `DB::connection('reporting')` reads that entry directly, so give independently selected entries their own `driver` and options.\n\n| Key | Meaning |\n| --- | --- |\n| `driver` | `sqlite`, `mysql`, or `pgsql`; the wrapper fallback is MySQL, while the skeleton chooses SQLite |\n| `database`, `file`, `path` | SQLite filename; `file`/`path` are normalized to `database` |\n| `name`, `database` | MySQL/PostgreSQL database name |\n| `username`, `user` | Username; `username` is normalized to `user` |\n| `host`, `port`, `password` | Network credentials |\n| `dsn` | Explicit PDO DSN instead of the generated DSN |\n| `options` | PDO constructor options; exception error mode is enforced |\n| `charset`, `collation`, `timezone` | MySQL connection initialization settings |\n\n```php\nuse Spark\\Database\\DB as Connection;\n\n$reporting = Connection::connection([\n    'driver' => 'pgsql',\n    'dsn' => 'pgsql:host=127.0.0.1;port=5432;dbname=reporting',\n    'username' => env('REPORTING_USER'),\n    'password' => env('REPORTING_PASSWORD'),\n    'options' => [PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC],\n]);\n```\n\nAn explicit DSN is useful for driver-specific connection options. SQLite connections enable foreign keys and create a missing parent directory for a configured file. In-memory SQLite is local to its PDO connection: creating or resetting the connection creates another database.\n\n## Connection ownership and transactions\n\n`Spark\\Facades\\DB::connection()` without arguments returns the application connection. `Spark\\Database\\DB::connection()` creates a new wrapper. Model queries resolve the application's query builder; opening a reporting connection does not automatically move models or migrations to it. Run reporting queries through `$reporting->table(...)`, or deliberately use a builder's `setDB()` before executing it.\n\n`DB::transaction()` uses the application connection and a zero-argument callback. The outer call begins and commits a transaction; nested calls use savepoints. A failed inner call rolls back its own work and rethrows the original exception, allowing the caller to catch it while preserving the outer transaction. There are no automatic retries. Use the explicit transaction pattern above for a separate connection. Do not commit, roll back, reconnect, or run implicitly committing DDL inside the callback.\n\nA missing named connection raises `InvalidDatabaseConfigException`; it does not silently fall back to the default database.\n\n`Schema` follows the application connection when it is replaced and refreshes its grammar. Creating a separate reporting wrapper still does not move migrations to it.\n\n`reset($config)` replaces configuration and drops the cached PDO; `resetPdo()` immediately reconnects. The facade's historical `resetConfig` annotation is not an implemented wrapper method. Do not reconnect halfway through a transaction.\n\n## Query diagnostics and extensions\n\nIn debug mode, wrapper/builder queries emit `app:db.queryExecuted` with SQL, elapsed milliseconds, and starting memory; builder events can also include bindings. Direct calls made on the returned PDO are outside that wrapper logging. Store only suitable diagnostics and avoid recording sensitive values.\n\nDB, query builders, and schema helpers support `macro()` extensions. Register application macros once in a service provider, and keep their contracts with your application documentation. Exact contracts and exception classes are available in the [API reference](./api-reference.md).\n\nFor model-specific archive behavior, see [Soft deletes](./soft-deletes.md). Raw connection queries never infer model scopes from a table name.\n\n\n## Common database exceptions\n\n| Situation | Exception or result |\n| --- | --- |\n| Invalid connection configuration | `Spark\\Database\\Exceptions\\InvalidDatabaseConfigException` |\n| PDO connection, SQL, or constraint failure | Usually `PDOException`; the wrapper enforces exception mode |\n| Invalid query / condition construction | `QueryBuilderException` or `QueryBuilderInvalidWhereClauseException` |\n| Invalid model or relation configuration | `InvalidOrmException` / `UndefinedOrmException` |\n| Accessing a relation with lazy loading disabled | `OrmDisabledLazyLoadingException` |\n| Invalid/unwritable migration history | `InvalidMigrationFile` |\n| Invalid schema argument, foreign key, driver, or SQLite alteration | Corresponding exceptions under `Spark\\Database\\Schema\\Exceptions` |\n\nA read with no matching row is different from a database error: `first()` / `find()` return false, while their `OrFail` variants throw a not-found exception. A guarded write without conditions can return zero/false without executing SQL. Handle these outcomes separately in application code, and roll back an open transaction before propagating an error.\n";export{e as default};
