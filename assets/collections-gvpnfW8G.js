const e="## Create a collection\n\n```php\n$names = collect([\n    ['name' => 'Ada', 'active' => true],\n    ['name' => 'Grace', 'active' => false],\n])->filter(fn($user) => $user['active'])\n  ->map(fn($user) => $user['name'])\n  ->values()\n  ->all();\n```\n\n`collect()` creates `Spark\\Support\\Collection`. Database `get()` returns a collection, while `all()` returns an array. Convert deliberately at API boundaries.\n\n## Common operations\n\n| Task | Methods |\n| --- | --- |\n| Transform | `map`, `mapWithKeys`, `mapToDictionary`, `flatMap` |\n| Select | `filter`, `reject`, `where`, `whereIn`, `first`, `last` |\n| Shape | `pluck`, `keys`, `values`, `only`, `except`, `flatten`, `collapse` |\n| Group | `groupBy`, `keyBy`, `chunk`, `partition` |\n| Order | `sort`, `sortBy`, `sortByDesc`, `reverse`, `shuffle` |\n| Combine | `merge`, `mergeRecursive`, `concat`, `union`, `zip` |\n| Aggregate | `count`, `sum`, `avg`, `min`, `max`, `reduce` |\n| Inspect | `contains`, `every`, `isEmpty`, `isNotEmpty`, `each` |\n| Convert | `all`, `toArray`, `toJson` |\n\nMany transformations preserve original keys; call `values()` when a JSON client expects a sequential array. Some methods mutate the collection while others return a new collection—check the reference for the method you use instead of assuming every chain is immutable.\n\n## Lazy collections\n\n`Spark\\Support\\LazyCollection` works with iterables/generators so supported operations can defer evaluation:\n\n```php\nuse Spark\\Support\\LazyCollection;\n\n$numbers = new LazyCollection(function () {\n    for ($i = 1; $i <= 1000; $i++) {\n        yield $i;\n    }\n});\n\n$total = $numbers->filter(fn($value) => $value % 2 === 0)->sum();\n```\n\nA lazy pipeline helps only if the source and operations remain lazy. Sorting or converting the entire sequence to an array materializes it. Close file/database resources in the generator's cleanup path when using external sources.\n\n## Nested arrays\n\n`Spark\\Support\\Arr` provides static array operations. Global helpers support dot paths:\n\n```php\n$data = ['profile' => ['name' => 'Ada']];\n$name = data_get($data, 'profile.name', 'Guest');\ndata_set($data, 'profile.locale', 'en');\ndata_forget($data, 'profile.name');\n```\n\n`data_fill()` sets a path only when it is missing. `Arr::get()`, `set()`, `has()`, `only()`, `except()`, `pluck()`, and `wrap()` support common array transformations. Be explicit about whether a helper mutates by reference or returns a transformed array.\n\n## Choosing where to transform\n\nFilter, aggregate, and paginate in the database when that avoids loading unnecessary rows. Use collections for already-loaded application data and presentation transformations. The [API reference](./api-reference.md) includes the full Collection, LazyCollection, Arr, and support-trait APIs, including less common grouping and comparison operations.\n\n## Keys, mutations, and repeatable lazy sources\n\n`map()` / `filter()` return transformed collections; `push()`, `put()`, `forget()`, and `transform()` modify the current Collection. Copy or rebuild the input when another consumer must keep its original values. A shallow copy still contains the same nested objects.\n\nUse a generator **closure** for a LazyCollection, as above. Passing an already-created Generator directly is rejected. A repeatable source closure can produce a fresh iteration; a captured stream that is already consumed cannot. `all()` materializes the sequence, so do that only after limiting or filtering when possible.\n\n```php\n$byId = collect([\n    ['id' => 12, 'name' => 'Ada'],\n    ['id' => 18, 'name' => 'Grace'],\n])->keyBy('id');\n\n$names = $byId->pluck('name')->values()->all();\n```\n\nUse keys intentionally: `keyBy()` keeps one item per key, so duplicate IDs replace an earlier item. Use `groupBy()` when a key can legitimately have several items.\n";export{e as default};
