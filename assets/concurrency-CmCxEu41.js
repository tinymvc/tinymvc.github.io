const e="## Run independent tasks\n\n```php\nuse Spark\\Concurrency;\n\n$results = Concurrency::run([\n    'first' => fn() => 2 + 2,\n    'second' => fn() => strtoupper('spark'),\n]);\n```\n\nThe utility preserves task results and provides a limit on concurrent work. `runWithLimit($tasks, $maxProcesses)` sets the limit for a batch. An instance supports `add($task, $key)`, `limit()`, `execute()`, and `wait()`.\n\n## Runtime behavior\n\nParallel execution requires a **thread-safe PHP build (ZTS)** and the **parallel extension**. Without them, the implementation executes tasks sequentially. Do not advertise or depend on a speedup without measuring the deployed runtime.\n\nParallel runtimes have isolation constraints. Keep tasks self-contained and avoid capturing open PDO connections, request objects, or other resources that cannot safely cross a runtime boundary. For network-only fan-out, consider the [HTTP pool](./http-client.md#request-pools).\n\n## Inspect results\n\n`getResults()`, `getTasks()`, and `getMaxProcesses()` expose state. `clearTasks()`, `clearResults()`, and `reset()` prepare an instance for reuse. A caught task exception becomes `['error' => true, 'message' => ...]` at that task’s key; it is not rethrown to the batch caller. Check those entries before consuming successful values. `wait()` calls `execute()` again—it is not a passive wait on an already-executed result. Use `getResults()` to read a completed batch without rerunning side effects.\n\n## Execute a process\n\n`Spark\\Console\\Process` wraps an external command:\n\n```php\nuse Spark\\Console\\Process;\n\n$process = Process::command([PHP_BINARY, '-v'])\n    ->path(root_dir())\n    ->timeout(10)\n    ->execute();\n\nif ($process->successful()) {\n    $output = $process->output();\n}\n```\n\nPrefer an argument array so arguments do not become shell syntax. Do not build a shell command by concatenating request input.\n\n`env()` adds environment values, `input()` supplies stdin, `tty()` enables terminal mode, and `forever()` removes the timeout. `output()`, `errorOutput()`, `exitCode()`, and `executionTime()` expose the result. `throw()` raises on failure, while `failed()` lets you handle it explicitly.\n\n## Choose the right tool\n\nUse a pipeline for ordered in-process steps, concurrency for independent calculations supported by the runtime, an HTTP pool for independent outbound requests, and a queue for durable background work. Deferred callbacks are useful for small post-response tasks but do not survive a process crash.\n";export{e as default};
