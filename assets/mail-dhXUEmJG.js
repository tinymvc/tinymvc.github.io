const e="## Install mail support\n\n```bash\ncomposer require phpmailer/phpmailer\n```\n\nMail is an optional integration built on PHPMailer. Configure `config/mail.php` with `from`, optional `reply`, and `smtp` arrays. SMTP keys include `enabled`, `host`, `port`, `username`, `password`, and `encryption`; the skeleton maps them to `MAIL_*` environment variables.\n\nUse a development mailbox service while building and real provider credentials in production. Clear cached configuration and restart workers after changing credentials.\n\n## Send a message\n\n```php\n$sent = mailer()\n    ->to('ada@example.com', 'Ada')\n    ->subject('Welcome')\n    ->body('Your account is ready.')\n    ->send();\n```\n\n`send()` returns a boolean: it catches send-time exceptions, logs the failure through `Spark\\Facades\\Log`, and returns false. Configuration or message-building errors can still throw before `send()` is reached. Avoid returning internal SMTP details to users.\n\n## Blade templates\n\n```php\n$sent = mailer()\n    ->to($user->email)\n    ->subject('Your report is ready')\n    ->view('emails.report', ['user' => $user, 'report' => $report])\n    ->send();\n```\n\nCreate the template under `resources/views/emails`. Escape dynamic values in the template. `body($content, isHtml: true)` explicitly sends HTML content when a template is unnecessary.\n\n## Recipients, headers, and attachments\n\nUse `to()`, `cc()`, `bcc()`, `from()`, and `reply()` for addresses. `withHeader()` / `withHeaders()` add custom headers. `attachFile($path, $name)` is the framework’s fluent attachment helper. The class extends PHPMailer, but its inherited methods need not return the wrapper for chaining.\n\nDerive attachment paths from trusted application records and authorize access. Avoid sending a user-controlled filesystem path as an attachment.\n\n## Configure PHPMailer\n\n`configureSmtp()` applies SMTP settings. `configure($callback)` gives access to the PHPMailer instance for settings beyond the wrapper. `mailer($address, $name)` is an alias for `from()`; `smtpKeepAlive()` can help a deliberate batch workflow.\n\nUse a fresh instance or `reset()` before composing another message. `copy()` clones existing state, including recipients and attachments, so copying a previously composed message does not clear them. `reset()` clears recipients, reply-to addresses, attachments, custom headers, subject, and body; reapply your reply-to when needed.\n\n## Queue delivery\n\nDispatch a [class job](./queues.md) with a user or message ID, then load the data in its handler. Record delivery state or use an idempotency strategy if retries could duplicate important messages.\n\nThe framework-specific Mail logging methods from older examples are not part of the current public API. Use your application logger around send attempts and failure hooks.\n";export{e as default};
