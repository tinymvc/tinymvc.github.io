const e="## Paginate a query\n\n```php\n$posts = Post::where('published', true)\n    ->orderDesc('id')\n    ->paginate(20);\n\nreturn view('posts.index', ['posts' => $posts]);\n```\n\n`paginate($limit = 10, $keyword = 'page', $fields = null)` returns `Spark\\Utils\\Paginator`. The page is read from the query string. Use a stable sort and a bounded page size.\n\n## Render a page\n\n```html\n@foreach($posts as $post)\n    <article>\n        <h2>{{ $post->title }}</h2>\n    </article>\n@endforeach\n\n@if($posts->hasLinks())\n    <nav aria-label=\"Pagination\">{!! $posts->links() !!}</nav>\n@endif\n```\n\n`links()` returns generated HTML, so it is rendered unescaped. Do not concatenate untrusted HTML into it. `generateLinks()` returns link data for your own renderer; `links($links, $classes, $entity)` customizes the link window, classes, and entities.\n\n## Metadata and items\n\n| Method | Value |\n| --- | --- |\n| `items()`, `data()` | Current page's items |\n| `total()` | Total number of results |\n| `limit()` | Page size |\n| `page()` | Current page |\n| `pages()` | Number of pages |\n| `offset()` | Starting offset |\n| `firstItem()`, `lastItem()` | Item positions |\n| `keyword()`, `keywordValue()` | Query parameter and its value |\n| `hasData()`, `isEmpty()`, `isNotEmpty()` | Result checks |\n| `hasLinks()` | Whether pagination controls are needed |\n\nThe paginator is iterable and implements array/JSON conversion. Return `json($posts)` or simple `$posts` for an API, or build an explicit response shape using these accessors.\n\n## Multiple paginators\n\nUse different query parameter names:\n\n```php\n$posts = Post::orderDesc('id')->paginate(10, 'posts_page');\n$comments = Comment::orderDesc('id')->paginate(10, 'comments_page');\n```\n\n## Paginate an existing array\n\n```php\nuse Spark\\Utils\\Paginator;\n\n$paginator = new Paginator(total: count($items), limit: 10);\n$paginator->setData($items);\n$currentItems = $paginator->data(slice: true);\n```\n\nOnly use array pagination when the dataset is already reasonably small. Database pagination avoids loading the entire result set.\n\n## Transform results\n\n`map`, `filter`, `reduce`, `each`, `pluck`, `sort`, `except`, and `only` operate on the paginator's data. Post-query filtering does not automatically re-run the total-count query; prefer database filters before `paginate()` when total counts must reflect them.\n\n\n## API output and soft-delete scopes\n\n`toArray()` / JSON serialization includes `pages`, `page`, `offset`, `limit`, `first_item`, `last_item`, `total`, `keyword`, `links`, and `data`. Use `generateLinks()` for a frontend-rendered pagination control, or return a smaller explicit metadata object if those link details are unnecessary.\n\n```php\n$posts = Post::onlyTrashed()->orderDesc('id')->paginate(20);\nreturn json([\n    'data' => $posts->items(),\n    'meta' => ['page' => $posts->page(), 'pages' => $posts->pages(), 'total' => $posts->total()],\n]);\n```\n\nThis trash listing assumes the [soft-delete model setup](./soft-deletes.md). Both the total and selected page use the same bound-model read scope. Use a unique tie-breaker for stable ordering, and apply filters before pagination. A grouped count has different semantics in the current query builder; construct an explicit count query for grouped reports rather than assuming `paginate()` counts groups.\n";export{e as default};
