const e='## What FireLine adds\n\n[FireLine](https://github.com/shahinmoyshan/fireline) is an Alpine plugin for dynamic navigation, form submissions, and DOM updates over server-rendered HTML. Keep your PHP routes and Blade templates while adding transitions between pages without a full document reload.\n\n## Install and initialize\n\n```bash\nnpm install fireline\n```\n\nIn the existing Vite entry, register the plugin before the single Alpine startup:\n\n```javascript\nimport Alpine from \'alpinejs\';\nimport FireLine from \'fireline\';\n\nAlpine.plugin(FireLine);\nwindow.Alpine = Alpine;\nAlpine.start();\n```\n\n## Return a FireLine response\n\n```php\nuse Spark\\Facades\\Route;\n\nRoute::get(\'/about\', fn() => fireline(\'pages.about\'));\n```\n\nThe core `fireline()` helper returns a normal Blade response for a normal request. For a request identified as FireLine, it returns JSON containing rendered `html` and the Blade `title` section, with caching disabled. `Route::fireline(\'/about\', \'pages.about\')` provides a shortcut.\n\n## Navigation and forms\n\n```html\n<div id="app">\n    <div>\n        <a x-navigate href="/about">About</a>\n        <form x-submit action="/feedback" method="POST">\n            @csrf\n            <label for="message">Message</label>\n            <input id="message" name="message">\n            <div status="success"></div>\n            <div status="error"></div>\n            <button type="submit">Send</button>\n        </form>\n    </div>\n</div>\n```\n\nThe replacement content needs a single root element. Use full Alpine attributes such as `x-on:click` rather than shorthand in content that FireLine patches. Keep CSRF protection and server validation on forms.\n\n## Settings and events\n\nConfigure `window.FireLine.settings` during Alpine initialization. Settings include `targetEl` (default `#app > div`), `timeout`, `interceptLinks`, and `interceptForms`. A `native` attribute lets a link or form bypass automatic interception.\n\nThe plugin emits `fireStart`, `fireEnd`, and `fireError`. The `$fire` magic property exposes `current`, `loading`, `navigate(url)`, `reload()`, `replaceHtml(html)`, and `formSubmit(formEl)`.\n\n## Form response contract\n\nForm responses can supply `status` and `message` for feedback, `html` and `title` for content replacement, or `redirect` / `navigate` for navigation. Match the response shape to the installed plugin. Test success, validation failure, network failure, and a normal non-JavaScript request.\n\nFor the plugin\'s complete client API, use the [official FireLine README](https://github.com/shahinmoyshan/fireline#readme).\n';export{e as default};
