const e="## Create a migration\n\nMigrations make schema changes reviewable and repeatable. Generate a file in `database/migrations`:\n\n```bash\nphp spark make:migration create_posts_table\n```\n\nA migration returns an anonymous class with `up()` and `down()` methods:\n\n```php\n<?php\n\nuse Spark\\Database\\Schema\\Blueprint;\nuse Spark\\Database\\Schema\\Schema;\n\nreturn new class {\n    public function up(): void\n    {\n        Schema::create('posts', function (Blueprint $table) {\n            $table->id();\n            $table->string('title')->required();\n            $table->text('body')->required();\n            $table->boolean('published')->default(false);\n            $table->timestamps();\n        });\n    }\n\n    public function down(): void\n    {\n        Schema::dropIfExists('posts');\n    }\n};\n```\n\n`up()` applies the change; `down()` reverses it where possible. Schema rollback can destroy data even when it successfully reverses the table structure.\n\n## Generate a pivot table migration\n\nFor a many-to-many relationship, let Spark generate the pivot migration:\n\n```bash\nphp spark make:migration --pivot\n```\n\nThe command asks for both related table names:\n\n```text\nWhat is the name of the first related table? users\nWhat is the name of the second related table? roles\n```\n\nSpark normalizes the names and sorts the plural table names alphabetically, so this example generates a `roles_users` pivot table migration in `database/migrations/migration_<timestamp>_roles_users.php`. The generated `up()` contains:\n\n```php\nSchema::create('roles_users', function (Blueprint $table) {\n    $table->id();\n    $table->foreignId('user_id')->constrained('users')->cascadeOnDelete();\n    $table->foreignId('role_id')->constrained('roles')->cascadeOnDelete();\n});\n```\n\nThe generated `down()` drops `roles_users` with `Schema::dropIfExists()`. The command creates the migration file; run `php spark migrate` to create the table in the database. Ensure the related tables are created before this migration runs.\n\nThe generated table includes its own `id` and both foreign keys with cascading physical deletes. Add any extra pivot attributes, timestamps, or a composite unique constraint before running the migration. For example, `$table->unique(['user_id', 'role_id'])` prevents duplicate associations; it is not added automatically.\n\nThe `-p` flag is an alias for `--pivot`. See [Many-to-many relationships](./relationships.md#many-to-many-and-pivot-data) for defining the relation and attaching, detaching, or synchronizing records.\n\n## Run and roll back\n\n```bash\nphp spark migrate\nphp spark migrate:rollback\nphp spark migrate:rollback --step=2\n```\n\nThe runner tracks applied migration files and skips already applied migrations. `migrate:fresh` rolls back all recorded migrations and reapplies them; use it only with a database whose contents may be discarded.\n\n```bash\nphp spark migrate:fresh\n```\n\nCreate a new migration to change deployed schema instead of editing an already applied migration.\n\n## Seed data\n\n```bash\nphp spark make:seeder InitialCategories\nphp spark migrate --seed\n```\n\nThe migration runner includes seed files only when the seed flag is enabled. Keep seeds repeatable when they may run in more than one environment, and avoid deploying demonstration users or credentials into production.\n\n## Column types\n\n| Family | Blueprint methods |\n| --- | --- |\n| Primary / auto-increment | `id`, `increments`, `bigIncrements`, `smallIncrements`, `tinyIncrements` |\n| Integers | `integer`, `bigInteger`, `mediumInteger`, `smallInteger`, `tinyInteger` and unsigned variants |\n| Text | `string(name, length)`, `char`, `text`, `longText` |\n| Numeric | `decimal(name, precision, scale)`, `double`, `float` |\n| Structured | `boolean`, `enum(name, allowed)`, `json`, `binary` |\n| Date / time | `date`, `dateTime`, `time`, `timestamp`, `nullableTimestamp` |\n| Convenience | `timestamps`, `nullableTimestamps`, `softDeletes`, `rememberToken` |\n| Identifiers | `uuid`, `ipAddress`, `macAddress` |\n\nThe SQL grammar translates types for the configured engine. Adding a `softDeletes()` column creates schema only. Enable `USE_SOFT_DELETES` on the model separately; see [Soft deletes](./soft-deletes.md#enable-the-model).\n\n## Column modifiers\n\n```php\n$table->string('email')->unique();\n$table->text('bio')->nullable();\n$table->boolean('active')->default(true);\n$table->timestamp('published_at')->nullable();\n```\n\nModifiers include `nullable()`, `required()`, `default()`, `unsigned()`, `autoIncrement()`, `after()`, `charset()`, `collation()`, `comment()`, `useCurrent()`, and `useCurrentOnUpdate()`. Some modifiers are database-specific; inspect the generated grammar and test on your target engine.\n\n## Indexes\n\nUse `primary()`, `unique()`, or `index()` with a column or array of columns. Full-text and spatial indexes are exposed through `fullText()` and `spatialIndex()` where the database supports them.\n\n```php\n$table->unique(['tenant_id', 'slug']);\n$table->index(['published', 'created_at']);\n```\n\nIndexes should follow actual filters, joins, and ordering. Give custom names when predictable rollback names are useful.\n\n## Foreign keys\n\n```php\n$table->foreignId('user_id', nullable: true)\n    ->constrained()\n    ->setNullOnDelete();\n```\n\n`foreignId()` defaults to non-nullable. Specify nullability when creating the foreign ID, especially for `setNullOnDelete()`. Use `foreign()` and its constraint builder when references do not follow naming conventions. Match the referenced key type and create referenced tables first.\n\n## Alter and inspect tables\n\n```php\nSchema::table('posts', function (Blueprint $table) {\n    $table->string('slug')->nullable();\n});\n\n$exists = Schema::hasTable('posts');\n$columns = Schema::getColumnListing('posts');\n```\n\nAlter helpers include `dropColumn`, `renameColumn`, `dropIndex`, `dropUnique`, `dropPrimary`, `dropFullText`, `dropSpatialIndex`, and `dropForeign`. `Schema::rename()`, `drop()`, `dropIfExists()`, `hasColumn()`, and `hasColumns()` provide table-level operations.\n\n`withoutForeignKeyConstraints($callback)` wraps deliberate maintenance work; `enableForeignKeyConstraints()` and `disableForeignKeyConstraints()` expose direct control. Re-enable constraints after maintenance and validate data consistency.\n\n## Production changes\n\nBack up data, test both directions on a representative database, and consider locking and deployment order for large tables. For portable migrations, test with the production driver, not only SQLite. The [API reference](./api-reference.md) includes Blueprint, Column, ForeignKeyConstraint, and Schema declarations.\n\n## Add soft deletes to an existing table\n\nGenerate a new migration such as `php spark make:migration add_deleted_at_to_posts`, then add the nullable timestamp:\n\n```php\n<?php\n\nuse Spark\\Database\\Schema\\Blueprint;\nuse Spark\\Database\\Schema\\Schema;\n\nreturn new class {\n    public function up(): void\n    {\n        Schema::table('posts', function (Blueprint $table) {\n            $table->softDeletes();\n        });\n    }\n\n    public function down(): void\n    {\n        Schema::table('posts', function (Blueprint $table) {\n            $table->dropColumn('deleted_at');\n        });\n    }\n};\n```\n\nExisting rows have a null deletion timestamp and remain active. Deploy the column before enabling the model. Before dropping it, disable the model's soft-delete behavior; dropping the column loses archive-state information, not the archived rows themselves.\n\n`softDeletes()` takes no arguments and returns `void`, so do not chain modifiers onto it. For a custom name, use `$table->timestamp('archived_at')->nullable()` and the model's `SOFT_DELETE_COLUMN` constant. There is no `dropSoftDeletes()` helper; use `dropColumn()`.\n\nA normal unique index still includes archived rows. Archiving also does not trigger foreign-key `ON DELETE` actions, because the SQL operation is an update. See [Soft deletes](./soft-deletes.md#relationships-joins-and-uniqueness) before designing restore/purge workflows.\n\n## Migration history and failures\n\nApplied filenames are recorded in `database/migrations.json`, not in a SQL migrations table. The runner sorts PHP files and applies names starting with `migration_`; `seed_` files participate with `--seed`. Generated files follow these conventions. Rollback uses reverse recorded order, with one file by default or the requested step count.\n\nPreserve the history file alongside the database it describes, including across deployments. A successful file is recorded; if a later migration fails, completed entries are retained. The runner does not automatically wrap each file in a transaction, so a failure partway through a file may leave partial DDL changes. Inspect the database and repair that migration before retrying. Do not delete the history file to retry a partially applied schema.\n\nA missing rollback file remains in the history even when another file rolls back successfully. Malformed history raises `InvalidMigrationFile` instead of replaying migrations. Run one migration process at a time; the file history is not a distributed migration lock.\n\nFor custom tooling, `new Spark\\Database\\Migration($folder, $historyFile)` selects another migration directory and history file. It does not select another database connection.\n\n## Explicit constraints and naming\n\nSpecify `required()` for columns that must reject null. `string()` and most other type methods do not automatically append `NOT NULL`. Use `nullable()` when null is intentional.\n\n```php\n$table->unsignedBigInteger('owner_id')->required();\n$table->foreign('owner_id', 'posts_owner_fk')\n    ->references('id')->on('users')\n    ->cascadeOnUpdate()->restrictOnDelete();\n```\n\nComposite foreign keys use matching arrays for local and referenced columns. Actions include `cascadeOnDelete`, `restrictOnDelete`, `noActionOnDelete`, `setNullOnDelete`, `setDefaultOnDelete`, and the corresponding `OnUpdate` methods. A SET NULL action requires nullable local columns. SQLite and PostgreSQL support SET DEFAULT; MySQL support depends on the storage engine. Choose actions supported by your production engine.\n\nName indexes explicitly when migrations need stable cross-driver rollback references. Generated names exceeding the driver limit now include a hash suffix. To drop an index created by an older release that truncated the name, pass its actual existing name explicitly. `dropIndex('posts_status_idx')` addresses an index name; `dropIndex(['status'])` asks the grammar to derive its conventional name. The same distinction applies to unique/foreign-key drops. Table options `engine()`, `charset()`, and `collation()` affect MySQL.\n\n## Inspect SQL and driver limits\n\n```php\n$blueprint = new Blueprint('posts');\n$blueprint->timestamp('deleted_at')->nullable();\n$statements = $blueprint->compileAlterStatements();\n```\n\n`compileCreateStatements()` / `compileAlterStatements()` return statement arrays without executing them. Their string equivalents are useful for inspection. `Schema::rawSql()` and `Schema::execute()` execute trusted SQL without value-binding arguments. Schema uses the application DB and refreshes its cached connection/grammar when that DB is replaced. Creating a separate DB wrapper does not switch migrations to it.\n\n| Operation | Current implementation |\n| --- | --- |\n| Add/drop a foreign key on an existing SQLite table | Raises `SqliteAlterFailedException`; define keys at creation or rebuild deliberately |\n| Add/drop a primary key on SQLite | Not supported by the grammar |\n| Drop columns | Uses native ALTER TABLE; SQLite emits one statement per column (requires SQLite 3.35 or newer) |\n| Change an existing column type | No declared `change()` modifier; use reviewed driver-specific SQL or a rebuild |\n| Full-text indexes | Native FULLTEXT on MySQL; other drivers fall back to ordinary index SQL |\n| Spatial indexes | Native MySQL form / PostgreSQL GiST; SQLite falls back to ordinary index SQL |\n| Automatic timestamp-on-update modifier | Emitted for MySQL only |\n\n`withoutForeignKeyConstraints()` restores the previous setting, including when nested or when the callback throws. SQLite requires toggling before a transaction starts; attempting it inside a transaction raises `LogicException`. PostgreSQL raises `LogicException` for these toggle helpers because deferring a constraint is not disabling it. For PostgreSQL, define appropriate DEFERRABLE constraints explicitly and use `SET CONSTRAINTS` inside a transaction. Test schema changes on the production driver.\n";export{e as default};
