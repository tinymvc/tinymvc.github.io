const e="## Helper conventions\n\nGlobal helpers provide short access to framework services and value utilities. Many overloaded helpers return a service when called without a value and perform an operation when given arguments. Check the signature rather than guessing from a familiar name.\n\nThe [API reference](./api-reference.md) includes every named declaration in `Foundation/helpers.php`, `Support/helpers.php`, and `Support/functions.php`, with exact parameters and source links.\n\n## Application and configuration\n\n| Helpers | Purpose |\n| --- | --- |\n| `app`, `get`, `call`, `has` | Resolve/invoke/inspect container services |\n| `bind`, `singleton` | Register dependencies |\n| `config` | Read or override configuration |\n| `env`, `envs` | Environment values |\n| `is_cli`, `is_web`, `is_debug_mode`, `windows_os` | Runtime checks |\n| `defer` | Register post-response work |\n\nConfiguration files should read environment values; application services should normally read `config()`.\n\n## HTTP and identity\n\n| Helpers | Purpose |\n| --- | --- |\n| `request`, `input` | Current request and input |\n| `response`, `json` | HTTP responses |\n| `redirect`, `to_route`, `back` | Redirect responses |\n| `session`, `cookie` | Browser state |\n| `validator`, `errors`, `old` | Validation and form feedback |\n| `csrf_token`, `csrf`, `method` | Form protection and method override |\n| `auth`, `user`, `is_guest`, `is_logged` | Authentication |\n| `gate`, `can`, `canAny`, `cannot`, `authorize` | Permission checks |\n| `abort`, `abort_if`, `abort_unless` | Stop with an HTTP error |\n\n`session('key', $default)` reads; use an array to write. `validator()` returns a Validator, not the validated data. `lock('key')` acquires a lock and returns a boolean; `lock(name: 'default')` returns the service.\n\nAuthentication helpers accept an optional guard: `auth('admin')`, `is_logged('admin')`, and `is_guest('admin')`. For `user()`, the guard is the third parameter: `user('email', 'Guest', 'admin')` or `user(guard: 'admin')`. Omitting it uses the default identity. Register named guards first as shown in [Authentication](./authentication.md).\n\n## Data and services\n\n| Helpers | Purpose |\n| --- | --- |\n| `database`, `db`, `query`, `connect_db` | Database wrapper and query builders |\n| `cache`, `unload_cache`, `lock` | Cached data and coordination |\n| `job`, `dispatch` | Queue work |\n| `event` | Event registration/dispatch |\n| `pipeline`, `concurrency` | Ordered or independent work |\n| `command` | Console command registration |\n| `mailer`, `http` | Outbound mail and HTTP |\n| `hashing`, `hasher`, `passcode`, `bcrypt`, `encrypt`, `decrypt` | Hash/encryption utilities |\n| `now`, `carbon` | Dates |\n| `paginator` | Pagination |\n\n`http()` returns the client; `http('GET', $url)` sends a request. The first positional argument is the method. `connect_db()` returns a query builder. Queue helper callbacks must match the current string/array job contract.\n\n## Views, URLs, and files\n\n| Helpers | Purpose |\n| --- | --- |\n| `view`, `blade`, `fireline` | Render templates |\n| `vite` | Asset tags and manifest integration |\n| `__`, `_e` | Translation and escaped translation |\n| `router`, `route_url`, `route` | Router and named URLs |\n| `url`, `home_url`, `request_url` | Application/request URLs |\n| `asset_url`, `asset`, `media_url`, `media` | Public asset/media URLs |\n| `root_dir`, `resource_dir`, `app_dir` | Application filesystem paths |\n| `storage_dir`, `lang_dir`, `upload_dir`, `views_dir`, `temp_dir` | Configured directories |\n| `dir_path` | Normalize a filesystem path |\n| `disk` | Select a configured local, public, or S3-compatible disk |\n| `uploader`, `filemanager`, `fm`, `image` | File utilities |\n\nA filesystem path and a URL are different values. Use `upload_dir($path)` to find a stored file and `media_url($path)` to generate its public URL when it is intended to be public.\n\n`storage('s3')` constructs a named disk; `storage()` uses `storage.default`. Use `$disk->url($key)` for that disk's public URL and `$disk->temporaryUrl($key, 300)` for an authorized S3 download. The global `uploader()` helper still uses `app.upload_dir`; select `$disk->uploader(...)` to store HTTP uploads on a disk. See [File storage & disks](./storage.md).\n\n## URL objects\n\n`url()` and `route()` return `Spark\\Url`, which can be converted to a string:\n\n```php\n$next = url('/posts')->withQuery(['page' => 2])->withFragment('results');\n$href = (string) $next;\n```\n\nInspect with `getScheme`, `getHost`, `getPort`, `getPath`, `getQueryParams`, `getFragment`, and `getUrl`. Transform with `withPath`, `withScheme`, `withHost`, `withPort`, `withQuery`, `withQueryString`, `withQueryParam`, `withoutQuery`, `withFragment`, and `withoutFragment`.\n\n`is`, `matches`, `matchesAny`, and `matchesAll` help with route/menu matching. `isSecure`, `isRelative`, `isAbsolute`, `getBaseUrl`, `getPathUrl`, `getDomain`, and `getSubdomain` expose URL characteristics. `parse()` and `fromComponents()` create objects from external representations; validate a destination separately before redirecting to it.\n\n## Arrays, strings, and control flow\n\nSupport helpers include `collect`, `str`, `data_get`, `data_set`, `data_fill`, `data_forget`, `head`, `last`, `object_get`, `arr_from_set`, `blank`, `filled`, `e`, `value`, `when`, `tap`, `with`, `transform`, `literal`, `throw_if`, `throw_unless`, and `enum_value`.\n\nReflection helpers include `class_basename`, `class_uses_recursive`, and `trait_uses_recursive`. `preg_replace_array` supports sequential replacements. Use the reference for argument order and by-reference behavior.\n\n## Debugging\n\n`dump()` displays values, `dd()` displays and stops, `tracer()` accesses diagnostics, and `tracer_log()` logs a message. Keep diagnostic output and secrets out of production responses. See [Errors & debugging](./errors.md).\n";export{e as default};
